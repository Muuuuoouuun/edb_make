/*
 * Generated by scripts/build_frontend_bundle.mjs.
 * Do not edit directly; update ui_prototype/*.jsx and rebuild.
 */

/* art.jsx */
const ART_KINDS = {
  'geometry-circle': function CircleArt({
    mode
  }) {
    const ink = mode === 'chalk' ? '#f4ede0' : '#1a1a1a';
    const accent = mode === 'chalk' ? '#9ec9f0' : '#2f6fed';
    const fill = mode === 'chalk' ? 'rgba(244,237,224,.06)' : '#f4f5f7';
    const sw = mode === 'chalk' ? 2.2 : 1.6;
    const filter = mode === 'chalk' ? 'url(#chalkRough)' : undefined;
    return React.createElement("svg", {
      viewBox: "0 0 200 150",
      xmlns: "http://www.w3.org/2000/svg",
      preserveAspectRatio: "xMidYMid meet"
    }, React.createElement("defs", null, React.createElement("filter", {
      id: "chalkRough",
      x: "-5%",
      y: "-5%",
      width: "110%",
      height: "110%"
    }, React.createElement("feTurbulence", {
      type: "fractalNoise",
      baseFrequency: "1.4",
      numOctaves: "2",
      seed: "3"
    }), React.createElement("feDisplacementMap", {
      in: "SourceGraphic",
      scale: "0.8"
    }))), React.createElement("g", {
      filter: filter
    }, React.createElement("circle", {
      cx: "100",
      cy: "80",
      r: "42",
      fill: fill,
      stroke: ink,
      strokeWidth: sw
    }), React.createElement("line", {
      x1: "100",
      y1: "80",
      x2: "142",
      y2: "80",
      stroke: ink,
      strokeWidth: sw
    }), React.createElement("line", {
      x1: "42",
      y1: "120",
      x2: "170",
      y2: "40",
      stroke: accent,
      strokeWidth: sw
    }), React.createElement("circle", {
      cx: "100",
      cy: "80",
      r: "2",
      fill: ink
    }), React.createElement("circle", {
      cx: "142",
      cy: "80",
      r: "2",
      fill: ink
    }), React.createElement("text", {
      x: "96",
      y: "76",
      fontSize: "9",
      fontFamily: "serif",
      fontStyle: "italic",
      fill: ink
    }, "O"), React.createElement("text", {
      x: "145",
      y: "76",
      fontSize: "9",
      fontFamily: "serif",
      fontStyle: "italic",
      fill: ink
    }, "P"), React.createElement("text", {
      x: "118",
      y: "92",
      fontSize: "8",
      fontFamily: "serif",
      fontStyle: "italic",
      fill: ink
    }, "r")));
  },
  'equation': function EqArt({
    mode
  }) {
    const ink = mode === 'chalk' ? '#f4ede0' : '#1a1a1a';
    const accent = mode === 'chalk' ? '#f6d365' : '#d54848';
    return React.createElement("svg", {
      viewBox: "0 0 200 150",
      xmlns: "http://www.w3.org/2000/svg",
      preserveAspectRatio: "xMidYMid meet"
    }, React.createElement("g", {
      fontFamily: "'Times New Roman', serif",
      fill: ink
    }, React.createElement("text", {
      x: "100",
      y: "48",
      fontSize: "22",
      fontStyle: "italic",
      textAnchor: "middle"
    }, "ax\xB2 + bx + c = 0"), React.createElement("line", {
      x1: "40",
      y1: "68",
      x2: "160",
      y2: "68",
      stroke: ink,
      strokeWidth: "1"
    }), React.createElement("text", {
      x: "100",
      y: "108",
      fontSize: "26",
      fontStyle: "italic",
      textAnchor: "middle"
    }, "x = ", React.createElement("tspan", {
      fontSize: "14"
    }, "\u2212b \xB1 \u221A(b\xB2\u22124ac)")), React.createElement("line", {
      x1: "56",
      y1: "118",
      x2: "144",
      y2: "118",
      stroke: ink,
      strokeWidth: "1.4"
    }), React.createElement("text", {
      x: "100",
      y: "134",
      fontSize: "14",
      fontStyle: "italic",
      textAnchor: "middle"
    }, "2a"), React.createElement("text", {
      x: "14",
      y: "42",
      fontSize: "11",
      fill: accent,
      fontWeight: "600"
    }, "\u2460"), React.createElement("text", {
      x: "14",
      y: "106",
      fontSize: "11",
      fill: accent,
      fontWeight: "600"
    }, "\u2461")));
  },
  'table': function TableArt({
    mode
  }) {
    const ink = mode === 'chalk' ? '#f4ede0' : '#1a1a1a';
    const hdr = mode === 'chalk' ? '#f5a7b8' : '#2f6fed';
    const cells = [['θ', 'sin', 'cos', 'tan'], ['30°', '½', '√3/2', '√3/3'], ['45°', '√2/2', '√2/2', '1'], ['60°', '√3/2', '½', '√3']];
    return React.createElement("svg", {
      viewBox: "0 0 200 150",
      xmlns: "http://www.w3.org/2000/svg",
      preserveAspectRatio: "xMidYMid meet"
    }, React.createElement("g", {
      fontFamily: "'Times New Roman', serif",
      fill: ink
    }, cells.map((row, ri) => row.map((c, ci) => {
      const x = 24 + ci * 40,
        y = 32 + ri * 26;
      const isHdr = ri === 0;
      return React.createElement("g", {
        key: `${ri}-${ci}`
      }, React.createElement("rect", {
        x: x - 18,
        y: y - 14,
        width: "36",
        height: "22",
        fill: "none",
        stroke: ink,
        strokeWidth: ".8",
        opacity: ".6"
      }), React.createElement("text", {
        x: x,
        y: y,
        fontSize: "10",
        textAnchor: "middle",
        fill: isHdr ? hdr : ink,
        fontStyle: isHdr ? 'normal' : 'italic',
        fontWeight: isHdr ? 600 : 400
      }, c));
    }))));
  },
  'graph': function GraphArt({
    mode
  }) {
    const ink = mode === 'chalk' ? '#f4ede0' : '#1a1a1a';
    const curve = mode === 'chalk' ? '#9ec9f0' : '#2f6fed';
    const curve2 = mode === 'chalk' ? '#f5a7b8' : '#d54848';
    const grid = mode === 'chalk' ? 'rgba(244,237,224,.15)' : '#e6e8ec';
    return React.createElement("svg", {
      viewBox: "0 0 200 150",
      xmlns: "http://www.w3.org/2000/svg",
      preserveAspectRatio: "xMidYMid meet"
    }, [...Array(9)].map((_, i) => React.createElement("line", {
      key: 'v' + i,
      x1: 20 + i * 20,
      y1: "20",
      x2: 20 + i * 20,
      y2: "130",
      stroke: grid,
      strokeWidth: ".5"
    })), [...Array(6)].map((_, i) => React.createElement("line", {
      key: 'h' + i,
      x1: "20",
      y1: 20 + i * 22,
      x2: "180",
      y2: 20 + i * 22,
      stroke: grid,
      strokeWidth: ".5"
    })), React.createElement("line", {
      x1: "20",
      y1: "75",
      x2: "184",
      y2: "75",
      stroke: ink,
      strokeWidth: "1.2",
      markerEnd: "url(#arr)"
    }), React.createElement("line", {
      x1: "100",
      y1: "135",
      x2: "100",
      y2: "16",
      stroke: ink,
      strokeWidth: "1.2",
      markerEnd: "url(#arr)"
    }), React.createElement("defs", null, React.createElement("marker", {
      id: "arr",
      viewBox: "0 0 10 10",
      refX: "8",
      refY: "5",
      markerWidth: "5",
      markerHeight: "5",
      orient: "auto"
    }, React.createElement("path", {
      d: "M0,0 L10,5 L0,10 z",
      fill: ink
    }))), React.createElement("path", {
      d: "M40,30 Q100,160 160,30",
      fill: "none",
      stroke: curve,
      strokeWidth: "2"
    }), React.createElement("path", {
      d: "M30,120 L170,40",
      fill: "none",
      stroke: curve2,
      strokeWidth: "1.6",
      strokeDasharray: "3 3"
    }), React.createElement("text", {
      x: "170",
      y: "36",
      fontSize: "9",
      fontFamily: "serif",
      fontStyle: "italic",
      fill: curve2
    }, "y=x"), React.createElement("text", {
      x: "40",
      y: "40",
      fontSize: "9",
      fontFamily: "serif",
      fontStyle: "italic",
      fill: curve
    }, "y=x\xB2"));
  },
  'geometry-triangles': function TriArt({
    mode
  }) {
    const ink = mode === 'chalk' ? '#f4ede0' : '#1a1a1a';
    const fill1 = mode === 'chalk' ? 'rgba(158,201,240,.18)' : 'rgba(47,111,237,.08)';
    const fill2 = mode === 'chalk' ? 'rgba(245,167,184,.18)' : 'rgba(213,72,72,.08)';
    return React.createElement("svg", {
      viewBox: "0 0 200 150",
      xmlns: "http://www.w3.org/2000/svg",
      preserveAspectRatio: "xMidYMid meet"
    }, React.createElement("polygon", {
      points: "30,120 90,120 60,50",
      fill: fill1,
      stroke: ink,
      strokeWidth: "1.6"
    }), React.createElement("polygon", {
      points: "110,130 188,130 149,40",
      fill: fill2,
      stroke: ink,
      strokeWidth: "1.6"
    }), React.createElement("text", {
      x: "25",
      y: "135",
      fontSize: "9",
      fontFamily: "serif",
      fontStyle: "italic",
      fill: ink
    }, "A"), React.createElement("text", {
      x: "92",
      y: "135",
      fontSize: "9",
      fontFamily: "serif",
      fontStyle: "italic",
      fill: ink
    }, "B"), React.createElement("text", {
      x: "56",
      y: "46",
      fontSize: "9",
      fontFamily: "serif",
      fontStyle: "italic",
      fill: ink
    }, "C"), React.createElement("text", {
      x: "105",
      y: "142",
      fontSize: "9",
      fontFamily: "serif",
      fontStyle: "italic",
      fill: ink
    }, "A'"), React.createElement("text", {
      x: "186",
      y: "142",
      fontSize: "9",
      fontFamily: "serif",
      fontStyle: "italic",
      fill: ink
    }, "B'"), React.createElement("text", {
      x: "143",
      y: "36",
      fontSize: "9",
      fontFamily: "serif",
      fontStyle: "italic",
      fill: ink
    }, "C'"), React.createElement("path", {
      d: "M95 90 Q105 80 110 90",
      fill: "none",
      stroke: ink,
      strokeWidth: ".8",
      strokeDasharray: "2 2"
    }), React.createElement("text", {
      x: "98",
      y: "78",
      fontSize: "7",
      fill: ink,
      opacity: ".7"
    }, "\u223D"));
  },
  'paragraph': function ParaArt({
    mode
  }) {
    const ink = mode === 'chalk' ? '#f4ede0' : '#1a1a1a';
    const accent = mode === 'chalk' ? '#f6d365' : '#aa6516';
    return React.createElement("svg", {
      viewBox: "0 0 200 150",
      xmlns: "http://www.w3.org/2000/svg",
      preserveAspectRatio: "xMidYMid meet"
    }, React.createElement("g", {
      fontFamily: "serif",
      fill: ink
    }, React.createElement("text", {
      x: "18",
      y: "32",
      fontSize: "11",
      fontWeight: "700"
    }, "[24] \uB2E4\uC74C \uAE00\uC758 \uC694\uC9C0\uB85C \uAC00\uC7A5 \uC801\uC808\uD55C \uAC83\uC740?"), ["The most successful learners in any field share", "one habit: they review what they did wrong,", "not what they did right. This deliberate", "self-correction (오답 노트) creates a feedback", "loop that compounds over time, turning", "ordinary effort into measurable progress."].map((line, i) => React.createElement("text", {
      key: i,
      x: "18",
      y: 52 + i * 13,
      fontSize: "9.5"
    }, line)), React.createElement("line", {
      x1: "18",
      y1: "65",
      x2: "60",
      y2: "65",
      stroke: accent,
      strokeWidth: "1.4",
      opacity: ".7"
    }), React.createElement("line", {
      x1: "18",
      y1: "104",
      x2: "92",
      y2: "104",
      stroke: accent,
      strokeWidth: "1.4",
      opacity: ".7"
    })));
  }
};
function ItemArt({
  kind,
  mode
}) {
  const C = ART_KINDS[kind] || ART_KINDS['equation'];
  return React.createElement(C, {
    mode: mode
  });
}
window.ItemArt = ItemArt;
window.ART_KINDS = ART_KINDS;


/* tweaks-panel.jsx */
const __TWEAKS_STYLE = `
  .twk-panel{position:fixed;right:16px;bottom:16px;z-index:2147483646;width:280px;
    max-height:calc(100vh - 32px);display:flex;flex-direction:column;
    transform:scale(var(--dc-inv-zoom,1));transform-origin:bottom right;
    background:rgba(250,249,247,.78);color:#29261b;
    -webkit-backdrop-filter:blur(24px) saturate(160%);backdrop-filter:blur(24px) saturate(160%);
    border:.5px solid rgba(255,255,255,.6);border-radius:14px;
    box-shadow:0 1px 0 rgba(255,255,255,.5) inset,0 12px 40px rgba(0,0,0,.18);
    font:11.5px/1.4 ui-sans-serif,system-ui,-apple-system,sans-serif;overflow:hidden}
  .twk-hd{display:flex;align-items:center;justify-content:space-between;
    padding:10px 8px 10px 14px;cursor:move;user-select:none}
  .twk-hd b{font-size:12px;font-weight:600;letter-spacing:.01em}
  .twk-x{appearance:none;border:0;background:transparent;color:rgba(41,38,27,.55);
    width:22px;height:22px;border-radius:6px;cursor:default;font-size:13px;line-height:1}
  .twk-x:hover{background:rgba(0,0,0,.06);color:#29261b}
  .twk-body{padding:2px 14px 14px;display:flex;flex-direction:column;gap:10px;
    overflow-y:auto;overflow-x:hidden;min-height:0;
    scrollbar-width:thin;scrollbar-color:rgba(0,0,0,.15) transparent}
  .twk-body::-webkit-scrollbar{width:8px}
  .twk-body::-webkit-scrollbar-track{background:transparent;margin:2px}
  .twk-body::-webkit-scrollbar-thumb{background:rgba(0,0,0,.15);border-radius:4px;
    border:2px solid transparent;background-clip:content-box}
  .twk-body::-webkit-scrollbar-thumb:hover{background:rgba(0,0,0,.25);
    border:2px solid transparent;background-clip:content-box}
  .twk-row{display:flex;flex-direction:column;gap:5px}
  .twk-row-h{flex-direction:row;align-items:center;justify-content:space-between;gap:10px}
  .twk-lbl{display:flex;justify-content:space-between;align-items:baseline;
    color:rgba(41,38,27,.72)}
  .twk-lbl>span:first-child{font-weight:500}
  .twk-val{color:rgba(41,38,27,.5);font-variant-numeric:tabular-nums}

  .twk-sect{font-size:10px;font-weight:600;letter-spacing:.06em;text-transform:uppercase;
    color:rgba(41,38,27,.45);padding:10px 0 0}
  .twk-sect:first-child{padding-top:0}

  .twk-field{appearance:none;box-sizing:border-box;width:100%;min-width:0;height:26px;padding:0 8px;
    border:.5px solid rgba(0,0,0,.1);border-radius:7px;
    background:rgba(255,255,255,.6);color:inherit;font:inherit;outline:none}
  .twk-field:focus{border-color:rgba(0,0,0,.25);background:rgba(255,255,255,.85)}
  select.twk-field{padding-right:22px;
    background-image:url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='10' height='6' viewBox='0 0 10 6'><path fill='rgba(0,0,0,.5)' d='M0 0h10L5 6z'/></svg>");
    background-repeat:no-repeat;background-position:right 8px center}

  .twk-slider{appearance:none;-webkit-appearance:none;width:100%;height:4px;margin:6px 0;
    border-radius:999px;background:rgba(0,0,0,.12);outline:none}
  .twk-slider::-webkit-slider-thumb{-webkit-appearance:none;appearance:none;
    width:14px;height:14px;border-radius:50%;background:#fff;
    border:.5px solid rgba(0,0,0,.12);box-shadow:0 1px 3px rgba(0,0,0,.2);cursor:default}
  .twk-slider::-moz-range-thumb{width:14px;height:14px;border-radius:50%;
    background:#fff;border:.5px solid rgba(0,0,0,.12);box-shadow:0 1px 3px rgba(0,0,0,.2);cursor:default}

  .twk-seg{position:relative;display:flex;padding:2px;border-radius:8px;
    background:rgba(0,0,0,.06);user-select:none}
  .twk-seg-thumb{position:absolute;top:2px;bottom:2px;border-radius:6px;
    background:rgba(255,255,255,.9);box-shadow:0 1px 2px rgba(0,0,0,.12);
    transition:left .15s cubic-bezier(.3,.7,.4,1),width .15s}
  .twk-seg.dragging .twk-seg-thumb{transition:none}
  .twk-seg button{appearance:none;position:relative;z-index:1;flex:1;border:0;
    background:transparent;color:inherit;font:inherit;font-weight:500;min-height:22px;
    border-radius:6px;cursor:default;padding:4px 6px;line-height:1.2;
    overflow-wrap:anywhere}

  .twk-toggle{position:relative;width:32px;height:18px;border:0;border-radius:999px;
    background:rgba(0,0,0,.15);transition:background .15s;cursor:default;padding:0}
  .twk-toggle[data-on="1"]{background:#34c759}
  .twk-toggle i{position:absolute;top:2px;left:2px;width:14px;height:14px;border-radius:50%;
    background:#fff;box-shadow:0 1px 2px rgba(0,0,0,.25);transition:transform .15s}
  .twk-toggle[data-on="1"] i{transform:translateX(14px)}

  .twk-num{display:flex;align-items:center;box-sizing:border-box;min-width:0;height:26px;padding:0 0 0 8px;
    border:.5px solid rgba(0,0,0,.1);border-radius:7px;background:rgba(255,255,255,.6)}
  .twk-num-lbl{font-weight:500;color:rgba(41,38,27,.6);cursor:ew-resize;
    user-select:none;padding-right:8px}
  .twk-num input{flex:1;min-width:0;height:100%;border:0;background:transparent;
    font:inherit;font-variant-numeric:tabular-nums;text-align:right;padding:0 8px 0 0;
    outline:none;color:inherit;-moz-appearance:textfield}
  .twk-num input::-webkit-inner-spin-button,.twk-num input::-webkit-outer-spin-button{
    -webkit-appearance:none;margin:0}
  .twk-num-unit{padding-right:8px;color:rgba(41,38,27,.45)}

  .twk-btn{appearance:none;height:26px;padding:0 12px;border:0;border-radius:7px;
    background:rgba(0,0,0,.78);color:#fff;font:inherit;font-weight:500;cursor:default}
  .twk-btn:hover{background:rgba(0,0,0,.88)}
  .twk-btn.secondary{background:rgba(0,0,0,.06);color:inherit}
  .twk-btn.secondary:hover{background:rgba(0,0,0,.1)}

  .twk-swatch{appearance:none;-webkit-appearance:none;width:56px;height:22px;
    border:.5px solid rgba(0,0,0,.1);border-radius:6px;padding:0;cursor:default;
    background:transparent;flex-shrink:0}
  .twk-swatch::-webkit-color-swatch-wrapper{padding:0}
  .twk-swatch::-webkit-color-swatch{border:0;border-radius:5.5px}
  .twk-swatch::-moz-color-swatch{border:0;border-radius:5.5px}

  .twk-chips{display:flex;gap:6px}
  .twk-chip{position:relative;appearance:none;flex:1;min-width:0;height:46px;
    padding:0;border:0;border-radius:6px;overflow:hidden;cursor:default;
    box-shadow:0 0 0 .5px rgba(0,0,0,.12),0 1px 2px rgba(0,0,0,.06);
    transition:transform .12s cubic-bezier(.3,.7,.4,1),box-shadow .12s}
  .twk-chip:hover{transform:translateY(-1px);
    box-shadow:0 0 0 .5px rgba(0,0,0,.18),0 4px 10px rgba(0,0,0,.12)}
  .twk-chip[data-on="1"]{box-shadow:0 0 0 1.5px rgba(0,0,0,.85),
    0 2px 6px rgba(0,0,0,.15)}
  .twk-chip>span{position:absolute;top:0;bottom:0;right:0;width:34%;
    display:flex;flex-direction:column;box-shadow:-1px 0 0 rgba(0,0,0,.1)}
  .twk-chip>span>i{flex:1;box-shadow:0 -1px 0 rgba(0,0,0,.1)}
  .twk-chip>span>i:first-child{box-shadow:none}
  .twk-chip svg{position:absolute;top:6px;left:6px;width:13px;height:13px;
    filter:drop-shadow(0 1px 1px rgba(0,0,0,.3))}
`;
function useTweaks(defaults) {
  const [values, setValues] = React.useState(defaults);
  const setTweak = React.useCallback((keyOrEdits, val) => {
    const edits = typeof keyOrEdits === 'object' && keyOrEdits !== null ? keyOrEdits : {
      [keyOrEdits]: val
    };
    setValues(prev => ({
      ...prev,
      ...edits
    }));
    window.parent.postMessage({
      type: '__edit_mode_set_keys',
      edits
    }, '*');
    window.dispatchEvent(new CustomEvent('tweakchange', {
      detail: edits
    }));
  }, []);
  return [values, setTweak];
}
function TweaksPanel({
  title = 'Tweaks',
  noDeckControls = false,
  children
}) {
  const [open, setOpen] = React.useState(false);
  const dragRef = React.useRef(null);
  const hasDeckStage = React.useMemo(() => typeof document !== 'undefined' && !!document.querySelector('deck-stage'), []);
  const [railEnabled, setRailEnabled] = React.useState(() => hasDeckStage && !!document.querySelector('deck-stage')?._railEnabled);
  React.useEffect(() => {
    if (!hasDeckStage || railEnabled) return undefined;
    const onMsg = e => {
      if (e.data && e.data.type === '__omelette_rail_enabled') setRailEnabled(true);
    };
    window.addEventListener('message', onMsg);
    return () => window.removeEventListener('message', onMsg);
  }, [hasDeckStage, railEnabled]);
  const [railVisible, setRailVisible] = React.useState(() => {
    try {
      return localStorage.getItem('deck-stage.railVisible') !== '0';
    } catch (e) {
      return true;
    }
  });
  const toggleRail = on => {
    setRailVisible(on);
    window.postMessage({
      type: '__deck_rail_visible',
      on
    }, '*');
  };
  const offsetRef = React.useRef({
    x: 16,
    y: 16
  });
  const PAD = 16;
  const clampToViewport = React.useCallback(() => {
    const panel = dragRef.current;
    if (!panel) return;
    const w = panel.offsetWidth,
      h = panel.offsetHeight;
    const maxRight = Math.max(PAD, window.innerWidth - w - PAD);
    const maxBottom = Math.max(PAD, window.innerHeight - h - PAD);
    offsetRef.current = {
      x: Math.min(maxRight, Math.max(PAD, offsetRef.current.x)),
      y: Math.min(maxBottom, Math.max(PAD, offsetRef.current.y))
    };
    panel.style.right = offsetRef.current.x + 'px';
    panel.style.bottom = offsetRef.current.y + 'px';
  }, []);
  React.useEffect(() => {
    if (!open) return;
    clampToViewport();
    if (typeof ResizeObserver === 'undefined') {
      window.addEventListener('resize', clampToViewport);
      return () => window.removeEventListener('resize', clampToViewport);
    }
    const ro = new ResizeObserver(clampToViewport);
    ro.observe(document.documentElement);
    return () => ro.disconnect();
  }, [open, clampToViewport]);
  React.useEffect(() => {
    const onMsg = e => {
      const t = e?.data?.type;
      if (t === '__activate_edit_mode') setOpen(true);else if (t === '__deactivate_edit_mode') setOpen(false);
    };
    window.addEventListener('message', onMsg);
    window.parent.postMessage({
      type: '__edit_mode_available'
    }, '*');
    return () => window.removeEventListener('message', onMsg);
  }, []);
  const dismiss = () => {
    setOpen(false);
    window.parent.postMessage({
      type: '__edit_mode_dismissed'
    }, '*');
  };
  const onDragStart = e => {
    const panel = dragRef.current;
    if (!panel) return;
    const r = panel.getBoundingClientRect();
    const sx = e.clientX,
      sy = e.clientY;
    const startRight = window.innerWidth - r.right;
    const startBottom = window.innerHeight - r.bottom;
    const move = ev => {
      offsetRef.current = {
        x: startRight - (ev.clientX - sx),
        y: startBottom - (ev.clientY - sy)
      };
      clampToViewport();
    };
    const up = () => {
      window.removeEventListener('mousemove', move);
      window.removeEventListener('mouseup', up);
    };
    window.addEventListener('mousemove', move);
    window.addEventListener('mouseup', up);
  };
  if (!open) return null;
  return React.createElement(React.Fragment, null, React.createElement("style", null, __TWEAKS_STYLE), React.createElement("div", {
    ref: dragRef,
    className: "twk-panel",
    "data-noncommentable": "",
    style: {
      right: offsetRef.current.x,
      bottom: offsetRef.current.y
    }
  }, React.createElement("div", {
    className: "twk-hd",
    onMouseDown: onDragStart
  }, React.createElement("b", null, title), React.createElement("button", {
    className: "twk-x",
    "aria-label": "Close tweaks",
    onMouseDown: e => e.stopPropagation(),
    onClick: dismiss
  }, "\u2715")), React.createElement("div", {
    className: "twk-body"
  }, children, hasDeckStage && railEnabled && !noDeckControls && React.createElement(TweakSection, {
    label: "Deck"
  }, React.createElement(TweakToggle, {
    label: "Thumbnail rail",
    value: railVisible,
    onChange: toggleRail
  })))));
}
function TweakSection({
  label,
  children
}) {
  return React.createElement(React.Fragment, null, React.createElement("div", {
    className: "twk-sect"
  }, label), children);
}
function TweakRow({
  label,
  value,
  children,
  inline = false
}) {
  return React.createElement("div", {
    className: inline ? 'twk-row twk-row-h' : 'twk-row'
  }, React.createElement("div", {
    className: "twk-lbl"
  }, React.createElement("span", null, label), value != null && React.createElement("span", {
    className: "twk-val"
  }, value)), children);
}
function TweakSlider({
  label,
  value,
  min = 0,
  max = 100,
  step = 1,
  unit = '',
  onChange
}) {
  return React.createElement(TweakRow, {
    label: label,
    value: `${value}${unit}`
  }, React.createElement("input", {
    type: "range",
    className: "twk-slider",
    min: min,
    max: max,
    step: step,
    value: value,
    onChange: e => onChange(Number(e.target.value))
  }));
}
function TweakToggle({
  label,
  value,
  onChange
}) {
  return React.createElement("div", {
    className: "twk-row twk-row-h"
  }, React.createElement("div", {
    className: "twk-lbl"
  }, React.createElement("span", null, label)), React.createElement("button", {
    type: "button",
    className: "twk-toggle",
    "data-on": value ? '1' : '0',
    role: "switch",
    "aria-checked": !!value,
    onClick: () => onChange(!value)
  }, React.createElement("i", null)));
}
function TweakRadio({
  label,
  value,
  options,
  onChange
}) {
  const trackRef = React.useRef(null);
  const [dragging, setDragging] = React.useState(false);
  const valueRef = React.useRef(value);
  valueRef.current = value;
  const labelLen = o => String(typeof o === 'object' ? o.label : o).length;
  const maxLen = options.reduce((m, o) => Math.max(m, labelLen(o)), 0);
  const fitsAsSegments = maxLen <= ({
    2: 16,
    3: 10
  }[options.length] ?? 0);
  if (!fitsAsSegments) {
    const resolve = s => {
      const m = options.find(o => String(typeof o === 'object' ? o.value : o) === s);
      return m === undefined ? s : typeof m === 'object' ? m.value : m;
    };
    return React.createElement(TweakSelect, {
      label: label,
      value: value,
      options: options,
      onChange: s => onChange(resolve(s))
    });
  }
  const opts = options.map(o => typeof o === 'object' ? o : {
    value: o,
    label: o
  });
  const idx = Math.max(0, opts.findIndex(o => o.value === value));
  const n = opts.length;
  const segAt = clientX => {
    const r = trackRef.current.getBoundingClientRect();
    const inner = r.width - 4;
    const i = Math.floor((clientX - r.left - 2) / inner * n);
    return opts[Math.max(0, Math.min(n - 1, i))].value;
  };
  const onPointerDown = e => {
    setDragging(true);
    const v0 = segAt(e.clientX);
    if (v0 !== valueRef.current) onChange(v0);
    const move = ev => {
      if (!trackRef.current) return;
      const v = segAt(ev.clientX);
      if (v !== valueRef.current) onChange(v);
    };
    const up = () => {
      setDragging(false);
      window.removeEventListener('pointermove', move);
      window.removeEventListener('pointerup', up);
    };
    window.addEventListener('pointermove', move);
    window.addEventListener('pointerup', up);
  };
  return React.createElement(TweakRow, {
    label: label
  }, React.createElement("div", {
    ref: trackRef,
    role: "radiogroup",
    onPointerDown: onPointerDown,
    className: dragging ? 'twk-seg dragging' : 'twk-seg'
  }, React.createElement("div", {
    className: "twk-seg-thumb",
    style: {
      left: `calc(2px + ${idx} * (100% - 4px) / ${n})`,
      width: `calc((100% - 4px) / ${n})`
    }
  }), opts.map(o => React.createElement("button", {
    key: o.value,
    type: "button",
    role: "radio",
    "aria-checked": o.value === value
  }, o.label))));
}
function TweakSelect({
  label,
  value,
  options,
  onChange
}) {
  return React.createElement(TweakRow, {
    label: label
  }, React.createElement("select", {
    className: "twk-field",
    value: value,
    onChange: e => onChange(e.target.value)
  }, options.map(o => {
    const v = typeof o === 'object' ? o.value : o;
    const l = typeof o === 'object' ? o.label : o;
    return React.createElement("option", {
      key: v,
      value: v
    }, l);
  })));
}
function TweakText({
  label,
  value,
  placeholder,
  onChange
}) {
  return React.createElement(TweakRow, {
    label: label
  }, React.createElement("input", {
    className: "twk-field",
    type: "text",
    value: value,
    placeholder: placeholder,
    onChange: e => onChange(e.target.value)
  }));
}
function TweakNumber({
  label,
  value,
  min,
  max,
  step = 1,
  unit = '',
  onChange
}) {
  const clamp = n => {
    if (min != null && n < min) return min;
    if (max != null && n > max) return max;
    return n;
  };
  const startRef = React.useRef({
    x: 0,
    val: 0
  });
  const onScrubStart = e => {
    e.preventDefault();
    startRef.current = {
      x: e.clientX,
      val: value
    };
    const decimals = (String(step).split('.')[1] || '').length;
    const move = ev => {
      const dx = ev.clientX - startRef.current.x;
      const raw = startRef.current.val + dx * step;
      const snapped = Math.round(raw / step) * step;
      onChange(clamp(Number(snapped.toFixed(decimals))));
    };
    const up = () => {
      window.removeEventListener('pointermove', move);
      window.removeEventListener('pointerup', up);
    };
    window.addEventListener('pointermove', move);
    window.addEventListener('pointerup', up);
  };
  return React.createElement("div", {
    className: "twk-num"
  }, React.createElement("span", {
    className: "twk-num-lbl",
    onPointerDown: onScrubStart
  }, label), React.createElement("input", {
    type: "number",
    value: value,
    min: min,
    max: max,
    step: step,
    onChange: e => onChange(clamp(Number(e.target.value)))
  }), unit && React.createElement("span", {
    className: "twk-num-unit"
  }, unit));
}
function __twkIsLight(hex) {
  const h = String(hex).replace('#', '');
  const x = h.length === 3 ? h.replace(/./g, c => c + c) : h.padEnd(6, '0');
  const n = parseInt(x.slice(0, 6), 16);
  if (Number.isNaN(n)) return true;
  const r = n >> 16 & 255,
    g = n >> 8 & 255,
    b = n & 255;
  return r * 299 + g * 587 + b * 114 > 148000;
}
const __TwkCheck = ({
  light
}) => React.createElement("svg", {
  viewBox: "0 0 14 14",
  "aria-hidden": "true"
}, React.createElement("path", {
  d: "M3 7.2 5.8 10 11 4.2",
  fill: "none",
  strokeWidth: "2.2",
  strokeLinecap: "round",
  strokeLinejoin: "round",
  stroke: light ? 'rgba(0,0,0,.78)' : '#fff'
}));
function TweakColor({
  label,
  value,
  options,
  onChange
}) {
  if (!options || !options.length) {
    return React.createElement("div", {
      className: "twk-row twk-row-h"
    }, React.createElement("div", {
      className: "twk-lbl"
    }, React.createElement("span", null, label)), React.createElement("input", {
      type: "color",
      className: "twk-swatch",
      value: value,
      onChange: e => onChange(e.target.value)
    }));
  }
  const key = o => String(JSON.stringify(o)).toLowerCase();
  const cur = key(value);
  return React.createElement(TweakRow, {
    label: label
  }, React.createElement("div", {
    className: "twk-chips",
    role: "radiogroup"
  }, options.map((o, i) => {
    const colors = Array.isArray(o) ? o : [o];
    const [hero, ...rest] = colors;
    const sup = rest.slice(0, 4);
    const on = key(o) === cur;
    return React.createElement("button", {
      key: i,
      type: "button",
      className: "twk-chip",
      role: "radio",
      "aria-checked": on,
      "data-on": on ? '1' : '0',
      "aria-label": colors.join(', '),
      title: colors.join(' · '),
      style: {
        background: hero
      },
      onClick: () => onChange(o)
    }, sup.length > 0 && React.createElement("span", null, sup.map((c, j) => React.createElement("i", {
      key: j,
      style: {
        background: c
      }
    }))), on && React.createElement(__TwkCheck, {
      light: __twkIsLight(hero)
    }));
  })));
}
function TweakButton({
  label,
  onClick,
  secondary = false
}) {
  return React.createElement("button", {
    type: "button",
    className: secondary ? 'twk-btn secondary' : 'twk-btn',
    onClick: onClick
  }, label);
}
Object.assign(window, {
  useTweaks,
  TweaksPanel,
  TweakSection,
  TweakRow,
  TweakSlider,
  TweakToggle,
  TweakRadio,
  TweakSelect,
  TweakText,
  TweakNumber,
  TweakColor,
  TweakButton
});


/* app.jsx */
const {
  useState,
  useRef,
  useEffect,
  useLayoutEffect,
  useMemo,
  useCallback
} = React;
function reportRuntimeDiagnostic(error, detail = {}) {
  const payload = {
    type: detail.type || 'runtime',
    message: error?.message || String(error || '알 수 없는 오류'),
    error,
    ...detail
  };
  try {
    console.error('[board-runtime]', payload.message, payload);
  } catch (_err) {}
  if (typeof window.EDB_REPORT_RUNTIME_ERROR === 'function') {
    window.EDB_REPORT_RUNTIME_ERROR(payload);
  }
  return payload;
}
class AppErrorBoundary extends React.Component {
  constructor(props) {
    super(props);
    this.state = {
      error: null
    };
  }
  static getDerivedStateFromError(error) {
    return {
      error
    };
  }
  componentDidCatch(error, info) {
    reportRuntimeDiagnostic(error, {
      type: 'react-render',
      componentStack: info?.componentStack || ''
    });
  }
  render() {
    if (this.state.error) {
      return React.createElement('div', {
        className: 'runtime-crash-panel',
        style: {
          margin: 16,
          padding: 16,
          border: '1px solid #ef4444',
          background: '#fff5f5',
          color: '#1f2937',
          font: '13px/1.5 system-ui,-apple-system,sans-serif',
          whiteSpace: 'pre-wrap'
        }
      }, `앱 화면을 렌더링하지 못했습니다.\n\n${this.state.error?.message || this.state.error}`);
    }
    return this.props.children;
  }
}
function requiredWindowHelper(namespace, helperName, fallback) {
  const helper = namespace && namespace[helperName];
  if (typeof helper === 'function') return helper;
  reportRuntimeDiagnostic(new Error(`필수 프론트엔드 helper 누락: ${helperName}`), {
    type: 'missing-helper',
    helperName
  });
  return fallback;
}
const TWEAK_DEFAULTS = {
  "dark": false,
  "boardColor": "#1d3a2c",
  "accent": "#2f6fed",
  "boardColumns": 2
};
const BOARD_COLORS = ['#1d3a2c', '#101418', '#264653', '#3a2f24'];
const ACCENTS = ['#2f6fed', '#6d3df0', '#1f7a4a', '#d97757'];
const SAMPLE_NAMES = ['원과 접선의 성질', '근의 공식 유도', '삼각비 특수각 표', '이차함수 그래프', '닮음 도형 예제', '영어 24번 지문', '함수의 극한', '미분계수의 정의', '적분 기본 정리', '벡터 내적 활용', '확률 조건부', '이항분포 표', '수열의 합', '점화식 풀이', '복소평면 회전', '도함수와 접선', '함수의 연속', '평균값의 정리', '곱의 미분법', '몫의 미분법', '삼각함수 합성', '로그 성질', '지수 방정식', '부등식 영역', '원의 방정식'];
const SAMPLE_SOURCES = ['고1 기하 · p.42', '개념원리 · p.15', 'PDF · 3쪽', '함수 단원 · p.78', '교재 자체 촬영', '모의고사 · 6월', '쎈 · 78번', '마플 · 단원평가', '판서노트 · 4/12', '학생 질문 캡처'];
const SAMPLE_KINDS = ['geometry-circle', 'equation', 'table', 'graph', 'geometry-triangles', 'paragraph'];
const SAMPLE_STEPS = ['raw', 'raw', 'raw', 's1', 's2', 'raw'];
const REORDER_HELPERS = window.EDB_REORDER || {};
const PUBLISH_GUARD = window.EDB_PUBLISH_GUARD || {};
const findBoardPlacementOverlaps = requiredWindowHelper(PUBLISH_GUARD, 'findBoardPlacementOverlaps', () => []);
const findPassageGroupSourceReuse = requiredWindowHelper(PUBLISH_GUARD, 'findPassageGroupSourceReuse', () => []);
const findSourceProblemOverlaps = requiredWindowHelper(PUBLISH_GUARD, 'findSourceProblemOverlaps', () => []);
const reorderItemsForDrop = REORDER_HELPERS.reorderItemsForDrop || ((items, fromId, toId, position = 'before') => {
  const sourceId = fromId == null ? '' : String(fromId);
  const targetId = toId == null ? '' : String(toId);
  if (!Array.isArray(items) || !sourceId || !targetId || sourceId === targetId) return items;
  const fromIndex = items.findIndex(item => String(item.id) === sourceId);
  const toIndex = items.findIndex(item => String(item.id) === targetId);
  if (fromIndex < 0 || toIndex < 0) return items;
  const next = items.slice();
  const [moved] = next.splice(fromIndex, 1);
  const targetIndex = next.findIndex(item => String(item.id) === targetId);
  if (targetIndex < 0) return items;
  next.splice(targetIndex + (position === 'after' ? 1 : 0), 0, moved);
  return next;
});
const dropPositionFromClientY = REORDER_HELPERS.dropPositionFromClientY || ((rect, clientY) => clientY > rect.top + rect.height / 2 ? 'after' : 'before');
const HEIGHT_BY_KIND = {
  'equation': 0.55,
  'geometry-triangles': 0.75,
  'graph': 0.85,
  'geometry-circle': 0.95,
  'table': 1.4,
  'paragraph': 1.85
};
const heightForKind = k => HEIGHT_BY_KIND[k] || 0.8;
const FIXED_LEFT_ZONE_RATIO = 1 / 3;
const DEFAULT_SLOT_HEIGHT_PAGES = 1.2;
const DEFAULT_PLACEMENT_X_RATIO = 0;
const DEFAULT_PLACEMENT_Y_RATIO = 0;
const DEFAULT_PLACEMENT_SCALE_RATIO = 1;
const PLACEMENT_SCALE_MIN = 0.6;
const PLACEMENT_SCALE_MAX = 1.6;
const PLACEMENT_NUDGE_STEP = 0.04;
const PLACEMENT_SCALE_STEP = 0.05;
const ADJACENT_RETRY_PADDING_RATIO = 0.16;
const ADJACENT_RETRY_MIN_PADDING_PX = 28;
const BOARD_DRAG_REORDER_THRESHOLD_PX = 28;
const BOARD_DRAG_AUTOSCROLL_EDGE_PX = 58;
const BOARD_DRAG_AUTOSCROLL_MAX_PX = 22;
const MANUAL_CROP_EDGE_MAX = 0.45;
const MANUAL_CROP_OUTSET_MAX = 0.60;
const MANUAL_CROP_EDGE_STEP = 0.01;
const RECENT_SESSIONS_COLLAPSED_KEY = 'edb.recentSessionsCollapsed';
const EMPTY_MANUAL_CROP = Object.freeze({
  leftRatio: 0,
  rightRatio: 0,
  topRatio: 0,
  bottomRatio: 0
});
function normalizePlacementXRatio(value) {
  const n = Number(value);
  return Number.isFinite(n) ? Math.max(0, Math.min(1, n)) : DEFAULT_PLACEMENT_X_RATIO;
}
function normalizePlacementYRatio(value) {
  const n = Number(value);
  return Number.isFinite(n) ? Math.max(0, Math.min(1, n)) : DEFAULT_PLACEMENT_Y_RATIO;
}
function normalizePlacementScaleRatio(value, maxRatio = PLACEMENT_SCALE_MAX) {
  const n = Number(value);
  const resolvedMax = Number.isFinite(Number(maxRatio)) ? Math.max(PLACEMENT_SCALE_MIN, Math.min(PLACEMENT_SCALE_MAX, Number(maxRatio))) : PLACEMENT_SCALE_MAX;
  return Number.isFinite(n) ? Math.max(PLACEMENT_SCALE_MIN, Math.min(resolvedMax, n)) : Math.min(DEFAULT_PLACEMENT_SCALE_RATIO, resolvedMax);
}
function normalizeManualCropEdgeRatio(value) {
  const n = Number(value);
  return Number.isFinite(n) ? Math.max(-MANUAL_CROP_OUTSET_MAX, Math.min(MANUAL_CROP_EDGE_MAX, n)) : 0;
}
function manualCropValue(rawCrop, camelKey, snakeKey, plainKey) {
  if (!rawCrop || typeof rawCrop !== 'object') return 0;
  const topLevelKey = `crop${camelKey.charAt(0).toUpperCase()}${camelKey.slice(1)}`;
  return rawCrop[camelKey] ?? rawCrop[topLevelKey] ?? rawCrop[snakeKey] ?? rawCrop[plainKey] ?? 0;
}
function normalizeManualCrop(rawCrop) {
  return {
    leftRatio: normalizeManualCropEdgeRatio(manualCropValue(rawCrop, 'leftRatio', 'crop_left_ratio', 'left')),
    rightRatio: normalizeManualCropEdgeRatio(manualCropValue(rawCrop, 'rightRatio', 'crop_right_ratio', 'right')),
    topRatio: normalizeManualCropEdgeRatio(manualCropValue(rawCrop, 'topRatio', 'crop_top_ratio', 'top')),
    bottomRatio: normalizeManualCropEdgeRatio(manualCropValue(rawCrop, 'bottomRatio', 'crop_bottom_ratio', 'bottom'))
  };
}
function manualCropIsActive(crop) {
  const normalized = normalizeManualCrop(crop);
  return Object.values(normalized).some(value => Math.abs(value) > 0.0001);
}
function manualCropEquals(a, b) {
  const left = normalizeManualCrop(a);
  const right = normalizeManualCrop(b);
  return ['leftRatio', 'rightRatio', 'topRatio', 'bottomRatio'].every(key => Math.abs(left[key] - right[key]) < 0.0001);
}
function manualCropPercent(value) {
  const normalized = normalizeManualCropEdgeRatio(value);
  if (normalized < -0.0001) return `+${Math.round(Math.abs(normalized) * 100)}%`;
  if (normalized > 0.0001) return `-${Math.round(normalized * 100)}%`;
  return '0%';
}
function finiteNumber(value, fallback = 0) {
  const number = Number(value);
  return Number.isFinite(number) ? number : fallback;
}
function expandBoxWithinPage(rawBox, page, ratio = ADJACENT_RETRY_PADDING_RATIO) {
  if (!rawBox || !page) return rawBox || null;
  const pageWidth = Math.max(1, finiteNumber(page.width, 1));
  const pageHeight = Math.max(1, finiteNumber(page.height, 1));
  const left = finiteNumber(rawBox.left, 0);
  const top = finiteNumber(rawBox.top, 0);
  const width = Math.max(1, finiteNumber(rawBox.width, 1));
  const height = Math.max(1, finiteNumber(rawBox.height, 1));
  const padX = Math.max(ADJACENT_RETRY_MIN_PADDING_PX, width * ratio);
  const padY = Math.max(ADJACENT_RETRY_MIN_PADDING_PX, height * ratio);
  const nextLeft = Math.max(0, left - padX);
  const nextTop = Math.max(0, top - padY);
  const nextRight = Math.min(pageWidth, left + width + padX);
  const nextBottom = Math.min(pageHeight, top + height + padY);
  return {
    left: nextLeft,
    top: nextTop,
    width: Math.max(1, nextRight - nextLeft),
    height: Math.max(1, nextBottom - nextTop)
  };
}
function replacementSourceIdFor(problem) {
  const retry = problem?.aiRetry || problem?.ai_retry || {};
  return String(retry.replacesProblemId || retry.replaces_problem_id || problem?.replacesProblemId || problem?.replaces_problem_id || '').trim();
}
function resetItemPlacement(item) {
  return {
    ...item,
    placementXRatio: DEFAULT_PLACEMENT_X_RATIO,
    placementYRatio: DEFAULT_PLACEMENT_Y_RATIO,
    placementScaleRatio: DEFAULT_PLACEMENT_SCALE_RATIO
  };
}
function snapUpPages(value, slotHeight = DEFAULT_SLOT_HEIGHT_PAGES) {
  if (!Number.isFinite(value) || value <= 0) return 0;
  return Math.ceil((value - 0.001) / slotHeight) * slotHeight;
}
function placementSlotHeightPages(item) {
  if (!item) return 0;
  const heightPages = Math.max(0.12, item.heightFrac || 0.8);
  const startPages = Number.isFinite(item.startYPages) ? Math.max(0, item.startYPages) : 0;
  const snappedNext = Number.isFinite(item.snappedNextStartYPages) ? Math.max(startPages + heightPages, item.snappedNextStartYPages) : snapUpPages(startPages + heightPages);
  return Math.max(heightPages, snappedNext - startPages);
}
function maxPlacementScaleRatio(item) {
  if (!item) return PLACEMENT_SCALE_MAX;
  const heightPages = Math.max(0.12, item.heightFrac || 0.8);
  const slotHeightPages = placementSlotHeightPages(item);
  return Math.max(PLACEMENT_SCALE_MIN, Math.min(PLACEMENT_SCALE_MAX, slotHeightPages / heightPages));
}
function verticalPlacementRoomPages(item, scaleRatio = item?.placementScaleRatio) {
  if (!item) return 0;
  const heightPages = Math.max(0.12, item.heightFrac || 0.8);
  const scale = normalizePlacementScaleRatio(scaleRatio, maxPlacementScaleRatio(item));
  return Math.max(0, placementSlotHeightPages(item) - heightPages * scale);
}
function itemSlotSpanPages(item, slotHeight = DEFAULT_SLOT_HEIGHT_PAGES) {
  if (!item) return slotHeight;
  const heightPages = Math.max(0.12, item.heightFrac || 0.8);
  const startPages = Number.isFinite(item.startYPages) ? Math.max(0, item.startYPages) : null;
  const snappedNext = Number.isFinite(item.snappedNextStartYPages) ? Math.max(0, item.snappedNextStartYPages) : null;
  const savedSpan = startPages !== null && snappedNext !== null && snappedNext > startPages ? snappedNext - startPages : 0;
  return Math.max(heightPages, savedSpan || snapUpPages(heightPages, slotHeight));
}
function reflowItemsForBoardOrder(items, slotHeight = DEFAULT_SLOT_HEIGHT_PAGES) {
  if (!Array.isArray(items)) return items;
  let cursorPages = 0;
  return items.map(item => {
    const heightPages = Math.max(0.12, item.heightFrac || 0.8);
    const startPages = snapUpPages(cursorPages, slotHeight);
    const slotSpanPages = itemSlotSpanPages(item, slotHeight);
    const snappedNextStartYPages = snapUpPages(startPages + Math.max(heightPages, slotSpanPages), slotHeight);
    const actualBottomPages = startPages + heightPages;
    const slotSpanCount = Math.max(1, Math.round((snappedNextStartYPages - startPages) / slotHeight));
    cursorPages = snappedNextStartYPages;
    return {
      ...item,
      heightFrac: heightPages,
      startYPages: Number(startPages.toFixed(6)),
      snappedNextStartYPages: Number(snappedNextStartYPages.toFixed(6)),
      actualBottomYPages: Number(actualBottomPages.toFixed(6)),
      slotSpanCount,
      overflowAmountPages: Math.max(0, heightPages - slotHeight)
    };
  });
}
const INITIAL_ITEMS = Array.from({
  length: 12
}).map((_, i) => {
  const kind = SAMPLE_KINDS[i % SAMPLE_KINDS.length];
  return {
    id: 'i' + (i + 1),
    name: SAMPLE_NAMES[i % SAMPLE_NAMES.length],
    source: SAMPLE_SOURCES[i % SAMPLE_SOURCES.length],
    type: i % 4 === 2 ? 'pdf' : 'image',
    kind,
    step: SAMPLE_STEPS[i % SAMPLE_STEPS.length],
    heightFrac: heightForKind(kind),
    placementXRatio: DEFAULT_PLACEMENT_X_RATIO,
    placementYRatio: DEFAULT_PLACEMENT_Y_RATIO,
    placementScaleRatio: DEFAULT_PLACEMENT_SCALE_RATIO
  };
});
const freshInitialItems = () => INITIAL_ITEMS.map(item => ({
  ...item
}));
function smoothScrollTo(el, top, duration = 380) {
  if (!el) return;
  const start = el.scrollTop;
  const delta = top - start;
  if (Math.abs(delta) < 4) {
    el.scrollTop = top;
    return;
  }
  if (el.__scrollRaf) cancelAnimationFrame(el.__scrollRaf);
  const t0 = performance.now();
  const ease = u => 1 - Math.pow(1 - u, 3);
  const step = now => {
    const u = Math.min(1, (now - t0) / duration);
    el.scrollTop = start + delta * ease(u);
    if (u < 1) el.__scrollRaf = requestAnimationFrame(step);else el.__scrollRaf = null;
  };
  el.__scrollRaf = requestAnimationFrame(step);
}
const Icon = {
  upload: React.createElement("svg", {
    viewBox: "0 0 24 24",
    className: "ic",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.8",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, React.createElement("path", {
    d: "M12 16V4M7 9l5-5 5 5M5 20h14"
  })),
  trash: React.createElement("svg", {
    viewBox: "0 0 24 24",
    className: "ic",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.8",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, React.createElement("path", {
    d: "M4 7h16M10 11v6M14 11v6M5 7l1 13a2 2 0 002 2h8a2 2 0 002-2l1-13M9 7V4a1 1 0 011-1h4a1 1 0 011 1v3"
  })),
  crop: React.createElement("svg", {
    viewBox: "0 0 24 24",
    className: "ic",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.8",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, React.createElement("path", {
    d: "M7 2v15h15M2 7h15v15"
  })),
  rotate: React.createElement("svg", {
    viewBox: "0 0 24 24",
    className: "ic",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.8",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, React.createElement("path", {
    d: "M3 12a9 9 0 0114.5-7.2M21 4v5h-5"
  })),
  wand: React.createElement("svg", {
    viewBox: "0 0 24 24",
    className: "ic",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.8",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, React.createElement("path", {
    d: "M15 4l1.5 3L20 8.5 16.5 10 15 13l-1.5-3L10 8.5 13.5 7 15 4zM5 19l8-8M5 19l1.5-1.5"
  })),
  aiBatch: React.createElement("svg", {
    viewBox: "0 0 24 24",
    className: "ic",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.75",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, React.createElement("rect", {
    x: "3.5",
    y: "4",
    width: "7",
    height: "5.5",
    rx: "1.2"
  }), React.createElement("rect", {
    x: "3.5",
    y: "14.5",
    width: "7",
    height: "5.5",
    rx: "1.2"
  }), React.createElement("path", {
    d: "M13 12h5.7M16.4 8.8L19.6 12l-3.2 3.2"
  }), React.createElement("path", {
    d: "M15.2 4.2l.7 1.5 1.6.7-1.6.7-.7 1.5-.7-1.5-1.6-.7 1.6-.7.7-1.5z",
    fill: "currentColor",
    stroke: "none"
  }), React.createElement("text", {
    x: "14.2",
    y: "20.3",
    fill: "currentColor",
    stroke: "none",
    fontSize: "5.2",
    fontWeight: "800",
    fontFamily: "JetBrains Mono, monospace"
  }, "AI")),
  check: React.createElement("svg", {
    viewBox: "0 0 24 24",
    className: "ic",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "2",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, React.createElement("path", {
    d: "M5 13l4 4L19 7"
  })),
  board: React.createElement("svg", {
    viewBox: "0 0 24 24",
    className: "ic",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.8",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, React.createElement("rect", {
    x: "3",
    y: "4",
    width: "18",
    height: "13",
    rx: "1"
  }), React.createElement("path", {
    d: "M8 21h8M12 17v4"
  })),
  download: React.createElement("svg", {
    viewBox: "0 0 24 24",
    className: "ic",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.8",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, React.createElement("path", {
    d: "M12 4v11M7 10l5 5 5-5M5 20h14"
  })),
  folder: React.createElement("svg", {
    viewBox: "0 0 24 24",
    className: "ic",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.8",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, React.createElement("path", {
    d: "M3.5 6.5h6l2 2h9v9.5a2 2 0 0 1-2 2h-13a2 2 0 0 1-2-2z"
  }), React.createElement("path", {
    d: "M3.5 8.5V6a2 2 0 0 1 2-2h4l2 2h7a2 2 0 0 1 2 2v.5"
  })),
  zoomIn: React.createElement("svg", {
    viewBox: "0 0 24 24",
    className: "ic",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.8",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, React.createElement("circle", {
    cx: "11",
    cy: "11",
    r: "7"
  }), React.createElement("path", {
    d: "M21 21l-4.3-4.3M8 11h6M11 8v6"
  })),
  zoomOut: React.createElement("svg", {
    viewBox: "0 0 24 24",
    className: "ic",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.8",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, React.createElement("circle", {
    cx: "11",
    cy: "11",
    r: "7"
  }), React.createElement("path", {
    d: "M21 21l-4.3-4.3M8 11h6"
  })),
  undo: React.createElement("svg", {
    viewBox: "0 0 24 24",
    className: "ic",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.8",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, React.createElement("path", {
    d: "M9 14L4 9l5-5M4 9h11a5 5 0 010 10h-3"
  })),
  refresh: React.createElement("svg", {
    viewBox: "0 0 24 24",
    className: "ic",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.8",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, React.createElement("path", {
    d: "M21 12a9 9 0 1 1-3.5-7.1M21 4v5h-5"
  })),
  reset: React.createElement("svg", {
    viewBox: "0 0 24 24",
    className: "ic",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.8",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, React.createElement("path", {
    d: "M3 12a9 9 0 1 0 3-6.7M3 4v5h5"
  })),
  power: React.createElement("svg", {
    viewBox: "0 0 24 24",
    className: "ic",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.9",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, React.createElement("path", {
    d: "M12 3v9"
  }), React.createElement("path", {
    d: "M6.3 7.5a8 8 0 1 0 11.4 0"
  })),
  pen: React.createElement("svg", {
    viewBox: "0 0 24 24",
    className: "ic",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.8",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, React.createElement("path", {
    d: "M16 3l5 5L8 21H3v-5L16 3z"
  })),
  align: React.createElement("svg", {
    viewBox: "0 0 24 24",
    className: "ic",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.8",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, React.createElement("path", {
    d: "M4 6h10M4 12h16M4 18h7"
  })),
  arrowUp: React.createElement("svg", {
    viewBox: "0 0 24 24",
    className: "ic",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.9",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, React.createElement("path", {
    d: "M12 19V5M5 12l7-7 7 7"
  })),
  arrowDown: React.createElement("svg", {
    viewBox: "0 0 24 24",
    className: "ic",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.9",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, React.createElement("path", {
    d: "M12 5v14M19 12l-7 7-7-7"
  })),
  arrowLeft: React.createElement("svg", {
    viewBox: "0 0 24 24",
    className: "ic",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.9",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, React.createElement("path", {
    d: "M19 12H5M12 5l-7 7 7 7"
  })),
  arrowRight: React.createElement("svg", {
    viewBox: "0 0 24 24",
    className: "ic",
    fill: "none",
    stroke: "currentColor",
    strokeWidth: "1.9",
    strokeLinecap: "round",
    strokeLinejoin: "round"
  }, React.createElement("path", {
    d: "M5 12h14M12 5l7 7-7 7"
  }))
};
const PROCESSING_STEPS = new Set(['raw', 's1', 's2', 's3']);
function normalizeProcessingStep(step) {
  const normalized = String(step || 'raw').trim().toLowerCase();
  return PROCESSING_STEPS.has(normalized) ? normalized : 'raw';
}
const stepLabel = s => {
  const step = normalizeProcessingStep(s);
  if (step === 's1') return '1단계';
  if (step === 's2') return '2단계 · AI';
  if (step === 's3') return '3단계 · 재구성';
  return '대기';
};
function TopBar({
  fileName,
  setFileName,
  progress,
  processed,
  total,
  onPublish,
  published,
  onReset,
  onRefresh,
  refreshing,
  canReset,
  view,
  setView,
  reviewAvailable,
  onUndo,
  canUndo,
  onShutdown
}) {
  return React.createElement("div", {
    className: "topbar"
  }, React.createElement("div", {
    className: "brand"
  }, React.createElement("span", {
    className: "logo"
  }, "\u677F"), "\uCE60\uD310 \uC790\uB8CC \uD3B8\uC9D1\uAE30"), React.createElement("div", {
    className: "crumb"
  }, React.createElement("span", null, "\uC218\uC5C5 \u203A"), React.createElement("input", {
    value: fileName,
    onChange: e => setFileName(e.target.value)
  })), React.createElement("div", {
    className: "spacer"
  }), React.createElement("div", {
    className: "view-toggle",
    title: reviewAvailable ? '' : '먼저 자료를 업로드하세요'
  }, React.createElement("button", {
    className: view === 'board' ? 'on' : '',
    onClick: () => setView && setView('board')
  }, "\uCE60\uD310"), React.createElement("button", {
    className: view === 'review' ? 'on' : '',
    onClick: () => reviewAvailable && setView && setView('review'),
    disabled: !reviewAvailable,
    style: !reviewAvailable ? {
      cursor: 'not-allowed',
      opacity: .5
    } : null
  }, "\uAC80\uC218")), React.createElement("div", {
    className: "progress",
    title: `${processed} / ${total} 처리됨`
  }, React.createElement("div", {
    className: "bar"
  }, React.createElement("i", {
    style: {
      width: `${Math.round(progress * 100)}%`
    }
  })), React.createElement("span", null, processed, "/", total, " \uCC98\uB9AC\uB428")), React.createElement("button", {
    className: "btn ghost",
    onClick: onRefresh,
    disabled: refreshing,
    title: "\uC800\uC7A5\uB41C \uC138\uC158\uC744 \uB2E4\uC2DC \uBD88\uB7EC\uC635\uB2C8\uB2E4"
  }, React.createElement("span", {
    className: refreshing ? 'spin-ic' : '',
    style: {
      display: 'inline-flex'
    }
  }, Icon.refresh), React.createElement("span", {
    style: {
      marginLeft: 4
    }
  }, refreshing ? '불러오는 중…' : '새로고침')), React.createElement("button", {
    className: "btn ghost",
    onClick: onReset,
    disabled: !canReset,
    title: "\uC138\uC158, \uB354\uBBF8, \uC5C5\uB85C\uB4DC \uB300\uAE30\uC5F4, \uCD5C\uADFC \uC791\uC5C5\uC744 \uBE44\uC6C1\uB2C8\uB2E4"
  }, Icon.reset, React.createElement("span", {
    style: {
      marginLeft: 4
    }
  }, "\uCD08\uAE30\uD654")), React.createElement("button", {
    className: "btn ghost icon",
    title: canUndo ? '검수 변경 되돌리기 (Ctrl/Cmd+Z)' : '되돌릴 변경이 없습니다',
    onClick: onUndo,
    disabled: !canUndo
  }, Icon.undo), React.createElement("button", {
    className: "btn ghost icon",
    onClick: onShutdown,
    title: "\uB85C\uCEEC \uC571 \uC885\uB8CC"
  }, Icon.power), React.createElement("button", {
    className: `btn primary ${published ? 'done' : ''}`,
    onClick: onPublish
  }, published ? React.createElement(React.Fragment, null, Icon.check, " \uC81C\uC791 \uC644\uB8CC") : React.createElement(React.Fragment, null, Icon.board, " EDB \uC81C\uC791")));
}
function ReviewStage({
  session,
  items,
  activeId,
  setActive,
  mutateSession,
  retryAiSession,
  mutating,
  aiAvailable,
  aiBusy,
  onConfirm,
  reviewFocus
}) {
  const pages = Array.isArray(session?.pages) ? session.pages : [];
  const problemsById = useMemo(() => {
    const map = new Map();
    (session?.problems || []).forEach(p => {
      if (p && p.id) map.set(p.id, p);
    });
    return map;
  }, [session]);
  const orderMap = useMemo(() => {
    const map = new Map();
    items.forEach((it, idx) => map.set(it.id, idx + 1));
    return map;
  }, [items]);
  const [selectedIds, setSelectedIds] = useState(() => new Set());
  const [splitTarget, setSplitTarget] = useState(null);
  const [splitRatio, setSplitRatio] = useState(0.5);
  const [boxEdit, setBoxEdit] = useState(null);
  const [reviewFilter, setReviewFilter] = useState('all');
  const [reviewRiskFilter, setReviewRiskFilter] = useState(null);
  const splitDraggingRef = useRef(false);
  const splitBoxRef = useRef(null);
  const boxEditDragRef = useRef(null);
  useEffect(() => {
    setSplitTarget(null);
    setBoxEdit(null);
    setSelectedIds(new Set());
    setReviewRiskFilter(null);
  }, [session]);
  useEffect(() => {
    const nextFilter = String(reviewFocus?.filter || '').trim();
    const focusedProblemIds = Array.isArray(reviewFocus?.problemIds) ? reviewFocus.problemIds.map(id => String(id || '').trim()).filter(Boolean) : [];
    if (!nextFilter && !focusedProblemIds.length) return;
    setReviewFilter(nextFilter || 'all');
    setReviewRiskFilter(null);
    setSelectedIds(new Set(focusedProblemIds));
    if (focusedProblemIds.length && setActive) setActive(focusedProblemIds[0]);
  }, [reviewFocus, setActive]);
  const onBoxClick = (probId, evt) => {
    if (splitTarget) return;
    if (boxEdit) return;
    if (evt.shiftKey) {
      setSelectedIds(prev => {
        const next = new Set(prev);
        if (next.has(probId)) next.delete(probId);else next.add(probId);
        return next;
      });
    } else {
      setSelectedIds(new Set([probId]));
    }
    if (setActive) setActive(probId);
  };
  const selectedList = Array.from(selectedIds);
  const selectedProblems = selectedList.map(id => problemsById.get(id)).filter(Boolean);
  const selectedSingleProblem = selectedProblems.length === 1 ? selectedProblems[0] : null;
  const selectedSinglePage = selectedSingleProblem ? pages.find(page => page.id === selectedSingleProblem.sourcePageId) : null;
  const sameSourcePage = selectedProblems.length >= 2 && selectedProblems.every(p => p.sourcePageId === selectedProblems[0].sourcePageId);
  const statusCounts = useMemo(() => {
    const helperCounts = globalThis.EDB_REVIEW_FILTERS?.countReviewFilters?.(session?.problems || []);
    if (helperCounts) return helperCounts;
    const counts = {
      all: 0,
      normal: 0,
      check_needed: 0,
      failed: 0,
      passage: 0,
      passageGroups: 0
    };
    const passageGroups = new Set();
    (session?.problems || []).forEach(problem => {
      const status = deriveProblemStatus(problem);
      counts.all += 1;
      counts[status] = (counts[status] || 0) + 1;
      const passageGroupId = passageGroupIdFor(problem);
      if (passageGroupId) {
        counts.passage += 1;
        passageGroups.add(passageGroupId);
      }
    });
    counts.supplemental = (session?.problems || []).filter(isSupplementalProblem).length;
    counts.passageGroups = passageGroups.size;
    return counts;
  }, [session]);
  const sessionCounts = useMemo(() => sessionProblemCounts(session), [session]);
  const reviewSummary = useMemo(() => sessionReviewSummary(session), [session]);
  const passageReviewProblemIds = useMemo(() => new Set(reviewSummary.passageReviewProblemIds || []), [reviewSummary.passageReviewProblemIds]);
  const riskFilterHasProblemMatches = useMemo(() => {
    if (!reviewRiskFilter) return false;
    return (session?.problems || []).some(problem => hasRiskFlag(problem, reviewRiskFilter));
  }, [session, reviewRiskFilter]);
  const pageRetryIds = useMemo(() => {
    const ids = [];
    const byId = problemsById;
    pages.forEach(page => {
      const pageFlags = riskFlagsFor(page);
      const pageStatus = normalizeReviewStatus(page.reviewStatus || page.review_status);
      const hasProblemRisk = (page.problemIds || []).map(pid => byId.get(pid)).filter(Boolean).some(problem => deriveProblemStatus(problem) !== 'normal');
      if (pageStatus === 'failed' || Array.isArray(pageFlags) && pageFlags.length || !(page.problemIds || []).length || hasProblemRisk) {
        ids.push(page.id);
      }
    });
    return listUnique(ids.filter(Boolean));
  }, [pages, problemsById]);
  const activeReviewFilter = reviewFilter !== 'all' || Boolean(reviewRiskFilter);
  const visibleReviewScope = useMemo(() => {
    const retryPageIdSet = new Set();
    const problemIdSet = new Set();
    let problemCount = 0;
    pages.forEach(page => {
      const allPageProblems = (page.problemIds || []).map(pid => problemsById.get(pid)).filter(Boolean);
      const pageMatchesRiskFilter = reviewRiskFilter && !riskFilterHasProblemMatches ? hasRiskFlag(page, reviewRiskFilter) : false;
      const pageProblems = allPageProblems.filter(problem => problemMatchesReviewFilter(problem, reviewFilter, {
        passageReviewProblemIds
      })).filter(problem => !reviewRiskFilter || pageMatchesRiskFilter || hasRiskFlag(problem, reviewRiskFilter));
      problemCount += pageProblems.length;
      pageProblems.forEach(problem => {
        if (problem?.id) problemIdSet.add(problem.id);
      });
      if (pageProblems.length && pageRetryIds.includes(page.id)) {
        retryPageIdSet.add(page.id);
      }
    });
    return {
      problemCount,
      problemIds: Array.from(problemIdSet),
      retryPageIds: Array.from(retryPageIdSet)
    };
  }, [pages, problemsById, pageRetryIds, passageReviewProblemIds, reviewFilter, reviewRiskFilter, riskFilterHasProblemMatches]);
  const actionableProblemIds = useMemo(() => (session?.problems || []).filter(problem => problem?.id && deriveProblemStatus(problem) !== 'normal').map(problem => problem.id), [session]);
  const selectedRetryPageIds = listUnique(selectedProblems.map(problem => problem.sourcePageId).filter(Boolean));
  const selectedHasRetryable = selectedProblems.some(problem => deriveProblemStatus(problem) !== 'normal');
  const selectedCanBoxEdit = Boolean(selectedSingleProblem?.bbox?.width && selectedSingleProblem?.bbox?.height && selectedSinglePage?.width && selectedSinglePage?.height && selectedSinglePage?.sourceImageUri);
  const clampReviewBox = useCallback((rawBox, page) => {
    const pageWidth = Math.max(1, Number(page?.width) || 1);
    const pageHeight = Math.max(1, Number(page?.height) || 1);
    const minWidth = Math.min(pageWidth, Math.max(12, Math.min(36, pageWidth * 0.02)));
    const minHeight = Math.min(pageHeight, Math.max(12, Math.min(36, pageHeight * 0.02)));
    let left = Number(rawBox?.left);
    let top = Number(rawBox?.top);
    let width = Number(rawBox?.width);
    let height = Number(rawBox?.height);
    if (!Number.isFinite(left)) left = 0;
    if (!Number.isFinite(top)) top = 0;
    if (!Number.isFinite(width) || width <= 0) width = minWidth;
    if (!Number.isFinite(height) || height <= 0) height = minHeight;
    left = Math.max(0, Math.min(pageWidth - minWidth, left));
    top = Math.max(0, Math.min(pageHeight - minHeight, top));
    width = Math.max(minWidth, Math.min(pageWidth - left, width));
    height = Math.max(minHeight, Math.min(pageHeight - top, height));
    return {
      left,
      top,
      width,
      height
    };
  }, []);
  const beginBoxEdit = () => {
    if (!selectedCanBoxEdit || !selectedSingleProblem || !selectedSinglePage) return;
    setSplitTarget(null);
    setBoxEdit({
      problemId: selectedSingleProblem.id,
      pageId: selectedSinglePage.id,
      box: clampReviewBox(selectedSingleProblem.bbox, selectedSinglePage)
    });
  };
  const cancelBoxEdit = () => {
    boxEditDragRef.current = null;
    setBoxEdit(null);
  };
  const applyBoxEdit = async () => {
    if (!boxEdit?.problemId || !boxEdit?.box) return;
    await mutateSession?.('crop', {
      problemId: boxEdit.problemId,
      cropBox: boxEdit.box
    });
    setBoxEdit(null);
  };
  const retryPartialAi = async (problem = selectedSingleProblem, page = selectedSinglePage, cropBox = null) => {
    if (!problem?.id || !page?.id) return;
    const retryBox = expandBoxWithinPage(cropBox || problem.bbox, page);
    await retryAiSession?.({
      partial: true,
      problemIds: [problem.id],
      cropBoxes: {
        [problem.id]: retryBox
      },
      inputIntent: 'single-problem'
    });
  };
  const retryBoxEdit = async () => {
    if (!boxEdit?.problemId || !boxEdit?.box) return;
    const problem = problemsById.get(boxEdit.problemId);
    const page = pages.find(item => item.id === (problem?.sourcePageId || boxEdit.pageId));
    await retryPartialAi(problem, page, boxEdit.box);
    setBoxEdit(null);
  };
  const beginBoxDrag = (evt, mode, page) => {
    if (!boxEdit?.box || evt.button !== 0) return;
    evt.preventDefault();
    evt.stopPropagation();
    const canvas = evt.currentTarget.closest?.('.review-page-canvas');
    const rect = canvas?.getBoundingClientRect?.();
    if (!rect?.width || !rect?.height) return;
    boxEditDragRef.current = {
      mode,
      startX: evt.clientX,
      startY: evt.clientY,
      initialBox: {
        ...boxEdit.box
      },
      pageWidth: Number(page?.width) || 1,
      pageHeight: Number(page?.height) || 1,
      scaleX: (Number(page?.width) || 1) / rect.width,
      scaleY: (Number(page?.height) || 1) / rect.height
    };
  };
  const beginSplit = () => {
    if (selectedList.length !== 1) return;
    setBoxEdit(null);
    setSplitTarget(selectedList[0]);
    setSplitRatio(0.5);
  };
  const cancelSplit = () => setSplitTarget(null);
  const confirmSplit = async () => {
    if (!splitTarget) return;
    const ratio = Math.max(0.06, Math.min(0.94, splitRatio));
    await mutateSession?.('split', {
      problemId: splitTarget,
      splitYRatio: ratio
    });
  };
  const doMerge = async () => {
    if (!sameSourcePage) return;
    await mutateSession?.('merge', {
      problemIds: selectedList
    });
  };
  const doExclude = async () => {
    if (selectedList.length === 0) return;
    if (selectedList.length === 1) {
      await mutateSession?.('exclude', {
        problemId: selectedList[0]
      });
      return;
    }
    await mutateSession?.('exclude', {
      problemIds: selectedList
    });
  };
  const doRetryAi = async pageIds => {
    if (!pageIds?.length) return;
    await retryAiSession?.({
      pageIds
    });
  };
  const toggleRiskFilter = flag => {
    const nextFlag = String(flag || '').trim();
    if (!nextFlag) return;
    setReviewRiskFilter(prev => prev === nextFlag ? null : nextFlag);
    setReviewFilter('all');
    setSelectedIds(new Set());
  };
  const selectVisibleProblems = () => {
    if (!visibleReviewScope.problemIds.length) return;
    setSelectedIds(new Set(visibleReviewScope.problemIds));
    if (setActive) setActive(visibleReviewScope.problemIds[0]);
  };
  useEffect(() => {
    if (!splitTarget) return;
    const onMove = evt => {
      if (!splitDraggingRef.current || !splitBoxRef.current) return;
      const rect = splitBoxRef.current.getBoundingClientRect();
      const y = evt.clientY - rect.top;
      const ratio = Math.max(0.06, Math.min(0.94, y / rect.height));
      setSplitRatio(ratio);
    };
    const onUp = () => {
      splitDraggingRef.current = false;
    };
    window.addEventListener('mousemove', onMove);
    window.addEventListener('mouseup', onUp);
    return () => {
      window.removeEventListener('mousemove', onMove);
      window.removeEventListener('mouseup', onUp);
    };
  }, [splitTarget]);
  useEffect(() => {
    const onMove = evt => {
      const drag = boxEditDragRef.current;
      if (!drag) return;
      const dx = (evt.clientX - drag.startX) * drag.scaleX;
      const dy = (evt.clientY - drag.startY) * drag.scaleY;
      const minWidth = Math.min(drag.pageWidth, Math.max(12, Math.min(36, drag.pageWidth * 0.02)));
      const minHeight = Math.min(drag.pageHeight, Math.max(12, Math.min(36, drag.pageHeight * 0.02)));
      const initial = drag.initialBox;
      let left = initial.left;
      let top = initial.top;
      let right = initial.left + initial.width;
      let bottom = initial.top + initial.height;
      if (drag.mode === 'move') {
        const nextLeft = Math.max(0, Math.min(drag.pageWidth - initial.width, initial.left + dx));
        const nextTop = Math.max(0, Math.min(drag.pageHeight - initial.height, initial.top + dy));
        left = nextLeft;
        top = nextTop;
        right = nextLeft + initial.width;
        bottom = nextTop + initial.height;
      } else {
        if (drag.mode.includes('w')) left = Math.max(0, Math.min(right - minWidth, initial.left + dx));
        if (drag.mode.includes('e')) right = Math.min(drag.pageWidth, Math.max(left + minWidth, initial.left + initial.width + dx));
        if (drag.mode.includes('n')) top = Math.max(0, Math.min(bottom - minHeight, initial.top + dy));
        if (drag.mode.includes('s')) bottom = Math.min(drag.pageHeight, Math.max(top + minHeight, initial.top + initial.height + dy));
      }
      const nextBox = clampReviewBox({
        left,
        top,
        width: right - left,
        height: bottom - top
      }, {
        width: drag.pageWidth,
        height: drag.pageHeight
      });
      setBoxEdit(prev => prev ? {
        ...prev,
        box: nextBox
      } : prev);
    };
    const onUp = () => {
      boxEditDragRef.current = null;
    };
    window.addEventListener('mousemove', onMove);
    window.addEventListener('mouseup', onUp);
    return () => {
      window.removeEventListener('mousemove', onMove);
      window.removeEventListener('mouseup', onUp);
    };
  }, [clampReviewBox]);
  if (!pages.length) {
    return React.createElement("div", {
      className: "col center"
    }, React.createElement("div", {
      className: "stage"
    }, React.createElement("div", {
      className: "stage-toolbar"
    }, React.createElement("span", {
      className: "name"
    }, "\uAC80\uC218 \u2014 \uAC80\uCD9C\uB41C \uBB38\uC81C \uBC15\uC2A4")), React.createElement("div", {
      className: "review-empty"
    }, "\uBA3C\uC800 \uC790\uB8CC\uB97C \uC5C5\uB85C\uB4DC\uD574 \uC8FC\uC138\uC694.", React.createElement("br", null), "\uC5C5\uB85C\uB4DC\uD55C \uD398\uC774\uC9C0 \uC6D0\uBCF8 \uC704\uC5D0 \uAC80\uCD9C\uB41C \uBC15\uC2A4\uB97C \uBCF4\uC5EC\uB4DC\uB9BD\uB2C8\uB2E4.")));
  }
  const actionableStatusCount = Math.max(0, Number(reviewSummary.actionableNeedsReviewCount) || 0);
  const riskyCount = actionableStatusCount || statusCounts.failed;
  const filterOptions = [['all', '전체', statusCounts.all], ['normal', '정상', statusCounts.normal], ['check_needed', '확인 필요', actionableStatusCount], ['failed', '실패', statusCounts.failed]];
  if (sessionCounts.supplemental > 0) {
    filterOptions.push(['supplemental', '자료', sessionCounts.supplemental]);
  }
  if (statusCounts.passage > 0) {
    filterOptions.push(['passage', '긴 지문', statusCounts.passage]);
  }
  const retryDisabledReason = !aiAvailable ? 'Gemini API 키를 먼저 저장해 주세요' : aiBusy ? 'AI 인식 중입니다' : mutating ? '처리 중입니다' : '';
  const bulkRetryPageIds = activeReviewFilter ? visibleReviewScope.retryPageIds : pageRetryIds;
  const bulkRetryProblemCount = activeReviewFilter ? visibleReviewScope.problemCount : riskyCount;
  const showBulkRetry = reviewFilter !== 'normal' && reviewFilter !== 'supplemental' && bulkRetryProblemCount > 0 && bulkRetryPageIds.length > 0;
  const actionBar = boxEdit ? React.createElement("div", {
    className: "review-actionbar"
  }, React.createElement("span", {
    className: "count-chip"
  }, "\uD2C0 \uC870\uC815 \uC911"), React.createElement("span", {
    className: "hint"
  }, "\uBAA8\uC11C\uB9AC\uC640 \uBCC0\uC744 \uB04C\uC5B4 \uBB38\uC81C \uC804\uCCB4\uAC00 \uB4E4\uC5B4\uC624\uAC8C \uB9DE\uCD98 \uB4A4 \uC790\uB974\uAC70\uB098, \uC8FC\uBCC0 \uC5EC\uBC31\uAE4C\uC9C0 \uD3EC\uD568\uD574 \uB2E4\uC2DC \uC778\uC2DD\uD558\uC138\uC694."), React.createElement("div", {
    className: "spacer"
  }), React.createElement("button", {
    className: "btn",
    type: "button",
    onClick: cancelBoxEdit,
    disabled: mutating
  }, "\uCDE8\uC18C"), React.createElement("button", {
    className: "btn",
    type: "button",
    title: retryDisabledReason || '조정한 영역 주변까지 AI로 다시 인식',
    onClick: retryBoxEdit,
    disabled: !aiAvailable || aiBusy || mutating
  }, "\uC8FC\uBCC0 \uC601\uC5ED AI \uC7AC\uC778\uC2DD"), React.createElement("button", {
    className: "btn primary",
    type: "button",
    onClick: applyBoxEdit,
    disabled: mutating
  }, "\uC790\uB974\uAE30 \uC801\uC6A9")) : splitTarget ? React.createElement("div", {
    className: "review-actionbar"
  }, React.createElement("span", {
    className: "count-chip"
  }, "\uAC00\uB974\uAE30 \uC911"), React.createElement("span", {
    className: "hint"
  }, "\uBC15\uC2A4 \uC548\uC758 \uD30C\uB780 \uC120\uC744 \uB4DC\uB798\uADF8\uD574\uC11C \uC704\uCE58\uB97C \uC815\uD55C \uB2E4\uC74C [\uAC00\uB974\uAE30]\uB97C \uB20C\uB7EC\uC8FC\uC138\uC694."), React.createElement("div", {
    className: "spacer"
  }), React.createElement("button", {
    className: "btn",
    onClick: cancelSplit,
    disabled: mutating
  }, "\uCDE8\uC18C"), React.createElement("button", {
    className: "btn primary",
    onClick: confirmSplit,
    disabled: mutating
  }, "\u2702 ", (splitRatio * 100).toFixed(0), "% \uC704\uCE58\uC5D0\uC11C \uAC00\uB974\uAE30")) : selectedList.length === 0 ? React.createElement("div", {
    className: "review-actionbar"
  }, React.createElement("div", {
    className: "review-filters",
    "aria-label": "\uAC80\uC218 \uC0C1\uD0DC \uD544\uD130"
  }, filterOptions.map(([value, label, count]) => React.createElement("button", {
    key: value,
    className: reviewFilter === value ? 'on' : '',
    type: "button",
    onClick: () => setReviewFilter(value)
  }, label, " ", React.createElement("span", null, count)))), reviewRiskFilter && React.createElement("span", {
    className: "review-risk-filter-active"
  }, "\uC6D0\uC778 \uD544\uD130 \xB7 ", riskFlagLabel(reviewRiskFilter), React.createElement("button", {
    type: "button",
    onClick: () => setReviewRiskFilter(null)
  }, "\uD574\uC81C")), React.createElement("span", {
    className: "hint"
  }, "\uBB38\uC81C \uBC15\uC2A4\uB97C \uD655\uC778\uD558\uACE0, \uC774\uC0C1\uD55C \uD398\uC774\uC9C0\uB9CC AI\uB85C \uB2E4\uC2DC \uC778\uC2DD\uD558\uC138\uC694."), React.createElement("div", {
    className: "spacer"
  }), activeReviewFilter && visibleReviewScope.problemIds.length > 0 && React.createElement(React.Fragment, null, React.createElement("button", {
    className: "btn",
    type: "button",
    onClick: selectVisibleProblems,
    disabled: mutating
  }, "\uD45C\uC2DC \uD56D\uBAA9 \uC120\uD0DD ", visibleReviewScope.problemIds.length), React.createElement("button", {
    className: "btn",
    type: "button",
    onClick: () => onConfirm?.(null, {
      problemIds: visibleReviewScope.problemIds,
      bulk: true
    }),
    disabled: mutating
  }, "\uD45C\uC2DC \uD56D\uBAA9 \uD655\uC778 \uC644\uB8CC ", visibleReviewScope.problemIds.length)), !activeReviewFilter && actionableProblemIds.length > 0 && React.createElement("button", {
    className: "btn",
    type: "button",
    onClick: () => onConfirm?.(null, {
      problemIds: actionableProblemIds,
      bulk: true
    }),
    disabled: mutating
  }, "\uD655\uC778 \uD544\uC694 \uC804\uCCB4 \uD655\uC778 ", actionableProblemIds.length), showBulkRetry && React.createElement("button", {
    className: "btn primary",
    type: "button",
    title: retryDisabledReason || `${bulkRetryPageIds.length}개 페이지 재인식`,
    onClick: () => doRetryAi(bulkRetryPageIds),
    disabled: !aiAvailable || aiBusy || mutating || !bulkRetryPageIds.length
  }, "\uD398\uC774\uC9C0 \uC804\uCCB4 AI \uC7AC\uC778\uC2DD ", bulkRetryProblemCount)) : React.createElement("div", {
    className: "review-actionbar"
  }, React.createElement("span", {
    className: "count-chip"
  }, selectedList.length, "\uAC1C \uC120\uD0DD\uB428"), React.createElement("span", {
    className: "hint"
  }, selectedList.length === 1 ? '선택 박스 주변만 다시 인식하거나, 틀을 조정해 자르고, 필요하면 두 문제로 나눌 수 있어요.' : sameSourcePage ? '같은 페이지의 박스들을 하나로 합치거나, 모두 제외할 수 있어요.' : '같은 페이지의 박스만 합칠 수 있어요. (현재 선택은 페이지가 다름)'), React.createElement("div", {
    className: "spacer"
  }), React.createElement("button", {
    className: "btn",
    type: "button",
    onClick: () => onConfirm?.(null, {
      problemIds: selectedList,
      bulk: true
    }),
    disabled: mutating
  }, "\uD655\uC778 \uC644\uB8CC ", selectedList.length), selectedList.length >= 2 && React.createElement("button", {
    className: "btn",
    type: "button",
    title: retryDisabledReason || `${selectedRetryPageIds.length}개 페이지 전체 재인식`,
    onClick: () => doRetryAi(selectedRetryPageIds),
    disabled: !aiAvailable || aiBusy || mutating || !selectedHasRetryable || !selectedRetryPageIds.length
  }, "\uD398\uC774\uC9C0 \uC804\uCCB4 AI \uC7AC\uC778\uC2DD ", selectedList.length), selectedList.length === 1 && React.createElement(React.Fragment, null, React.createElement("button", {
    className: "btn primary",
    type: "button",
    onClick: () => retryPartialAi(),
    disabled: !aiAvailable || aiBusy || mutating || !selectedCanBoxEdit,
    title: retryDisabledReason || '선택한 박스 주변 여백까지 AI로 다시 인식'
  }, "\uC8FC\uBCC0 \uC601\uC5ED AI \uC7AC\uC778\uC2DD"), React.createElement("button", {
    className: "btn",
    type: "button",
    onClick: beginBoxEdit,
    disabled: mutating || !selectedCanBoxEdit,
    title: selectedCanBoxEdit ? '원본 페이지 위에서 자르기 틀을 직접 조정' : '원본 페이지 이미지가 있어야 합니다'
  }, "\uD2C0 \uC870\uC815/\uC790\uB974\uAE30"), React.createElement("button", {
    className: "btn primary",
    onClick: beginSplit,
    disabled: mutating
  }, "\u2702 \uB450 \uBB38\uC81C\uB85C \uB098\uB204\uAE30")), selectedList.length >= 2 && React.createElement("button", {
    className: "btn primary",
    onClick: doMerge,
    disabled: !sameSourcePage || mutating
  }, "\u21F2 \uD569\uCE58\uAE30"), React.createElement("button", {
    className: "btn danger",
    onClick: doExclude,
    disabled: mutating
  }, Icon.trash, " \uC81C\uC678 ", selectedList.length), React.createElement("button", {
    className: "btn",
    onClick: () => setSelectedIds(new Set()),
    disabled: mutating
  }, "\uC120\uD0DD \uD574\uC81C"));
  return React.createElement("div", {
    className: "col center"
  }, React.createElement("div", {
    className: "stage"
  }, React.createElement("div", {
    className: "stage-toolbar"
  }, React.createElement("span", {
    className: "name"
  }, "\uAC80\uC218 \u2014 \uAC80\uCD9C\uB41C \uBB38\uC81C \uBC15\uC2A4"), React.createElement("span", {
    className: "pill"
  }, React.createElement("span", {
    className: "dotc"
  }), " ", pages.length, " \uD398\uC774\uC9C0 \xB7 ", formatProblemCount(sessionCounts)), React.createElement("div", {
    className: "spacer"
  }), React.createElement("span", {
    style: {
      fontSize: 11.5,
      color: 'var(--muted)'
    }
  }, "\uBE68\uAC04 \uBC15\uC2A4\uB294 \uC778\uC2DD\uC774 \uC758\uC2EC\uB429\uB2C8\uB2E4. \uD074\uB9AD \uD6C4 \uAC00\uB974\uAE30\xB7\uD569\uCE58\uAE30\xB7\uC81C\uC678\uD558\uC138\uC694.")), React.createElement("div", {
    className: "review-wrap"
  }, actionBar, React.createElement("div", {
    className: "review-summary-strip"
  }, React.createElement("span", {
    className: "review-summary-title"
  }, "\uAC80\uC218 \uC694\uC57D"), React.createElement("span", {
    className: "review-summary-chip"
  }, formatProblemCount(reviewSummary.counts)), React.createElement("span", {
    className: `review-summary-chip ${reviewSummary.warningCount ? 'warn' : 'ok'}`
  }, "\uC8FC\uC758 ", reviewSummary.warningCount), reviewSummary.aiStages.map(stage => React.createElement("span", {
    key: stage.stage,
    className: `review-summary-chip ${stage.usedPageCount || stage.attemptedBlockCount || stage.appliedPageCount ? 'ok' : ''}`,
    title: aiStageTooltip(stage)
  }, aiStageChipText(stage))), reviewSummary.actionableNeedsReviewCount > 0 && React.createElement("span", {
    className: "review-summary-chip warn"
  }, "\uD655\uC778 ", reviewSummary.actionableNeedsReviewCount, reviewSummary.reviewStatusCounts.failed ? ` · 실패 ${reviewSummary.reviewStatusCounts.failed}` : ''), reviewSummary.hwpOversegmentationCount > 0 && React.createElement("span", {
    className: "review-summary-chip warn",
    title: "HWP \uB0B4\uBD80 \uBB38\uD56D \uC218\uBCF4\uB2E4 \uCD5C\uC885 \uBD84\uD560 \uC218\uAC00 \uD06C\uAC8C \uB9CE\uC2B5\uB2C8\uB2E4."
  }, "HWP \uACFC\uBD84\uD560 ", reviewSummary.hwpOversegmentationCount), reviewSummary.hwpProblemCountMismatchCount > 0 && reviewSummary.hwpOversegmentationCount <= 0 && React.createElement("span", {
    className: "review-summary-chip warn",
    title: "HWP \uB0B4\uBD80 \uBB38\uD56D \uC218\uC640 \uCD5C\uC885 \uBD84\uD560 \uC218\uAC00 \uB2E4\uB985\uB2C8\uB2E4."
  }, "HWP \uBB38\uD56D \uCC28\uC774 ", reviewSummary.hwpProblemCountMismatchCount), reviewSummary.duplicateProblemNumberGroups.length > 0 && React.createElement("span", {
    className: "review-summary-chip",
    title: "\uAC19\uC740 \uBB38\uD56D \uBC88\uD638\uAC00 \uC5EC\uB7EC \uAD6C\uAC04\uC5D0 \uBCF4\uC874\uB418\uC5B4 \uC788\uC2B5\uB2C8\uB2E4."
  }, "\uC911\uBCF5 \uBC88\uD638 ", reviewSummary.duplicateProblemNumberLabel), reviewSummary.sourceProblemOverlapGroups.length > 0 && React.createElement("span", {
    className: "review-summary-chip warn",
    title: "\uAC19\uC740 \uC6D0\uBCF8 \uD398\uC774\uC9C0\uC5D0\uC11C \uBB38\uD56D \uC778\uC2DD \uC601\uC5ED\uC774 \uD06C\uAC8C \uACB9\uCE69\uB2C8\uB2E4."
  }, "\uC6D0\uBCF8 \uACB9\uCE68 ", reviewSummary.sourceProblemOverlapLabel), reviewSummary.passageGroupSourceReuseGroups.length > 0 && React.createElement("button", {
    type: "button",
    className: `review-summary-chip warn risk-filter-chip ${reviewRiskFilter === 'passage_group_source_reuse' ? 'on' : ''}`,
    title: "\uAC19\uC740 \uAE34 \uC9C0\uBB38 \uADF8\uB8F9\uC758 \uD558\uC704 \uBB38\uD56D \uC6D0\uBCF8 \uC601\uC5ED\uC774 \uD06C\uAC8C \uACB9\uCE69\uB2C8\uB2E4.",
    "aria-pressed": reviewRiskFilter === 'passage_group_source_reuse',
    "data-risk-flag": "passage_group_source_reuse",
    onClick: () => toggleRiskFilter('passage_group_source_reuse')
  }, "\uC9C0\uBB38 \uC6D0\uBCF8 \uC911\uBCF5 ", reviewSummary.passageGroupSourceReuseLabel), reviewSummary.passageGroupCount > 0 && React.createElement("button", {
    type: "button",
    className: `review-summary-chip risk-filter-chip ${reviewFilter === 'passage' ? 'on' : ''}`,
    title: `${reviewSummary.passageProblemCount}개 문항이 긴 지문 그룹에 연결되어 있습니다.${reviewSummary.passageContinuationBlockCount > 0 ? ` 이어짐 블록 ${reviewSummary.passageContinuationBlockCount}개 포함.` : ''}`,
    "aria-pressed": reviewFilter === 'passage',
    onClick: () => setReviewFilter(prev => prev === 'passage' ? 'all' : 'passage')
  }, "\uAE34 \uC9C0\uBB38 \uADF8\uB8F9 ", reviewSummary.passageGroupCount, reviewSummary.passageContinuationBlockCount > 0 ? ` · 이어짐 ${reviewSummary.passageContinuationBlockCount}` : ''), reviewSummary.passageReviewItemCount > 0 && React.createElement("button", {
    type: "button",
    className: `review-summary-chip warn risk-filter-chip ${reviewFilter === 'passage-review' ? 'on' : ''}`,
    title: ['긴 지문 검수 큐', reviewSummary.passageReviewLabel, reviewSummary.passageReviewReasonLabel, reviewSummary.passageReviewPreview ? `대상 ${reviewSummary.passageReviewPreview}` : ''].filter(Boolean).join(' · '),
    "aria-pressed": reviewFilter === 'passage-review',
    onClick: () => setReviewFilter(prev => prev === 'passage-review' ? 'all' : 'passage-review')
  }, reviewSummary.passageReviewLabel, reviewSummary.passageReviewReasonLabel ? ` · ${reviewSummary.passageReviewReasonLabel}` : ''), reviewSummary.topRiskFlags.map(item => React.createElement("button", {
    key: item.flag,
    type: "button",
    className: `review-summary-chip risk-filter-chip ${reviewRiskFilter === item.flag ? 'on' : ''}`,
    title: `${riskFlagLabel(item.flag)} 항목만 보기`,
    "aria-pressed": reviewRiskFilter === item.flag,
    "data-risk-flag": item.flag,
    onClick: () => toggleRiskFilter(item.flag)
  }, riskFlagLabel(item.flag), " ", item.count)), reviewSummary.hwpTextProblemSignalCount > 0 && React.createElement("span", {
    className: "review-summary-chip"
  }, "HWP \uD14D\uC2A4\uD2B8 ", reviewSummary.hwpTextProblemSignalCount, reviewSummary.hwpTextExtractorLabel ? ` · ${reviewSummary.hwpTextExtractorLabel}` : ''), reviewSummary.hwpTextProblemSignalCount > 0 && reviewSummary.hwpTextProblemCountStatus !== 'unknown' && React.createElement("span", {
    className: `review-summary-chip ${reviewSummary.hwpTextProblemCountStatus === 'match' ? 'ok' : 'warn'}`,
    title: reviewSummary.hwpTextProblemCountMessage
  }, reviewSummary.hwpTextProblemCountStatus === 'match' ? '문항 수 일치' : `문항 수 차이 ${reviewSummary.hwpTextProblemDelta > 0 ? '+' : ''}${reviewSummary.hwpTextProblemDelta}`), reviewSummary.hwpLayoutProblemSignalCount > 0 && React.createElement("span", {
    className: "review-summary-chip"
  }, "HWP \uB808\uC774\uC544\uC6C3 ", reviewSummary.hwpLayoutProblemSignalCount, reviewSummary.hwpLayoutExtractorLabel ? ` · ${reviewSummary.hwpLayoutExtractorLabel}` : ''), reviewSummary.hwpLayoutProblemSignalCount > 0 && reviewSummary.hwpLayoutProblemCountStatus !== 'unknown' && React.createElement("span", {
    className: `review-summary-chip ${reviewSummary.hwpLayoutProblemCountStatus === 'match' ? 'ok' : 'warn'}`,
    title: reviewSummary.hwpLayoutProblemCountMessage
  }, reviewSummary.hwpLayoutProblemCountStatus === 'match' ? '레이아웃 일치' : `레이아웃 차이 ${reviewSummary.hwpLayoutProblemDelta > 0 ? '+' : ''}${reviewSummary.hwpLayoutProblemDelta}`), reviewSummary.hwpCacheHitPageCount > 0 && React.createElement("span", {
    className: "review-summary-chip ok",
    title: `렌더 캐시 ${reviewSummary.hwpRendererCacheHitCount} · 정규화 캐시 ${reviewSummary.hwpNormalizedCacheHitCount}`
  }, "HWP \uCE90\uC2DC ", reviewSummary.hwpCacheHitPageCount), reviewSummary.warningPreview && React.createElement("span", {
    className: "review-summary-note"
  }, reviewSummary.warningPreview)), pages.map(page => {
    const allPageProblems = (page.problemIds || []).map(pid => problemsById.get(pid)).filter(Boolean);
    const pageMatchesRiskFilter = reviewRiskFilter && !riskFilterHasProblemMatches ? hasRiskFlag(page, reviewRiskFilter) : false;
    const pageProblems = allPageProblems.filter(problem => problemMatchesReviewFilter(problem, reviewFilter, {
      passageReviewProblemIds
    })).filter(problem => !reviewRiskFilter || pageMatchesRiskFilter || hasRiskFlag(problem, reviewRiskFilter));
    if ((reviewFilter !== 'all' || reviewRiskFilter) && pageProblems.length === 0) return null;
    const pageCounts = countSessionProblems(allPageProblems);
    const pageRiskFlags = riskFlagsFor(page);
    const pageStatus = normalizeReviewStatus(page.reviewStatus || page.review_status) || (!(page.problemIds || []).length ? 'failed' : pageRiskFlags.length ? 'check_needed' : 'normal');
    const hasRisk = pageRiskFlags.length > 0 || pageStatus !== 'normal';
    const pageCanRetry = pageRetryIds.includes(page.id);
    return React.createElement("div", {
      key: page.id,
      className: `review-page ${reviewStatusClass(pageStatus)}`
    }, React.createElement("div", {
      className: "review-page-hd"
    }, React.createElement("span", {
      className: "pg-num"
    }, page.id), React.createElement("span", {
      className: "pg-count"
    }, reviewFilter === 'all' && !reviewRiskFilter ? formatProblemCount(pageCounts) : `${pageProblems.length}/${allPageProblems.length} 표시`), React.createElement("span", {
      className: `status-badge ${reviewStatusClass(pageStatus)}`
    }, reviewStatusMeta(pageStatus).label), hasRisk && React.createElement("span", {
      className: "pg-risk",
      title: pageRiskFlags.join(', ')
    }, pageRiskFlags.length ? `위험 · ${pageRiskFlags.join(' · ')}` : '문제 인식 실패'), React.createElement("div", {
      className: "spacer"
    }), pageCanRetry && React.createElement("button", {
      className: "mini-action",
      type: "button",
      title: retryDisabledReason || '이 페이지만 AI로 다시 인식',
      onClick: () => doRetryAi([page.id]),
      disabled: !aiAvailable || aiBusy || mutating
    }, "AI \uC7AC\uC778\uC2DD"), React.createElement("span", {
      style: {
        fontSize: 11,
        color: 'var(--muted-2)',
        fontFamily: "'JetBrains Mono', monospace"
      }
    }, page.width, "\xD7", page.height)), React.createElement("div", {
      className: "review-page-canvas"
    }, page.sourceImageUri ? React.createElement("img", {
      src: page.sourceImageUri,
      alt: page.id,
      draggable: false
    }) : React.createElement("div", {
      style: {
        padding: 40,
        textAlign: 'center',
        color: 'var(--muted)'
      }
    }, "\uD398\uC774\uC9C0 \uC774\uBBF8\uC9C0\uB97C \uBD88\uB7EC\uC62C \uC218 \uC5C6\uC5B4\uC694."), pageProblems.map(prob => {
      const isEditing = boxEdit?.problemId === prob.id;
      const bbox = isEditing ? boxEdit.box : prob.bbox || {};
      const w = page.width || 1;
      const h = page.height || 1;
      if (!bbox.width || !bbox.height) return null;
      const leftPct = bbox.left / w * 100;
      const topPct = bbox.top / h * 100;
      const widthPct = bbox.width / w * 100;
      const heightPct = bbox.height / h * 100;
      const isSelected = selectedIds.has(prob.id);
      const isActive = prob.id === activeId;
      const status = deriveProblemStatus(prob);
      const statusMeta = reviewStatusMeta(status);
      const isRisky = status !== 'normal';
      const isSplitting = splitTarget === prob.id;
      const passageGroupId = passageGroupIdFor(prob);
      const isPassage = Boolean(passageGroupId);
      const order = orderMap.get(prob.id);
      const tooltipParts = [prob.title || ''];
      const problemRiskFlags = riskFlagsFor(prob);
      if (isRisky) tooltipParts.push(`${statusMeta.label}: ${problemRiskFlags.join(', ') || '경계 확인 필요'}`);
      if (isPassage) tooltipParts.push(`긴 지문 ${passageGroupId}`);
      const classes = ['review-bbox', isPassage ? 'review-bbox-passage' : '', isSelected ? 'selected' : '', isActive ? 'active' : '', isRisky ? 'risky' : '', reviewStatusClass(status), isSplitting ? 'splitting' : '', isEditing ? 'editing' : ''].filter(Boolean).join(' ');
      return React.createElement("div", {
        key: prob.id,
        ref: isSplitting ? splitBoxRef : null,
        className: classes,
        style: {
          left: `${leftPct}%`,
          top: `${topPct}%`,
          width: `${widthPct}%`,
          height: `${heightPct}%`
        },
        onMouseDown: isEditing ? evt => beginBoxDrag(evt, 'move', page) : undefined,
        onClick: evt => {
          if (isEditing) {
            evt.stopPropagation();
            return;
          }
          onBoxClick(prob.id, evt);
        },
        title: tooltipParts.filter(Boolean).join(' · ')
      }, React.createElement("div", {
        className: "review-bbox-label"
      }, String(order || '?').padStart(2, '0'), isPassage && React.createElement("span", {
        className: "review-bbox-passage-tag"
      }, "\uC9C0\uBB38"), isRisky && React.createElement("span", {
        className: "review-bbox-risk"
      }, statusMeta.shortLabel)), isEditing && React.createElement(React.Fragment, null, React.createElement("div", {
        className: "crop-frame-label"
      }, "\uD2C0 \uC870\uC815"), ['nw', 'n', 'ne', 'e', 'se', 's', 'sw', 'w'].map(mode => React.createElement("button", {
        key: mode,
        type: "button",
        className: `crop-frame-handle ${mode}`,
        "aria-label": `자르기 틀 ${mode}`,
        onMouseDown: evt => beginBoxDrag(evt, mode, page),
        onClick: evt => evt.stopPropagation()
      }))), isSplitting && React.createElement("div", {
        className: "split-guide",
        style: {
          top: `${splitRatio * 100}%`
        },
        onMouseDown: evt => {
          evt.stopPropagation();
          splitDraggingRef.current = true;
        }
      }));
    })));
  }))));
}
function ItemsRail({
  items,
  activeId,
  setActive,
  reorder,
  removeItem,
  addSample,
  bulkApply,
  handleFiles,
  pendingFiles,
  removePendingFile,
  clearPendingFiles,
  processQueuedFiles,
  queueBusy,
  aiAvailable,
  addMockSample,
  canAddDummy,
  recentSessions,
  restoringSessionId,
  onRestoreRecentSession
}) {
  const dragId = useRef(null);
  const [draggingId, setDraggingId] = useState(null);
  const [dropTarget, setDropTarget] = useState(null);
  const [dropZoneActive, setDropZoneActive] = useState(false);
  const railRef = useRef(null);
  const itemRefs = useRef({});
  const previousItemRects = useRef(new Map());
  const pointerDragRef = useRef(null);
  const suppressClickRef = useRef(false);
  const dropTargetRef = useRef(null);
  const [recentSessionsCollapsed, setRecentSessionsCollapsed] = useState(() => {
    try {
      return window.localStorage?.getItem(RECENT_SESSIONS_COLLAPSED_KEY) === '1';
    } catch (_err) {
      return false;
    }
  });
  useLayoutEffect(() => {
    const reduceMotion = window.matchMedia?.('(prefers-reduced-motion: reduce)')?.matches;
    const nextRects = new Map();
    items.forEach(it => {
      const el = itemRefs.current[it.id];
      if (!el) return;
      const rect = el.getBoundingClientRect();
      const prevRect = previousItemRects.current.get(it.id);
      if (prevRect && !reduceMotion) {
        const dx = prevRect.left - rect.left;
        const dy = prevRect.top - rect.top;
        if (Math.abs(dx) > 1 || Math.abs(dy) > 1) {
          el.animate([{
            transform: `translate(${dx}px, ${dy}px)`
          }, {
            transform: 'translate(0, 0)'
          }], {
            duration: 220,
            easing: 'cubic-bezier(.2,.8,.2,1)'
          });
        }
      }
      nextRects.set(it.id, {
        top: rect.top,
        left: rect.left,
        width: rect.width,
        height: rect.height
      });
    });
    previousItemRects.current = nextRects;
  }, [items]);
  const clearDragState = () => {
    dragId.current = null;
    setDraggingId(null);
    dropTargetRef.current = null;
    setDropTarget(null);
  };
  const setCurrentDropTarget = target => {
    dropTargetRef.current = target;
    setDropTarget(prev => prev?.id === target?.id && prev?.position === target?.position ? prev : target);
  };
  const updateDropTarget = (event, targetId) => {
    if (!dragId.current || dragId.current === targetId) {
      setCurrentDropTarget(null);
      return;
    }
    event.preventDefault();
    event.dataTransfer.dropEffect = 'move';
    const position = dropPositionFromClientY(event.currentTarget.getBoundingClientRect(), event.clientY);
    setCurrentDropTarget({
      id: targetId,
      position
    });
  };
  const findPointerDropTarget = (clientX, clientY, sourceId) => {
    const rail = railRef.current;
    if (!rail) return null;
    const hit = document.elementFromPoint(clientX, clientY);
    const row = hit?.closest?.('.item[data-item-id]');
    if (row && rail.contains(row)) {
      const id = row.getAttribute('data-item-id');
      if (!id || id === sourceId) return null;
      return {
        id,
        position: dropPositionFromClientY(row.getBoundingClientRect(), clientY)
      };
    }
    const rows = Array.from(rail.querySelectorAll('.item[data-item-id]'));
    if (!rows.length) return null;
    const first = rows[0].getBoundingClientRect();
    if (clientY < first.top) {
      const id = rows[0].getAttribute('data-item-id');
      return id && id !== sourceId ? {
        id,
        position: 'before'
      } : null;
    }
    for (const candidate of rows) {
      const rect = candidate.getBoundingClientRect();
      const id = candidate.getAttribute('data-item-id');
      if (clientY < rect.top + rect.height / 2) {
        return id && id !== sourceId ? {
          id,
          position: 'before'
        } : null;
      }
    }
    const last = rows[rows.length - 1];
    const id = last.getAttribute('data-item-id');
    return id && id !== sourceId ? {
      id,
      position: 'after'
    } : null;
  };
  const startPointerDrag = (event, itemId) => {
    if (event.button !== 0 || event.target.closest?.('button')) return;
    pointerDragRef.current = {
      id: itemId,
      pointerId: event.pointerId,
      startX: event.clientX,
      startY: event.clientY,
      moved: false
    };
    dragId.current = itemId;
    event.currentTarget.setPointerCapture?.(event.pointerId);
  };
  const movePointerDrag = event => {
    const drag = pointerDragRef.current;
    if (!drag || drag.pointerId !== event.pointerId) return;
    const dx = event.clientX - drag.startX;
    const dy = event.clientY - drag.startY;
    if (!drag.moved && Math.hypot(dx, dy) < 5) return;
    drag.moved = true;
    event.preventDefault();
    setDraggingId(drag.id);
    const target = findPointerDropTarget(event.clientX, event.clientY, drag.id);
    setCurrentDropTarget(target);
  };
  const finishPointerDrag = event => {
    const drag = pointerDragRef.current;
    if (!drag || drag.pointerId !== event.pointerId) return;
    const target = drag.moved ? findPointerDropTarget(event.clientX, event.clientY, drag.id) || dropTargetRef.current : null;
    if (drag.moved) {
      event.preventDefault();
      suppressClickRef.current = true;
      window.setTimeout(() => {
        suppressClickRef.current = false;
      }, 0);
      if (target) reorder(drag.id, target.id, target.position, {
        resetPlacement: true
      });
    }
    event.currentTarget.releasePointerCapture?.(event.pointerId);
    pointerDragRef.current = null;
    clearDragState();
  };
  const toggleRecentSessionsCollapsed = () => {
    setRecentSessionsCollapsed(prev => {
      const next = !prev;
      try {
        window.localStorage?.setItem(RECENT_SESSIONS_COLLAPSED_KEY, next ? '1' : '0');
      } catch (_err) {}
      return next;
    });
  };
  useEffect(() => {
    const el = itemRefs.current[activeId];
    const rail = railRef.current;
    if (!el || !rail) return;
    const top = el.offsetTop;
    const bot = top + el.offsetHeight;
    const vTop = rail.scrollTop;
    const vBot = vTop + rail.clientHeight;
    if (top < vTop + 16) {
      smoothScrollTo(rail, top - 16);
    } else if (bot > vBot - 16) {
      smoothScrollTo(rail, bot - rail.clientHeight + 24);
    }
  }, [activeId]);
  return React.createElement("div", {
    className: "col left"
  }, React.createElement("div", {
    className: "col-hd"
  }, React.createElement("h2", null, "\uC790\uB8CC"), React.createElement("span", {
    className: "count"
  }, items.length), React.createElement("div", {
    className: "spacer"
  }), React.createElement("button", {
    className: "icon-btn",
    title: "\uC804\uCCB4\uB97C 2\uB2E8\uACC4 AI \uBCC0\uD658",
    onClick: () => bulkApply('s2'),
    disabled: !items.length
  }, Icon.aiBatch), React.createElement("button", {
    className: "icon-btn",
    title: canAddDummy ? '더미 추가' : '실제 세션 또는 대기열이 있을 때는 더미를 추가하지 않습니다',
    onClick: addMockSample,
    disabled: !canAddDummy
  }, Icon.wand), React.createElement("button", {
    className: "icon-btn",
    title: "\uD30C\uC77C \uCD94\uAC00",
    onClick: addSample
  }, Icon.upload)), React.createElement("div", {
    className: "items",
    ref: railRef
  }, React.createElement("div", {
    className: `drop-zone ${dropZoneActive ? 'is-active' : ''}`,
    onClick: addSample,
    onDragEnter: e => {
      if (!e.dataTransfer?.types?.includes('Files')) return;
      e.preventDefault();
      setDropZoneActive(true);
    },
    onDragOver: e => {
      if (!e.dataTransfer?.types?.includes('Files')) return;
      e.preventDefault();
      e.dataTransfer.dropEffect = 'copy';
      setDropZoneActive(true);
    },
    onDragLeave: e => {
      if (e.currentTarget.contains(e.relatedTarget)) return;
      setDropZoneActive(false);
    },
    onDrop: e => {
      e.preventDefault();
      setDropZoneActive(false);
      const files = Array.from(e.dataTransfer?.files || []);
      if (files.length && handleFiles) handleFiles(files);
    }
  }, Icon.upload, React.createElement("strong", {
    style: {
      marginTop: 6
    }
  }, "\uC774\uBBF8\uC9C0\xB7PDF\xB7HWP \uB300\uAE30\uC5F4\uC5D0 \uCD94\uAC00"), React.createElement("small", null, "\uD30C\uC77C\uBCC4\uB85C \uADF8\uB300\uB85C \uB4F1\uB85D\uD558\uAC70\uB098 AI \uC778\uC2DD\uD569\uB2C8\uB2E4")), !!recentSessions?.length && React.createElement("div", {
    className: `session-history-card ${recentSessionsCollapsed ? 'is-collapsed' : ''}`
  }, React.createElement("div", {
    className: "source-queue-head session-history-head"
  }, React.createElement("strong", null, "\uCD5C\uADFC \uC791\uC5C5"), React.createElement("span", null, recentSessions.length, "\uAC1C"), React.createElement("div", {
    className: "spacer"
  }), React.createElement("button", {
    className: "icon-btn",
    type: "button",
    title: recentSessionsCollapsed ? '최근 작업 펼치기' : '최근 작업 접기',
    "aria-expanded": !recentSessionsCollapsed,
    "aria-controls": "recent-session-history-list",
    onClick: toggleRecentSessionsCollapsed
  }, recentSessionsCollapsed ? Icon.arrowDown : Icon.arrowUp)), !recentSessionsCollapsed && React.createElement("div", {
    className: "session-history-list",
    id: "recent-session-history-list"
  }, recentSessions.slice(0, 5).map(entry => {
    const publish = normalizePublishSummary(entry.publishSummary || entry.publish_summary, entry);
    return React.createElement("div", {
      className: "session-history-row",
      key: entry.id
    }, React.createElement("div", {
      className: "session-history-main",
      title: entry.outputDir || entry.sessionName
    }, React.createElement("div", {
      className: "name"
    }, entry.sessionName || '이름 없는 작업'), React.createElement("div", {
      className: "meta"
    }, formatProblemCount({
      core: entry.coreProblemCount,
      supplemental: entry.supplementalItemCount
    }), entry.updatedAt ? ` · ${formatPublishTime(entry.updatedAt)}` : '', publish?.recordCountLabel ? ` · ${publish.recordCountLabel}` : '', publish?.classinReviewStatusLabel ? ` · ${publish.classinReviewStatusLabel}` : '')), React.createElement("div", {
      className: "session-history-actions"
    }, publish && React.createElement(React.Fragment, null, React.createElement("button", {
      className: "icon-btn",
      type: "button",
      disabled: !publish.canDownload,
      onClick: () => downloadPublishSummary(publish),
      title: publish.edbFileExists === false ? '최근 제작본 파일이 없습니다' : '최근 제작본 다운로드'
    }, Icon.download), React.createElement("button", {
      className: "icon-btn",
      type: "button",
      disabled: !publish.canOpenEdbFile,
      onClick: () => openPublishedEdb(publish),
      title: publish.edbFileExists === false ? '최근 제작본 파일이 없습니다' : 'ClassIn 또는 기본 앱으로 열기'
    }, Icon.board), React.createElement("button", {
      className: "icon-btn",
      type: "button",
      disabled: !publish.canOpenOutputDir,
      onClick: () => openOutputFolder(publish.outputDir),
      title: publish.outputDirExists === false ? '최근 작업 출력 폴더가 없습니다' : '최근 작업 출력 폴더 열기'
    }, Icon.folder), React.createElement("button", {
      className: "icon-btn",
      type: "button",
      disabled: !publish.canOpenClassinHandoff,
      onClick: () => openClassinHandoff(publish),
      title: publish.canOpenClassinHandoff ? 'ClassIn 검수 파일 열기' : 'ClassIn 검수 파일이 없습니다'
    }, Icon.check)), React.createElement("button", {
      className: "btn",
      type: "button",
      disabled: restoringSessionId === entry.id,
      onClick: () => onRestoreRecentSession?.(entry.id),
      title: "\uC774 \uC791\uC5C5\uC744 \uB2E4\uC2DC \uC5FD\uB2C8\uB2E4"
    }, Icon.refresh, React.createElement("span", null, restoringSessionId === entry.id ? '여는 중' : '열기'))));
  }))), !!pendingFiles?.length && React.createElement("div", {
    className: "source-queue-card"
  }, React.createElement("div", {
    className: "source-queue-head"
  }, React.createElement("strong", null, "\uC5C5\uB85C\uB4DC \uB300\uAE30\uC5F4"), React.createElement("span", null, pendingFiles.length, "\uAC1C"), React.createElement("div", {
    className: "spacer"
  }), React.createElement("button", {
    className: "icon-btn",
    title: "\uB300\uAE30\uC5F4 \uBE44\uC6B0\uAE30",
    onClick: clearPendingFiles,
    disabled: queueBusy
  }, Icon.trash)), React.createElement("div", {
    className: "source-queue-list"
  }, pendingFiles.map((file, index) => {
    const key = fileQueueKey(file);
    return React.createElement("div", {
      className: "source-queue-row",
      key: key
    }, React.createElement("span", {
      className: "idx"
    }, String(index + 1).padStart(2, '0')), React.createElement("div", {
      className: "file"
    }, React.createElement("div", {
      className: "name"
    }, file.name || '이름 없는 파일'), React.createElement("div", {
      className: "meta"
    }, sourceFileKindLabel(file), " \xB7 ", formatBytes(file.size))), React.createElement("button", {
      className: "icon-btn queue-row-action",
      title: "\uC774 \uD30C\uC77C\uC744 \uD55C \uBB38\uC81C/\uD55C \uC790\uB8CC\uB85C \uADF8\uB300\uB85C \uB4F1\uB85D",
      onClick: () => processQueuedFiles('register', key),
      disabled: queueBusy
    }, Icon.check), React.createElement("button", {
      className: "icon-btn queue-row-action",
      title: "\uC774 \uD30C\uC77C\uB9CC AI \uBB38\uC81C \uC778\uC2DD",
      onClick: () => processQueuedFiles('recognize', key),
      disabled: queueBusy
    }, Icon.aiBatch), React.createElement("button", {
      className: "icon-btn",
      title: "\uB300\uAE30\uC5F4\uC5D0\uC11C \uC81C\uAC70",
      onClick: () => removePendingFile(key),
      disabled: queueBusy
    }, Icon.trash));
  })), React.createElement("div", {
    className: "source-queue-actions"
  }, React.createElement("button", {
    className: "btn",
    type: "button",
    title: "\uB300\uAE30\uC5F4 \uC804\uCCB4\uB97C \uD55C \uBB38\uC81C \uB610\uB294 \uD55C \uC790\uB8CC\uB85C \uADF8\uB300\uB85C \uB4F1\uB85D",
    onClick: () => processQueuedFiles('register'),
    disabled: queueBusy
  }, Icon.check, React.createElement("span", null, "\uC804\uCCB4 \uADF8\uB300\uB85C \uB4F1\uB85D")), React.createElement("button", {
    className: "btn primary",
    type: "button",
    title: "\uB300\uAE30\uC5F4 \uC804\uCCB4\uB97C \uBB38\uC81C\uBCC4\uB85C AI \uC778\uC2DD",
    onClick: () => processQueuedFiles('recognize'),
    disabled: queueBusy
  }, Icon.aiBatch, React.createElement("span", null, "\uC804\uCCB4 AI \uC778\uC2DD")), !aiAvailable && React.createElement("div", {
    className: "source-queue-note full"
  }, "Gemini \uD0A4 \uC5C6\uC74C \xB7 \uAE30\uBCF8 \uC778\uC2DD\uC73C\uB85C \uC2E4\uD589"))), items.map((it, i) => {
    const dropPosition = dropTarget?.id === it.id ? dropTarget.position : null;
    return React.createElement("div", {
      key: it.id,
      ref: el => {
        if (el) itemRefs.current[it.id] = el;else delete itemRefs.current[it.id];
      },
      className: `item ${activeId === it.id ? 'active' : ''} ${draggingId === it.id ? 'dragging' : ''} ${dropPosition === 'before' ? 'drop-before' : ''} ${dropPosition === 'after' ? 'drop-after' : ''}`,
      "data-item-id": it.id,
      onClick: () => {
        if (suppressClickRef.current) return;
        setActive(it.id);
      },
      onPointerDown: e => startPointerDrag(e, it.id),
      onPointerMove: movePointerDrag,
      onPointerUp: finishPointerDrag,
      onPointerCancel: finishPointerDrag,
      onDragStart: e => {
        dragId.current = it.id;
        setDraggingId(it.id);
        e.dataTransfer.effectAllowed = 'move';
        e.dataTransfer.setData('text/plain', it.id);
      },
      onDragEnter: e => updateDropTarget(e, it.id),
      onDragOver: e => updateDropTarget(e, it.id),
      onDragLeave: e => {
        if (e.currentTarget.contains(e.relatedTarget)) return;
        if (dropTargetRef.current?.id === it.id) setCurrentDropTarget(null);
      },
      onDrop: e => {
        e.preventDefault();
        const sourceId = e.dataTransfer.getData('text/plain') || dragId.current;
        const position = dropPositionFromClientY(e.currentTarget.getBoundingClientRect(), e.clientY);
        if (sourceId && sourceId !== it.id) reorder(sourceId, it.id, position, {
          resetPlacement: true
        });
        clearDragState();
      },
      onDragEnd: clearDragState
    }, React.createElement("div", {
      className: "grip",
      title: "\uB04C\uC5B4 \uC62E\uAE30\uAE30"
    }, React.createElement("span", {
      className: "idx"
    }, String(i + 1).padStart(2, '0'))), React.createElement("div", {
      className: "thumb"
    }, React.createElement(TileImage, {
      item: it,
      forceMode: "raw"
    })), React.createElement("div", {
      className: "meta"
    }, React.createElement("div", {
      className: "name"
    }, React.createElement("span", {
      className: `status-dot ${reviewStatusClass(it.reviewStatus)}`,
      title: it.statusReason || it.statusLabel
    }), it.name), React.createElement("div", {
      className: "sub"
    }, React.createElement("span", {
      className: `tag status-tag ${reviewStatusClass(it.reviewStatus)}`,
      title: it.statusLabel,
      "aria-label": it.statusLabel
    }, it.statusShortLabel || it.statusLabel), it.step === 's1' && React.createElement("span", {
      className: "tag s1"
    }, "1\uB2E8\uACC4"), it.step === 's2' && React.createElement("span", {
      className: "tag s2"
    }, "AI"), it.step === 's3' && React.createElement("span", {
      className: "tag s3"
    }, "\uC7AC\uAD6C\uC131"), it.step === 'raw' && React.createElement("span", {
      className: "tag"
    }, "\uB300\uAE30"), React.createElement("span", {
      style: {
        whiteSpace: 'nowrap',
        overflow: 'hidden',
        textOverflow: 'ellipsis'
      }
    }, it.source))), React.createElement("div", {
      className: "actions"
    }, React.createElement("button", {
      className: "icon-btn",
      title: "\uC0AD\uC81C",
      onClick: e => {
        e.stopPropagation();
        removeItem(it.id);
      }
    }, Icon.trash)));
  })));
}
function BoardStage({
  items,
  activeId,
  setActive,
  boardColor,
  fileName,
  addSample,
  setPlacement,
  reorder
}) {
  const scrollRef = useRef(null);
  const contentRef = useRef(null);
  const tileRefs = useRef({});
  const syncLock = useRef(0);
  const positionDragRef = useRef(null);
  const suppressClickRef = useRef(null);
  const boardDropTargetRef = useRef(null);
  const autoScrollRef = useRef({
    raf: null,
    clientY: null
  });
  const [positioningId, setPositioningId] = useState(null);
  const [boardDropTarget, setBoardDropTarget] = useState(null);
  const [pageH, setPageH] = useState(400);
  const [contentW, setContentW] = useState(0);
  useEffect(() => {
    const el = scrollRef.current;
    if (!el) return;
    const measure = () => setPageH(el.clientHeight || 400);
    measure();
    const ro = new ResizeObserver(measure);
    ro.observe(el);
    return () => ro.disconnect();
  }, []);
  useEffect(() => {
    const el = contentRef.current;
    if (!el) return;
    const measure = () => setContentW(el.clientWidth || 0);
    measure();
    const ro = new ResizeObserver(measure);
    ro.observe(el);
    return () => ro.disconnect();
  }, []);
  const layout = useMemo(() => {
    const EPS = 0.001;
    const positions = [];
    const usesPlacement = items.some(it => Number.isFinite(it.startYPages));
    let cursorPages = 0;
    let maxBottom = 0;
    items.forEach(it => {
      const heightPages = Math.max(0.12, it.heightFrac || 0.8);
      const startPages = usesPlacement && Number.isFinite(it.startYPages) ? Math.max(0, it.startYPages) : cursorPages;
      const top = startPages * pageH;
      const height = heightPages * pageH;
      const snappedNext = Number.isFinite(it.snappedNextStartYPages) ? Math.max(startPages + heightPages, it.snappedNextStartYPages) : snapUpPages(startPages + heightPages);
      positions.push({
        top,
        height,
        page: Math.floor(top / pageH) + 1,
        spans: Math.max(1, Math.ceil(height / pageH)),
        startPages,
        heightPages,
        snappedNext
      });
      cursorPages = snappedNext;
      maxBottom = Math.max(maxBottom, snappedNext * pageH, top + height);
    });
    const endTop = items.length === 0 ? 0 : Math.ceil(maxBottom / pageH - EPS) * pageH;
    const endH = pageH * 0.42;
    const totalH = endTop + endH;
    const totalPages = Math.max(1, Math.ceil(totalH / pageH));
    return {
      positions,
      endTop,
      endH,
      totalH,
      totalPages,
      usesPlacement
    };
  }, [items, pageH]);
  const [scrollTop, setScrollTop] = useState(0);
  const currentPage = Math.min(layout.totalPages, Math.floor(scrollTop / pageH) + 1);
  useEffect(() => {
    if (Date.now() - syncLock.current < 450) return;
    const container = scrollRef.current;
    const idx = items.findIndex(x => x.id === activeId);
    if (!container || idx < 0) return;
    const target = Math.max(0, layout.positions[idx].top - 8);
    if (Math.abs(container.scrollTop - target) < 6) return;
    smoothScrollTo(container, target);
  }, [activeId, layout]);
  const onScroll = () => {
    const c = scrollRef.current;
    if (!c) return;
    setScrollTop(c.scrollTop);
    let nearestIdx = 0;
    let nearestDist = Infinity;
    layout.positions.forEach((p, idx) => {
      const dist = Math.abs(p.top - c.scrollTop - 24);
      if (dist < nearestDist) {
        nearestDist = dist;
        nearestIdx = idx;
      }
    });
    const id = items[nearestIdx]?.id;
    if (id && id !== activeId) {
      syncLock.current = Date.now();
      setActive(id);
    }
  };
  const onTileClick = id => {
    if (suppressClickRef.current === id) return;
    syncLock.current = Date.now();
    setActive(id);
  };
  const setCurrentBoardDropTarget = target => {
    boardDropTargetRef.current = target;
    setBoardDropTarget(prev => prev?.id === target?.id && prev?.position === target?.position ? prev : target);
  };
  const findBoardDropTarget = (clientY, sourceId) => {
    const content = contentRef.current;
    if (!content || !items.length) return null;
    const rect = content.getBoundingClientRect();
    const contentY = clientY - rect.top;
    const candidates = layout.positions.map((placement, index) => ({
      placement,
      item: items[index]
    })).filter(row => row.item?.id && row.item.id !== sourceId);
    if (!candidates.length) return null;
    for (const row of candidates) {
      const midpoint = row.placement.top + row.placement.height / 2;
      if (contentY < midpoint) {
        return {
          id: row.item.id,
          position: 'before'
        };
      }
    }
    const last = candidates[candidates.length - 1];
    return last?.item?.id ? {
      id: last.item.id,
      position: 'after'
    } : null;
  };
  const updateBoardDropTarget = (clientY, sourceId, force = false) => {
    const drag = positionDragRef.current;
    if (!force && drag && Math.abs(clientY - drag.startY) < BOARD_DRAG_REORDER_THRESHOLD_PX) {
      setCurrentBoardDropTarget(null);
      return null;
    }
    const target = findBoardDropTarget(clientY, sourceId);
    setCurrentBoardDropTarget(target);
    return target;
  };
  const stopBoardAutoScroll = () => {
    if (autoScrollRef.current.raf) {
      cancelAnimationFrame(autoScrollRef.current.raf);
    }
    autoScrollRef.current = {
      raf: null,
      clientY: null
    };
  };
  const stepBoardAutoScroll = () => {
    const drag = positionDragRef.current;
    const scroll = scrollRef.current;
    const clientY = autoScrollRef.current.clientY;
    if (!drag || !scroll || clientY == null) {
      stopBoardAutoScroll();
      return;
    }
    const rect = scroll.getBoundingClientRect();
    let delta = 0;
    if (clientY < rect.top + BOARD_DRAG_AUTOSCROLL_EDGE_PX) {
      const strength = 1 - Math.max(0, clientY - rect.top) / BOARD_DRAG_AUTOSCROLL_EDGE_PX;
      delta = -Math.ceil(strength * BOARD_DRAG_AUTOSCROLL_MAX_PX);
    } else if (clientY > rect.bottom - BOARD_DRAG_AUTOSCROLL_EDGE_PX) {
      const strength = 1 - Math.max(0, rect.bottom - clientY) / BOARD_DRAG_AUTOSCROLL_EDGE_PX;
      delta = Math.ceil(strength * BOARD_DRAG_AUTOSCROLL_MAX_PX);
    }
    if (delta) {
      scroll.scrollTop = Math.max(0, Math.min(scroll.scrollHeight - scroll.clientHeight, scroll.scrollTop + delta));
      setScrollTop(scroll.scrollTop);
      updateBoardDropTarget(clientY, drag.id);
      autoScrollRef.current.raf = requestAnimationFrame(stepBoardAutoScroll);
    } else {
      autoScrollRef.current.raf = null;
    }
  };
  const applyBoardAutoScroll = clientY => {
    autoScrollRef.current.clientY = clientY;
    if (!autoScrollRef.current.raf) {
      autoScrollRef.current.raf = requestAnimationFrame(stepBoardAutoScroll);
    }
  };
  useEffect(() => () => stopBoardAutoScroll(), []);
  const beginPositionDrag = (evt, item, placement) => {
    if (evt.button !== 0 || !contentRef.current) return;
    const contentRect = contentRef.current.getBoundingClientRect();
    const tileRect = evt.currentTarget.getBoundingClientRect();
    const maxLeft = Math.max(1, contentRect.width - tileRect.width);
    const maxTopOffset = Math.max(0, placement.snappedNext * pageH - placement.top - tileRect.height);
    const startXRatio = normalizePlacementXRatio(item.placementXRatio);
    const startYRatio = normalizePlacementYRatio(item.placementYRatio);
    positionDragRef.current = {
      id: item.id,
      pointerId: evt.pointerId,
      startX: evt.clientX,
      startY: evt.clientY,
      startLeft: startXRatio * maxLeft,
      startTopOffset: startYRatio * maxTopOffset,
      maxLeft,
      maxTopOffset,
      lastXRatio: startXRatio,
      lastYRatio: startYRatio,
      moved: false
    };
    setPositioningId(item.id);
    setCurrentBoardDropTarget(null);
    syncLock.current = Date.now();
    setActive(item.id);
    evt.currentTarget.setPointerCapture?.(evt.pointerId);
  };
  const movePositionDrag = evt => {
    const drag = positionDragRef.current;
    if (!drag || drag.pointerId !== evt.pointerId) return;
    const dx = evt.clientX - drag.startX;
    const dy = evt.clientY - drag.startY;
    if (!drag.moved && Math.hypot(dx, dy) < 4) return;
    drag.moved = true;
    evt.preventDefault();
    applyBoardAutoScroll(evt.clientY);
    updateBoardDropTarget(evt.clientY, drag.id);
    const nextLeft = Math.max(0, Math.min(drag.maxLeft, drag.startLeft + dx));
    const nextTopOffset = Math.max(0, Math.min(drag.maxTopOffset, drag.startTopOffset + dy));
    const nextXRatio = drag.maxLeft > 0 ? nextLeft / drag.maxLeft : DEFAULT_PLACEMENT_X_RATIO;
    const nextYRatio = drag.maxTopOffset > 0 ? nextTopOffset / drag.maxTopOffset : DEFAULT_PLACEMENT_Y_RATIO;
    if (Math.abs(nextXRatio - drag.lastXRatio) < 0.002 && Math.abs(nextYRatio - drag.lastYRatio) < 0.002) {
      return;
    }
    drag.lastXRatio = nextXRatio;
    drag.lastYRatio = nextYRatio;
    setPlacement?.(drag.id, {
      xRatio: nextXRatio,
      yRatio: nextYRatio
    });
  };
  const endPositionDrag = evt => {
    const drag = positionDragRef.current;
    if (!drag || drag.pointerId !== evt.pointerId) return;
    const canReorder = drag.moved && Math.abs(evt.clientY - drag.startY) >= BOARD_DRAG_REORDER_THRESHOLD_PX;
    const target = canReorder ? findBoardDropTarget(evt.clientY, drag.id) || boardDropTargetRef.current : null;
    if (drag.moved) {
      suppressClickRef.current = drag.id;
      window.setTimeout(() => {
        if (suppressClickRef.current === drag.id) suppressClickRef.current = null;
      }, 0);
      if (target && target.id && target.id !== drag.id) {
        reorder?.(drag.id, target.id, target.position, {
          resetPlacement: false
        });
      }
    }
    evt.currentTarget.releasePointerCapture?.(evt.pointerId);
    positionDragRef.current = null;
    setPositioningId(null);
    setCurrentBoardDropTarget(null);
    stopBoardAutoScroll();
  };
  const processedCount = items.filter(i => i.step !== 'raw').length;
  const aiCount = items.filter(i => i.step === 's2').length;
  const reconstructCount = items.filter(i => i.step === 's3').length;
  const rawCount = items.filter(i => i.step === 'raw').length;
  const s1Count = items.filter(i => i.step === 's1').length;
  const leftZonePercent = `${FIXED_LEFT_ZONE_RATIO * 100}%`;
  const dividers = [];
  for (let i = 1; i < Math.min(layout.totalPages, 50); i++) {
    dividers.push(i * pageH);
  }
  return React.createElement("div", {
    className: "col center"
  }, React.createElement("div", {
    className: "stage"
  }, React.createElement("div", {
    className: "stage-toolbar"
  }, React.createElement("span", {
    className: "name"
  }, "\uC2E4\uC2DC\uAC04 \uCE60\uD310 \uBBF8\uB9AC\uBCF4\uAE30"), React.createElement("span", {
    className: "pill"
  }, React.createElement("span", {
    className: "dotc"
  }), " ", fileName.length > 32 ? fileName.slice(0, 30) + '…' : fileName), React.createElement("div", {
    className: "spacer"
  }), React.createElement("button", {
    className: "btn ghost icon",
    title: "\uC790\uB3D9 \uC815\uB82C"
  }, Icon.align), React.createElement("button", {
    className: "btn ghost icon",
    title: "\uD655\uB300"
  }, Icon.zoomIn), React.createElement("div", {
    style: {
      width: 1,
      height: 22,
      background: 'var(--line)',
      margin: '0 4px'
    }
  }), React.createElement("button", {
    className: "btn ghost"
  }, React.createElement("span", {
    style: {
      opacity: .6,
      marginRight: 4
    }
  }, "\uD654\uBA74"), "\uB9DE\uCDA4")), React.createElement("div", {
    className: "stage-wrap"
  }, React.createElement("div", {
    className: "stage-board",
    style: {
      background: boardColor
    }
  }, React.createElement("div", {
    className: "stage-scroll",
    ref: scrollRef,
    onScroll: onScroll
  }, React.createElement("div", {
    className: "stage-content",
    ref: contentRef,
    style: {
      height: layout.totalH,
      '--left-zone-width': leftZonePercent
    }
  }, dividers.map((top, i) => React.createElement("div", {
    key: i,
    className: "page-divider",
    style: {
      top
    }
  }, React.createElement("span", {
    className: "label"
  }, "\u2014 ", i + 2, " \uD398\uC774\uC9C0 \u2014"))), items.map((it, i) => {
    const p = layout.positions[i];
    if (!p) return null;
    const tileWidth = contentW > 0 ? Math.max(120, contentW * FIXED_LEFT_ZONE_RATIO - 10) : null;
    const maxScale = tileWidth ? Math.max(PLACEMENT_SCALE_MIN, Math.min(PLACEMENT_SCALE_MAX, contentW / Math.max(tileWidth, 1), (p.snappedNext * pageH - p.top) / Math.max(p.height, 1))) : PLACEMENT_SCALE_MAX;
    const scaleRatio = normalizePlacementScaleRatio(it.placementScaleRatio, maxScale);
    const scaledWidth = tileWidth ? tileWidth * scaleRatio : null;
    const scaledHeight = p.height * scaleRatio;
    const maxLeft = scaledWidth ? Math.max(0, contentW - scaledWidth) : 0;
    const maxTopOffset = Math.max(0, p.snappedNext * pageH - p.top - scaledHeight);
    const xRatio = normalizePlacementXRatio(it.placementXRatio);
    const yRatio = normalizePlacementYRatio(it.placementYRatio);
    const dropPosition = boardDropTarget?.id === it.id ? boardDropTarget.position : null;
    const tileStyle = {
      top: p.top + yRatio * maxTopOffset,
      height: scaledHeight,
      ...(scaledWidth ? {
        left: xRatio * maxLeft,
        width: scaledWidth
      } : null)
    };
    return React.createElement("button", {
      key: it.id,
      ref: el => {
        tileRefs.current[it.id] = el;
      },
      className: `stage-tile ${activeId === it.id ? 'active' : ''} ${it.step === 's1' ? 'paper' : ''} ${positioningId === it.id ? 'positioning' : ''} ${dropPosition === 'before' ? 'drop-before' : ''} ${dropPosition === 'after' ? 'drop-after' : ''}`,
      onClick: () => onTileClick(it.id),
      title: it.name,
      style: tileStyle,
      onPointerDown: e => beginPositionDrag(e, it, p),
      onPointerMove: movePositionDrag,
      onPointerUp: endPositionDrag,
      onPointerCancel: endPositionDrag
    }, React.createElement("div", {
      className: "tile-hd"
    }, React.createElement("span", {
      className: "n"
    }, String(i + 1).padStart(2, '0')), React.createElement("span", {
      className: "nm"
    }, it.name), p.spans > 1 && React.createElement("span", {
      className: "span-mark"
    }, p.page, "\u2013", p.page + p.spans - 1, "p"), React.createElement("span", {
      className: `step-mark ${it.step}`
    }, it.step === 's1' ? '1' : it.step === 's2' ? 'AI' : it.step === 's3' ? 'HQ' : '··')), React.createElement("div", {
      className: "tile-art"
    }, React.createElement(TileImage, {
      item: it
    })));
  }), React.createElement("div", {
    className: "stage-tile-end",
    onClick: addSample,
    style: {
      top: layout.endTop,
      height: layout.endH
    }
  }, React.createElement("span", {
    className: "plus"
  }, "\uFF0B"), "\uC790\uB8CC \uCD94\uAC00", React.createElement("small", null, "\uCD5C\uB300 50 \uD398\uC774\uC9C0")))), React.createElement("div", {
    className: "board-pageind"
  }, React.createElement("span", null, String(currentPage).padStart(2, '0')), React.createElement("span", {
    className: "sep"
  }, "/"), React.createElement("span", {
    className: "total"
  }, String(layout.totalPages).padStart(2, '0')), React.createElement("span", {
    style: {
      marginLeft: 6,
      opacity: .6
    }
  }, "p")), React.createElement("div", {
    className: "vignette"
  }))), React.createElement("div", {
    className: "stage-status"
  }, React.createElement("span", {
    className: "chip"
  }, layout.usesPlacement ? 'Export 배치 기준' : '1문제 / 1.2페이지 · 자동 페이지 나눔'), React.createElement("span", {
    className: "chip"
  }, React.createElement("span", {
    style: {
      width: 8,
      height: 8,
      borderRadius: 2,
      background: '#aa6516'
    }
  }), "1\uB2E8\uACC4 ", s1Count), React.createElement("span", {
    className: "chip"
  }, React.createElement("span", {
    style: {
      width: 8,
      height: 8,
      borderRadius: 2,
      background: 'linear-gradient(135deg,#6d3df0,#2f6fed)'
    }
  }), "AI ", aiCount), React.createElement("span", {
    className: "chip"
  }, React.createElement("span", {
    style: {
      width: 8,
      height: 8,
      borderRadius: 2,
      background: 'linear-gradient(135deg,#10b981,#22d3ee)'
    }
  }), "\uC7AC\uAD6C\uC131 ", reconstructCount), rawCount > 0 && React.createElement("span", {
    className: "chip",
    style: {
      color: 'var(--danger)',
      borderColor: 'rgba(213,72,72,.3)'
    }
  }, "\uBBF8\uCC98\uB9AC ", rawCount), React.createElement("div", {
    className: "spacer"
  }), React.createElement("span", {
    style: {
      fontFamily: 'JetBrains Mono, monospace'
    }
  }, processedCount, "/", items.length, " \uCC98\uB9AC\uB428 \xB7 ", layout.totalPages, "p"))));
}
function downloadPublishSummary(target) {
  if (!target?.canDownload) return;
  const a = document.createElement('a');
  a.href = target.edbFileUri;
  a.download = target.edbFileName || 'classin.edb';
  document.body.appendChild(a);
  a.click();
  a.remove();
}
async function openPublishedEdb(target) {
  if (!target?.canOpenEdbFile || !target.edbPath) return;
  try {
    const resp = await fetch('/api/system/open-file', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        path: target.edbPath
      })
    });
    const json = await readJsonResponse(resp, '파일 열기 실패').catch(() => ({}));
    if (!resp.ok || !json.ok) {
      console.warn('[board] open-file failed:', json.error || resp.status);
    }
  } catch (e) {
    console.warn('[board] open-file error:', e.message);
  }
}
function openClassinHandoff(target) {
  const url = target?.classinHandoffMarkdownUri || target?.classinHandoffUri;
  if (!url) return;
  window.open(url, '_blank', 'noopener');
}
function PublishResultPanel({
  session,
  visible,
  onClassinReviewComplete
}) {
  const summary = useMemo(() => visible ? sessionPublishSummary(session) : null, [session, visible]);
  const history = useMemo(() => visible ? sessionPublishHistory(session) : [], [session, visible]);
  if (!summary) return null;
  return React.createElement("div", {
    className: "publish-result-panel"
  }, React.createElement("div", {
    className: "publish-result-head"
  }, React.createElement("div", {
    className: "publish-result-title"
  }, React.createElement("strong", null, "\uC81C\uC791 \uACB0\uACFC"), React.createElement("span", null, summary.statusLabel, " \xB7 ", summary.recordCountLabel || `${summary.recordCount || summary.recordCountActual}개 자료`)), React.createElement("span", {
    className: `publish-result-status ${summary.validated ? 'ok' : 'warn'}`
  }, summary.validated ? '검증 완료' : '확인 필요')), React.createElement("div", {
    className: "publish-result-file",
    title: summary.edbPath || summary.edbFileName
  }, summary.edbFileName), React.createElement("div", {
    className: "publish-result-metrics"
  }, React.createElement("span", null, summary.recordCountActual || summary.recordCount, " records"), summary.pageCountHint > 0 && React.createElement("span", null, summary.pageCountHint, "p hint"), summary.outerSize > 0 && React.createElement("span", null, formatBytes(summary.outerSize)), summary.classinHandoffStatusLabel && React.createElement("span", {
    title: "ClassIn \uC804\uB2EC \uC0C1\uD0DC"
  }, summary.classinHandoffStatusLabel), summary.classinPreflightStatusLabel && React.createElement("span", {
    title: "ClassIn \uC0AC\uC804\uC810\uAC80"
  }, summary.classinPreflightStatusLabel), summary.classinPreflightIssueSummaryLabel && React.createElement("span", {
    title: "ClassIn \uC0AC\uC804\uC810\uAC80 \uC774\uC288"
  }, summary.classinPreflightIssueSummaryLabel), summary.passageGroupLabel && React.createElement("span", {
    title: "\uAE34 \uC9C0\uBB38/\uACF5\uD1B5 \uC9C0\uBB38 \uADF8\uB8F9"
  }, summary.passageGroupLabel), summary.passageReviewLabel && React.createElement("span", {
    title: "\uAE34 \uC9C0\uBB38 \uAC80\uC218 \uD050"
  }, summary.passageReviewLabel), summary.passageReviewReasonLabel && React.createElement("span", {
    title: "\uAE34 \uC9C0\uBB38 \uAC80\uC218 \uC0AC\uC720"
  }, summary.passageReviewReasonLabel), summary.sourceProblemOverlapLabel && React.createElement("span", {
    title: "\uC6D0\uBCF8 \uBB38\uC81C \uC601\uC5ED \uACB9\uCE68"
  }, summary.sourceProblemOverlapLabel), summary.passageGroupSourceReuseLabel && React.createElement("span", {
    title: "\uC9C0\uBB38 \uC6D0\uBCF8 \uC911\uBCF5"
  }, summary.passageGroupSourceReuseLabel), summary.classinReviewStatusLabel && React.createElement("span", null, summary.classinReviewStatusLabel)), React.createElement("div", {
    className: "publish-result-actions"
  }, React.createElement("button", {
    className: "btn",
    type: "button",
    onClick: () => downloadPublishSummary(summary),
    disabled: !summary.canDownload,
    title: summary.edbFileExists === false ? 'EDB 파일이 없습니다' : 'EDB 다시 다운로드'
  }, Icon.download, React.createElement("span", null, summary.edbFileExists === false ? '파일 없음' : '다운로드')), React.createElement("button", {
    className: "btn",
    type: "button",
    onClick: () => openPublishedEdb(summary),
    disabled: !summary.canOpenEdbFile,
    title: summary.edbFileExists === false ? 'EDB 파일이 없습니다' : 'ClassIn 또는 기본 앱으로 열기'
  }, Icon.board, React.createElement("span", null, "ClassIn \uC5F4\uAE30")), React.createElement("button", {
    className: "btn",
    type: "button",
    onClick: () => summary.outputDir && openOutputFolder(summary.outputDir),
    disabled: !summary.canOpenOutputDir,
    title: summary.outputDirExists === false ? '출력 폴더가 없습니다' : '출력 폴더 열기'
  }, Icon.folder, React.createElement("span", null, summary.outputDirExists === false ? '폴더 없음' : '폴더')), React.createElement("button", {
    className: "btn",
    type: "button",
    onClick: () => openClassinHandoff(summary),
    disabled: !summary.canOpenClassinHandoff,
    title: summary.canOpenClassinHandoff ? 'ClassIn 검수 파일 열기' : 'ClassIn 검수 파일이 없습니다'
  }, Icon.check, React.createElement("span", null, "ClassIn \uAC80\uC218")), React.createElement("button", {
    className: "btn",
    type: "button",
    onClick: () => onClassinReviewComplete?.(),
    disabled: !summary.canMarkClassinReviewComplete || !onClassinReviewComplete,
    title: summary.classinReviewPassed ? '이미 ClassIn 검수를 완료했습니다' : 'ClassIn에서 확인한 결과를 완료로 저장'
  }, Icon.check, React.createElement("span", null, summary.classinReviewPassed ? '검수 완료됨' : '검수 완료'))), history.length > 1 && React.createElement("div", {
    className: "publish-history"
  }, React.createElement("div", {
    className: "publish-history-title"
  }, "\uCD5C\uADFC \uC81C\uC791"), history.slice(0, 5).map((item, index) => React.createElement("div", {
    className: "publish-history-row",
    key: `${item.edbPath || item.edbFileName}-${index}`
  }, React.createElement("div", {
    className: "publish-history-main",
    title: item.edbPath || item.edbFileName
  }, React.createElement("strong", null, index === 0 ? '최신' : `${index + 1}`), React.createElement("span", null, item.edbFileName)), React.createElement("small", null, formatPublishHistoryMeta(item), item.classinReviewStatusLabel ? ` · ${item.classinReviewStatusLabel}` : ''), React.createElement("button", {
    className: "icon-btn",
    type: "button",
    title: "\uC774 \uC81C\uC791\uBCF8 \uB2E4\uC6B4\uB85C\uB4DC",
    disabled: !item.canDownload,
    onClick: () => downloadPublishSummary(item)
  }, Icon.download), React.createElement("button", {
    className: "icon-btn",
    type: "button",
    title: "ClassIn \uB610\uB294 \uAE30\uBCF8 \uC571\uC73C\uB85C \uC5F4\uAE30",
    disabled: !item.canOpenEdbFile,
    onClick: () => openPublishedEdb(item)
  }, Icon.board), React.createElement("button", {
    className: "icon-btn",
    type: "button",
    title: "ClassIn \uAC80\uC218 \uD30C\uC77C \uC5F4\uAE30",
    disabled: !item.canOpenClassinHandoff,
    onClick: () => openClassinHandoff(item)
  }, Icon.check)))));
}
function SidePanel({
  item,
  items,
  activeIndex,
  setStep,
  applyToAll,
  bulk,
  setBulk,
  setPlacement,
  mutateSession,
  mutating,
  boardColumns,
  setBoardColumns,
  boardColor,
  setBoardColor,
  accent,
  setAccent,
  onConfirm,
  userSettings,
  runtimeDiagnostics,
  onSaveGeminiKey,
  onSaveOpenAiKey,
  onEnhanceImage,
  imageEnhanceBusy,
  aiEnabled,
  setAiEnabled,
  inputIntent,
  setInputIntent,
  onRecognizeSession,
  canRecognizeSession,
  session,
  published,
  onClassinReviewComplete,
  updateInfo,
  updateBusy,
  onCheckUpdate,
  onOpenUpdate
}) {
  const [tab, setTab] = useState('item');
  const [previewMode, setPreviewMode] = useState('raw');
  const [compareX, setCompareX] = useState(50);
  const [keyDraft, setKeyDraft] = useState('');
  const [openAiKeyDraft, setOpenAiKeyDraft] = useState('');
  const [showKey, setShowKey] = useState(false);
  const [showOpenAiKey, setShowOpenAiKey] = useState(false);
  const [hangulDetailsExpanded, setHangulDetailsExpanded] = useState(false);
  const [cropDraft, setCropDraft] = useState({
    ...EMPTY_MANUAL_CROP
  });
  const dragging = useRef(false);
  const wrapRef = useRef(null);
  const cropControlRef = useRef(null);
  const hangulDiagnostics = runtimeDiagnostics?.hangul || null;
  const hangulStatusMeta = hangulRuntimeStatusMeta(hangulDiagnostics);
  const hangulToolRows = hangulRuntimeToolRows(hangulDiagnostics);
  const hangulDetailsOpen = !!hangulDiagnostics && hangulDiagnostics.status !== 'ready';
  useEffect(() => {
    const onMove = e => {
      if (!dragging.current || !wrapRef.current) return;
      const r = wrapRef.current.getBoundingClientRect();
      const x = (e.clientX - r.left) / r.width * 100;
      setCompareX(Math.max(6, Math.min(94, x)));
    };
    const onUp = () => {
      dragging.current = false;
    };
    window.addEventListener('mousemove', onMove);
    window.addEventListener('mouseup', onUp);
    return () => {
      window.removeEventListener('mousemove', onMove);
      window.removeEventListener('mouseup', onUp);
    };
  }, []);
  useEffect(() => {
    if (hangulDiagnostics) setHangulDetailsExpanded(hangulDetailsOpen);
  }, [hangulDiagnostics?.status]);
  useEffect(() => {
    setCropDraft(item ? normalizeManualCrop(item.manualCrop) : {
      ...EMPTY_MANUAL_CROP
    });
  }, [item?.id, item?.manualCrop?.leftRatio, item?.manualCrop?.rightRatio, item?.manualCrop?.topRatio, item?.manualCrop?.bottomRatio]);
  const itemPosLabel = item ? `${String(activeIndex + 1).padStart(2, '0')} / ${String(items.length).padStart(2, '0')}` : '— / —';
  const maxScale = maxPlacementScaleRatio(item);
  const placementScale = item ? normalizePlacementScaleRatio(item.placementScaleRatio, maxScale) : DEFAULT_PLACEMENT_SCALE_RATIO;
  const placementX = item ? normalizePlacementXRatio(item.placementXRatio) : DEFAULT_PLACEMENT_X_RATIO;
  const placementY = item ? normalizePlacementYRatio(item.placementYRatio) : DEFAULT_PLACEMENT_Y_RATIO;
  const hasVerticalRoom = verticalPlacementRoomPages(item, placementScale) > 0.001;
  const canZoomOut = item && placementScale > PLACEMENT_SCALE_MIN + 0.001;
  const canZoomIn = item && placementScale < maxScale - 0.001;
  const canEnhanceCurrent = !!item && !!userSettings?.hasGeminiApiKey && !imageEnhanceBusy;
  const updateStatus = updateInfo?.channelStatus || 'unknown';
  const updateDownloadUrl = updateInfo?.downloadUrl || updateInfo?.latest?.downloadUrl || updateInfo?.releaseNotesUrl || updateInfo?.latest?.releaseNotesUrl || '';
  const updateStatusLabel = updateBusy ? '확인 중' : updateInfo?.updateAvailable ? '새 버전' : updateStatus === 'up_to_date' ? '최신' : updateStatus === 'manual_download' ? '수동' : updateStatus === 'not_configured' ? '미설정' : updateStatus === 'error' ? '오류' : updateStatus === 'unsupported_platform' ? '미지원' : '확인 전';
  const updateStatusTone = updateInfo?.updateAvailable ? 'var(--accent)' : updateStatus === 'up_to_date' ? 'var(--ok)' : updateStatus === 'error' ? 'var(--danger)' : 'var(--muted)';
  const updateVersionLine = updateInfo?.currentVersion ? `현재 ${updateInfo.currentVersion}${updateInfo?.latest?.version ? ` · 최신 ${updateInfo.latest.version}` : ''}` : '버전 정보를 불러오지 않았습니다';
  const savedCrop = item ? normalizeManualCrop(item.manualCrop) : {
    ...EMPTY_MANUAL_CROP
  };
  const cropChanged = item && !manualCropEquals(cropDraft, savedCrop);
  const savedCropActive = manualCropIsActive(savedCrop);
  const draftCropActive = manualCropIsActive(cropDraft);
  const updatePlacement = patch => {
    if (!item) return;
    setPlacement?.(item.id, patch);
  };
  const nudgePlacement = (dx, dy) => {
    if (!item) return;
    updatePlacement({
      xRatio: placementX + dx,
      yRatio: placementY + dy
    });
  };
  const nudgeScale = delta => {
    if (!item) return;
    updatePlacement({
      scaleRatio: placementScale + delta
    });
  };
  const resetPlacement = () => {
    updatePlacement({
      xRatio: DEFAULT_PLACEMENT_X_RATIO,
      yRatio: DEFAULT_PLACEMENT_Y_RATIO
    });
  };
  const resetScale = () => {
    updatePlacement({
      scaleRatio: DEFAULT_PLACEMENT_SCALE_RATIO
    });
  };
  const updateCropDraft = (key, value) => {
    setCropDraft(prev => normalizeManualCrop({
      ...prev,
      [key]: value
    }));
  };
  const adjustCropDraft = patch => {
    setCropDraft(prev => normalizeManualCrop({
      ...prev,
      ...Object.fromEntries(Object.entries(patch).map(([key, delta]) => [key, (prev[key] || 0) + delta]))
    }));
  };
  const focusManualCrop = () => {
    setPreviewMode('raw');
    cropControlRef.current?.scrollIntoView({
      block: 'nearest'
    });
  };
  const applyManualCrop = (nextCrop = cropDraft) => {
    if (!item || mutating) return;
    const normalized = normalizeManualCrop(nextCrop);
    setCropDraft(normalized);
    mutateSession?.('crop', {
      problemId: item.id,
      crop: normalized
    });
  };
  const resetManualCrop = () => {
    const reset = {
      ...EMPTY_MANUAL_CROP
    };
    setCropDraft(reset);
    if (savedCropActive) applyManualCrop(reset);
  };
  return React.createElement("div", {
    className: "col right"
  }, React.createElement("div", {
    className: "tab-bar"
  }, React.createElement("button", {
    className: tab === 'item' ? 'on' : '',
    onClick: () => setTab('item')
  }, "\uC120\uD0DD \uC790\uB8CC ", React.createElement("span", {
    className: "badge"
  }, itemPosLabel)), React.createElement("button", {
    className: tab === 'board' ? 'on' : '',
    onClick: () => setTab('board')
  }, "\uCE60\uD310 \uC124\uC815")), React.createElement(PublishResultPanel, {
    session: session,
    visible: published,
    onClassinReviewComplete: onClassinReviewComplete
  }), tab === 'item' && React.createElement(React.Fragment, null, React.createElement("div", {
    className: "tab-body"
  }, item ? React.createElement(React.Fragment, null, React.createElement("div", {
    className: "item-meta"
  }, React.createElement("div", {
    className: "nm"
  }, React.createElement("div", {
    className: "t"
  }, item.name), React.createElement("div", {
    className: "s"
  }, React.createElement("span", {
    className: `status-badge ${reviewStatusClass(item.reviewStatus)}`
  }, item.statusLabel), item.source, " \xB7 ", item.type.toUpperCase())), React.createElement("div", {
    className: "pos-tag"
  }, itemPosLabel)), React.createElement("div", {
    className: "item-preview",
    ref: wrapRef
  }, React.createElement("div", {
    className: "ptab"
  }, React.createElement("button", {
    className: previewMode === 'raw' ? 'on' : '',
    onClick: () => setPreviewMode('raw')
  }, "\uC6D0\uBCF8"), React.createElement("button", {
    className: previewMode === 'chalk' ? 'on' : '',
    onClick: () => setPreviewMode('chalk')
  }, "\uCE60\uD310\uC6A9"), React.createElement("button", {
    className: previewMode === 'compare' ? 'on' : '',
    onClick: () => setPreviewMode('compare')
  }, "\uBE44\uAD50")), previewMode === 'compare' ? React.createElement("div", {
    className: "canvas-mini",
    style: {
      background: 'white'
    }
  }, React.createElement("div", {
    className: "inner"
  }, React.createElement(TileImage, {
    item: item,
    forceMode: "raw"
  })), React.createElement("div", {
    style: {
      position: 'absolute',
      inset: 0,
      clipPath: `inset(0 0 0 ${compareX}%)`,
      background: boardColor
    }
  }, React.createElement("div", {
    className: "inner"
  }, React.createElement(TileImage, {
    item: item,
    forceMode: "chalk"
  }))), React.createElement("div", {
    className: "compare-handle",
    style: {
      left: `${compareX}%`
    },
    onMouseDown: () => {
      dragging.current = true;
    }
  })) : React.createElement("div", {
    className: `canvas-mini ${previewMode === 'chalk' ? 'board' : ''}`,
    style: previewMode === 'chalk' ? {
      background: boardColor
    } : null
  }, React.createElement("div", {
    className: "inner"
  }, React.createElement(TileImage, {
    item: item,
    forceMode: previewMode === 'chalk' ? 'chalk' : 'raw'
  }))), React.createElement("div", {
    className: "ptools"
  }, React.createElement("button", {
    className: "icon-btn",
    title: "\uD68C\uC804"
  }, Icon.rotate), React.createElement("button", {
    className: `icon-btn ${savedCropActive ? 'on' : ''}`,
    type: "button",
    title: "\uC0C1\uD558\uC88C\uC6B0 \uC5EC\uBC31 \uC790\uB974\uAE30",
    onClick: focusManualCrop
  }, Icon.crop), React.createElement("button", {
    className: "icon-btn",
    title: "\uCD95\uC18C",
    disabled: !canZoomOut,
    onClick: () => nudgeScale(-PLACEMENT_SCALE_STEP)
  }, Icon.zoomOut), React.createElement("button", {
    className: "icon-btn",
    title: "\uD655\uB300",
    disabled: !canZoomIn,
    onClick: () => nudgeScale(PLACEMENT_SCALE_STEP)
  }, Icon.zoomIn), React.createElement("div", {
    className: "spacer"
  }), React.createElement("span", {
    className: "scale"
  }, Math.round(placementScale * 100), "%"))), React.createElement("div", {
    className: "panel-section-hd"
  }, "\uCC98\uB9AC \uBC29\uC2DD \uC120\uD0DD ", React.createElement("span", {
    className: "line"
  })), React.createElement("div", {
    className: "steps"
  }, React.createElement("button", {
    className: `step-row ${item.step === 's1' ? 'on' : ''}`,
    onClick: () => setStep(item.id, 's1')
  }, React.createElement("span", {
    className: "radio"
  }), React.createElement("div", null, React.createElement("div", {
    className: "t"
  }, "1\uB2E8\uACC4 \xB7 \uADF8\uB300\uB85C \uBD99\uC774\uAE30"), React.createElement("div", {
    className: "d"
  }, "\uC2A4\uCE94\uD55C \uBAA8\uC591 \uADF8\uB300\uB85C. \uC6D0\uBCF8 \uC0C9\xB7\uC5EC\uBC31 \uC720\uC9C0.")), React.createElement("div", {
    className: "meta-r"
  }, "\uC989\uC2DC", React.createElement("strong", null, "~ 0.3s"))), React.createElement("button", {
    className: `step-row ${item.step === 's2' ? 'on' : ''}`,
    onClick: () => setStep(item.id, 's2')
  }, React.createElement("span", {
    className: "radio"
  }), React.createElement("div", null, React.createElement("div", {
    className: "t"
  }, "2\uB2E8\uACC4 \xB7 AI \uCE60\uD310 \uBCC0\uD658 ", React.createElement("span", {
    className: "ai-badge"
  }, "AI")), React.createElement("div", {
    className: "d"
  }, "\uBC30\uACBD \uC81C\uAC70 \xB7 \uBD84\uD544 \uC0C9\uC0C1 \uC790\uB3D9 \uBC30\uCE58 \xB7 \uAC00\uB3C5\uC131 \uCD5C\uC801\uD654.")), React.createElement("div", {
    className: "meta-r"
  }, "\uC790\uB3D9", React.createElement("strong", null, "~ 4s"))), React.createElement("button", {
    className: `step-row ${item.step === 's3' ? 'on' : ''}`,
    onClick: () => setStep(item.id, 's3')
  }, React.createElement("span", {
    className: "radio"
  }), React.createElement("div", null, React.createElement("div", {
    className: "t"
  }, "3\uB2E8\uACC4 \xB7 \uACE0\uD654\uC9C8 \uC7AC\uAD6C\uC131 ", React.createElement("span", {
    className: "ai-badge"
  }, "HQ")), React.createElement("div", {
    className: "d"
  }, "\uC120\uD0DD\uD55C \uBB38\uC81C\uB9CC \uC5C5\uC2A4\uCF00\uC77C\uB9C1 \xB7 \uBC30\uACBD\uC81C\uAC70 \xB7 \uC644\uC804 \uD22C\uBA85 PNG\uB85C \uC81C\uC791.")), React.createElement("div", {
    className: "meta-r"
  }, "\uC81C\uC791\uC2DC", React.createElement("strong", null, "~ 8s")))), React.createElement("div", {
    className: "panel-section-hd"
  }, "\uC5EC\uBC31 \uC790\uB974\uAE30 ", React.createElement("span", {
    className: "line"
  })), React.createElement("div", {
    className: "manual-crop-control",
    ref: cropControlRef
  }, React.createElement("div", {
    className: "manual-crop-note"
  }, "+\uAC12\uC740 \uC601\uC5ED\uC744 \uBC14\uAE65\uC73C\uB85C \uB113\uD788\uACE0, -\uAC12\uC740 \uC548\uCABD\uC73C\uB85C \uC798\uB77C\uB0C5\uB2C8\uB2E4."), React.createElement("div", {
    className: "manual-crop-presets"
  }, React.createElement("button", {
    type: "button",
    className: "btn",
    disabled: !item || mutating,
    onClick: () => adjustCropDraft({
      leftRatio: -0.05,
      rightRatio: -0.05,
      topRatio: -0.05,
      bottomRatio: -0.05
    })
  }, "\uC0AC\uBC29 +5%"), React.createElement("button", {
    type: "button",
    className: "btn",
    disabled: !item || mutating,
    onClick: () => adjustCropDraft({
      topRatio: -0.05,
      bottomRatio: -0.05
    })
  }, "\uC704\uC544\uB798 +5%"), React.createElement("button", {
    type: "button",
    className: "btn",
    disabled: !item || mutating,
    onClick: () => adjustCropDraft({
      leftRatio: -0.05,
      rightRatio: -0.05
    })
  }, "\uC88C\uC6B0 +5%"), React.createElement("button", {
    type: "button",
    className: "btn",
    disabled: !item || mutating,
    onClick: () => adjustCropDraft({
      leftRatio: 0.05,
      rightRatio: 0.05,
      topRatio: 0.05,
      bottomRatio: 0.05
    })
  }, "\uC0AC\uBC29 -5%")), React.createElement("div", {
    className: "manual-crop-grid"
  }, React.createElement("label", null, React.createElement("span", null, "\uC67C\uCABD"), React.createElement("input", {
    type: "range",
    min: -Math.round(MANUAL_CROP_OUTSET_MAX * 100),
    max: Math.round(MANUAL_CROP_EDGE_MAX * 100),
    step: Math.round(MANUAL_CROP_EDGE_STEP * 100),
    value: Math.round(cropDraft.leftRatio * 100),
    onChange: e => updateCropDraft('leftRatio', Number(e.target.value) / 100)
  }), React.createElement("strong", null, manualCropPercent(cropDraft.leftRatio))), React.createElement("label", null, React.createElement("span", null, "\uC624\uB978\uCABD"), React.createElement("input", {
    type: "range",
    min: -Math.round(MANUAL_CROP_OUTSET_MAX * 100),
    max: Math.round(MANUAL_CROP_EDGE_MAX * 100),
    step: Math.round(MANUAL_CROP_EDGE_STEP * 100),
    value: Math.round(cropDraft.rightRatio * 100),
    onChange: e => updateCropDraft('rightRatio', Number(e.target.value) / 100)
  }), React.createElement("strong", null, manualCropPercent(cropDraft.rightRatio))), React.createElement("label", null, React.createElement("span", null, "\uC704"), React.createElement("input", {
    type: "range",
    min: -Math.round(MANUAL_CROP_OUTSET_MAX * 100),
    max: Math.round(MANUAL_CROP_EDGE_MAX * 100),
    step: Math.round(MANUAL_CROP_EDGE_STEP * 100),
    value: Math.round(cropDraft.topRatio * 100),
    onChange: e => updateCropDraft('topRatio', Number(e.target.value) / 100)
  }), React.createElement("strong", null, manualCropPercent(cropDraft.topRatio))), React.createElement("label", null, React.createElement("span", null, "\uC544\uB798"), React.createElement("input", {
    type: "range",
    min: -Math.round(MANUAL_CROP_OUTSET_MAX * 100),
    max: Math.round(MANUAL_CROP_EDGE_MAX * 100),
    step: Math.round(MANUAL_CROP_EDGE_STEP * 100),
    value: Math.round(cropDraft.bottomRatio * 100),
    onChange: e => updateCropDraft('bottomRatio', Number(e.target.value) / 100)
  }), React.createElement("strong", null, manualCropPercent(cropDraft.bottomRatio)))), React.createElement("div", {
    className: "manual-crop-actions"
  }, React.createElement("button", {
    className: "btn",
    type: "button",
    disabled: !item || mutating || !draftCropActive && !savedCropActive,
    onClick: resetManualCrop
  }, "\uCD08\uAE30\uD654"), React.createElement("button", {
    className: "btn primary",
    type: "button",
    disabled: !item || mutating || !cropChanged,
    onClick: () => applyManualCrop()
  }, "\uC790\uB974\uAE30 \uC801\uC6A9"))), React.createElement("div", {
    className: "panel-section-hd"
  }, "\uC704\uCE58\xB7\uD06C\uAE30 \uBBF8\uC138 \uC870\uC808 ", React.createElement("span", {
    className: "line"
  })), React.createElement("div", {
    className: "position-control"
  }, React.createElement("div", {
    className: "position-pad",
    "aria-label": "\uC120\uD0DD \uC790\uB8CC \uC704\uCE58 \uBBF8\uC138 \uC870\uC808"
  }, React.createElement("button", {
    className: "pos-btn up",
    type: "button",
    title: "\uC704\uB85C",
    disabled: !hasVerticalRoom,
    onClick: () => nudgePlacement(0, -PLACEMENT_NUDGE_STEP)
  }, Icon.arrowUp), React.createElement("button", {
    className: "pos-btn left",
    type: "button",
    title: "\uC67C\uCABD\uC73C\uB85C",
    onClick: () => nudgePlacement(-PLACEMENT_NUDGE_STEP, 0)
  }, Icon.arrowLeft), React.createElement("button", {
    className: "pos-btn reset",
    type: "button",
    title: "\uC704\uCE58 \uCD08\uAE30\uD654",
    onClick: resetPlacement
  }, Icon.reset), React.createElement("button", {
    className: "pos-btn right",
    type: "button",
    title: "\uC624\uB978\uCABD\uC73C\uB85C",
    onClick: () => nudgePlacement(PLACEMENT_NUDGE_STEP, 0)
  }, Icon.arrowRight), React.createElement("button", {
    className: "pos-btn down",
    type: "button",
    title: "\uC544\uB798\uB85C",
    disabled: !hasVerticalRoom,
    onClick: () => nudgePlacement(0, PLACEMENT_NUDGE_STEP)
  }, Icon.arrowDown)), React.createElement("div", {
    className: "position-sliders"
  }, React.createElement("label", null, React.createElement("span", null, "\uC88C\uC6B0"), React.createElement("input", {
    type: "range",
    min: "0",
    max: "100",
    value: Math.round(placementX * 100),
    onChange: e => updatePlacement({
      xRatio: Number(e.target.value) / 100
    })
  }), React.createElement("strong", null, Math.round(placementX * 100), "%")), React.createElement("label", null, React.createElement("span", null, "\uC0C1\uD558"), React.createElement("input", {
    type: "range",
    min: "0",
    max: "100",
    value: Math.round((hasVerticalRoom ? placementY : 0) * 100),
    disabled: !hasVerticalRoom,
    onChange: e => updatePlacement({
      yRatio: Number(e.target.value) / 100
    })
  }), React.createElement("strong", null, Math.round((hasVerticalRoom ? placementY : 0) * 100), "%")), React.createElement("label", null, React.createElement("span", null, "\uD06C\uAE30"), React.createElement("input", {
    type: "range",
    min: Math.round(PLACEMENT_SCALE_MIN * 100),
    max: Math.round(maxScale * 100),
    value: Math.round(placementScale * 100),
    onChange: e => updatePlacement({
      scaleRatio: Number(e.target.value) / 100
    })
  }), React.createElement("strong", null, Math.round(placementScale * 100), "%")), React.createElement("div", {
    className: "scale-actions"
  }, React.createElement("button", {
    className: "icon-btn",
    type: "button",
    title: "\uCD95\uC18C",
    disabled: !canZoomOut,
    onClick: () => nudgeScale(-PLACEMENT_SCALE_STEP)
  }, Icon.zoomOut), React.createElement("button", {
    className: "scale-reset",
    type: "button",
    onClick: resetScale,
    disabled: !item || Math.abs(placementScale - DEFAULT_PLACEMENT_SCALE_RATIO) < 0.001
  }, "100%"), React.createElement("button", {
    className: "icon-btn",
    type: "button",
    title: "\uD655\uB300",
    disabled: !canZoomIn,
    onClick: () => nudgeScale(PLACEMENT_SCALE_STEP)
  }, Icon.zoomIn)))), React.createElement("div", {
    className: "panel-section-hd",
    style: {
      marginTop: 6
    }
  }, "\uCD94\uAC00 \uC5C5\uC2A4\uCF00\uC77C ", React.createElement("span", {
    className: "line"
  })), React.createElement("button", {
    className: "btn primary",
    type: "button",
    style: {
      width: '100%',
      justifyContent: 'space-between'
    },
    onClick: () => onEnhanceImage?.([item.id]),
    disabled: !canEnhanceCurrent,
    title: userSettings?.hasGeminiApiKey ? 'Nano Banana 2로 선택 문항을 투명 PNG로 재구성합니다' : 'Gemini API 키를 저장하면 사용할 수 있습니다'
  }, React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8
    }
  }, Icon.wand, " AI \uC5C5\uC2A4\uCF00\uC77C \uC7AC\uAD6C\uC131"), React.createElement("span", {
    style: {
      fontFamily: 'JetBrains Mono, monospace',
      fontSize: 11,
      opacity: .82
    }
  }, "Nano Banana 2")), React.createElement("div", {
    style: {
      fontSize: 11.5,
      lineHeight: 1.45,
      color: 'var(--muted)',
      marginTop: 7
    }
  }, "\uC6D0\uBB38\uC740 \uC720\uC9C0\uD558\uACE0 \uBB38\uC790\xB7\uC22B\uC790 \uC120\uBA85\uB3C4\uC640 \uD22C\uBA85 \uBC30\uACBD\uC744 \uAC1C\uC120\uD569\uB2C8\uB2E4. \uC801\uC6A9 \uD6C4 \uD14D\uC2A4\uD2B8 \uAC80\uD1A0 \uD45C\uC2DC\uAC00 \uB0A8\uC2B5\uB2C8\uB2E4.")) : React.createElement("div", {
    style: {
      padding: 40,
      textAlign: 'center',
      color: 'var(--muted)',
      fontSize: 13,
      border: '1px dashed var(--line)',
      borderRadius: 10
    }
  }, "\uC67C\uCABD \uBAA9\uB85D \uB610\uB294 \uCE60\uD310\uC5D0\uC11C", React.createElement("br", null), "\uC790\uB8CC\uB97C \uC120\uD0DD\uD558\uC138\uC694")), React.createElement("div", {
    className: "tab-foot"
  }, React.createElement("label", {
    className: "check"
  }, React.createElement("input", {
    type: "checkbox",
    checked: bulk,
    onChange: e => setBulk(e.target.checked)
  }), "\uC804\uCCB4 ", items.length, "\uAC1C \uC77C\uAD04"), React.createElement("div", {
    className: "spacer"
  }), React.createElement("button", {
    className: "btn",
    disabled: !item
  }, "\uAC74\uB108\uB6F0\uAE30"), React.createElement("button", {
    className: "btn primary",
    disabled: !item || item.step === 'raw',
    onClick: () => {
      if (!item) return;
      if (bulk) applyToAll(item.step, {
        silent: true
      });
      onConfirm(item.id, {
        bulk
      });
    }
  }, Icon.check, " ", bulk ? '일괄 확인' : '확인'))), tab === 'board' && React.createElement(React.Fragment, null, React.createElement("div", {
    className: "tab-body"
  }, React.createElement("div", {
    className: "panel-section-hd"
  }, "\uB808\uC774\uC544\uC6C3 ", React.createElement("span", {
    className: "line"
  })), React.createElement("div", {
    className: "row-control"
  }, React.createElement("div", {
    className: "lbl"
  }, "\uC5F4 \uC218", React.createElement("small", null, "\uD55C \uD589\uC5D0 \uB098\uB780\uD788 \uB458 \uC790\uB8CC \uC218")), React.createElement("div", {
    className: "seg-mini"
  }, [1, 2, 3].map(n => React.createElement("button", {
    key: n,
    className: boardColumns === n ? 'on' : '',
    onClick: () => setBoardColumns(n)
  }, n)))), React.createElement("div", {
    className: "row-control"
  }, React.createElement("div", {
    className: "lbl"
  }, "\uC2A4\uD06C\uB864 \uBAA8\uB4DC", React.createElement("small", null, "\uBC11\uC73C\uB85C \uBB34\uD55C \uC2A4\uD06C\uB864 (\uCD5C\uB300 50p)")), React.createElement("span", {
    className: "pos-tag",
    style: {
      background: 'var(--ok)'
    }
  }, "ON")), React.createElement("div", {
    className: "panel-section-hd",
    style: {
      marginTop: 4
    }
  }, "\uCE60\uD310 \uC0C9\uC0C1 ", React.createElement("span", {
    className: "line"
  })), React.createElement("div", {
    className: "row-control"
  }, React.createElement("div", {
    className: "lbl"
  }, "\uCE60\uD310 \uBC30\uACBD", React.createElement("small", null, "\uC2E4\uC81C \uAD50\uC2E4 \uCE60\uD310\uACFC \uB9DE\uCD94\uAE30")), React.createElement("div", {
    className: "swatches"
  }, BOARD_COLORS.map(c => React.createElement("div", {
    key: c,
    className: `sw ${boardColor === c ? 'on' : ''}`,
    style: {
      background: c
    },
    onClick: () => setBoardColor(c),
    title: c
  })))), React.createElement("div", {
    className: "row-control"
  }, React.createElement("div", {
    className: "lbl"
  }, "\uAC15\uC870\uC0C9", React.createElement("small", null, "UI \uAC15\uC870\uC5D0 \uC4F0\uC774\uB294 \uC0C9")), React.createElement("div", {
    className: "swatches"
  }, ACCENTS.map(c => React.createElement("div", {
    key: c,
    className: `sw ${accent === c ? 'on' : ''}`,
    style: {
      background: c
    },
    onClick: () => setAccent(c),
    title: c
  })))), React.createElement("div", {
    className: "panel-section-hd",
    style: {
      marginTop: 4
    }
  }, "\uC77C\uAD04 \uC791\uC5C5 ", React.createElement("span", {
    className: "line"
  })), React.createElement("button", {
    className: "btn",
    style: {
      justifyContent: 'space-between'
    },
    onClick: () => applyToAll('s2')
  }, React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8
    }
  }, Icon.aiBatch, " \uC804\uCCB4\uB97C 2\uB2E8\uACC4 AI \uBCC0\uD658"), React.createElement("span", {
    style: {
      fontFamily: 'JetBrains Mono, monospace',
      fontSize: 11,
      color: 'var(--muted)'
    }
  }, "~ ", items.length * 4, "s")), React.createElement("button", {
    className: "btn primary",
    style: {
      justifyContent: 'space-between'
    },
    onClick: onRecognizeSession,
    disabled: !canRecognizeSession,
    title: userSettings?.hasGeminiApiKey ? '현재 세션의 모든 원본 페이지를 문제 단위로 다시 인식' : 'Gemini API 키를 저장하면 AI 인식을 실행할 수 있습니다'
  }, React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8
    }
  }, Icon.aiBatch, " \uD604\uC7AC \uC790\uB8CC \uBB38\uC81C \uC778\uC2DD"), React.createElement("span", {
    style: {
      fontFamily: 'JetBrains Mono, monospace',
      fontSize: 11,
      opacity: .82
    }
  }, "AI")), React.createElement("button", {
    className: "btn",
    style: {
      justifyContent: 'space-between'
    },
    onClick: () => applyToAll('s1')
  }, React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8
    }
  }, Icon.check, " \uC804\uCCB4\uB97C 1\uB2E8\uACC4\uB85C"), React.createElement("span", {
    style: {
      fontFamily: 'JetBrains Mono, monospace',
      fontSize: 11,
      color: 'var(--muted)'
    }
  }, "\uC989\uC2DC")), React.createElement("div", {
    className: "panel-section-hd",
    style: {
      marginTop: 4
    }
  }, "\uC5C5\uB85C\uB4DC \uC635\uC158 ", React.createElement("span", {
    className: "line"
  })), React.createElement("div", {
    className: "row-control"
  }, React.createElement("div", {
    className: "lbl"
  }, "HWP/HWPX", React.createElement("small", null, hangulRuntimeSummary(hangulDiagnostics))), React.createElement("span", {
    className: "pos-tag",
    style: {
      background: hangulStatusMeta.tone,
      minWidth: 58,
      textAlign: 'center'
    },
    title: (hangulDiagnostics?.recommendedActions || [])[0] || '한글 문서 변환 상태'
  }, hangulStatusMeta.label)), hangulDiagnostics && React.createElement("div", {
    className: `runtime-details ${hangulDetailsExpanded ? 'open' : ''}`
  }, React.createElement("button", {
    className: "runtime-details-summary",
    type: "button",
    "aria-expanded": hangulDetailsExpanded ? 'true' : 'false',
    onClick: () => setHangulDetailsExpanded(open => !open)
  }, "\uBCC0\uD658 \uD658\uACBD"), hangulDetailsExpanded && React.createElement(React.Fragment, null, React.createElement("div", {
    className: "runtime-tool-grid"
  }, hangulToolRows.map(row => React.createElement("div", {
    key: row.label,
    className: "runtime-tool-row"
  }, React.createElement("span", null, row.label), React.createElement("strong", null, row.count), React.createElement("small", null, row.names || '없음')))), Array.isArray(hangulDiagnostics?.warnings) && hangulDiagnostics.warnings.length > 0 && React.createElement("div", {
    className: "runtime-note warn"
  }, hangulDiagnostics.warnings[0]), Array.isArray(hangulDiagnostics?.recommendedActions) && hangulDiagnostics.recommendedActions.length > 0 && React.createElement("div", {
    className: "runtime-note"
  }, hangulDiagnostics.recommendedActions[0]))), React.createElement("div", {
    className: "intent-control"
  }, INPUT_INTENT_OPTIONS.map(option => React.createElement("button", {
    key: option.value,
    className: `intent-choice ${normalizeInputIntent(inputIntent) === option.value ? 'on' : ''}`,
    type: "button",
    onClick: () => setInputIntent?.(option.value),
    title: option.description
  }, React.createElement("span", null, option.label), React.createElement("small", null, option.description)))), React.createElement("div", {
    className: "row-control"
  }, React.createElement("div", {
    className: "lbl"
  }, "AI \uBCF4\uC815 \uC0AC\uC6A9", React.createElement("small", null, userSettings?.hasGeminiApiKey ? 'Gemini 키 설정됨' : 'Gemini 키 없음 — 자동 비활성화')), React.createElement("label", {
    className: "check",
    style: {
      cursor: userSettings?.hasGeminiApiKey ? 'pointer' : 'not-allowed',
      opacity: userSettings?.hasGeminiApiKey ? 1 : .5
    }
  }, React.createElement("input", {
    type: "checkbox",
    checked: aiEnabled && !!userSettings?.hasGeminiApiKey,
    disabled: !userSettings?.hasGeminiApiKey,
    onChange: e => setAiEnabled && setAiEnabled(e.target.checked)
  }), React.createElement("span", {
    style: {
      fontSize: 12,
      color: 'var(--muted)'
    }
  }, aiEnabled && userSettings?.hasGeminiApiKey ? '켜짐' : '꺼짐'))), React.createElement("div", {
    className: "panel-section-hd",
    style: {
      marginTop: 4
    }
  }, "Gemini API \uD0A4 ", React.createElement("span", {
    className: "line"
  })), React.createElement("div", {
    className: "row-control",
    style: {
      gridTemplateColumns: '1fr'
    }
  }, React.createElement("div", {
    className: "lbl"
  }, React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8
    }
  }, React.createElement("span", {
    className: `pos-tag`,
    style: {
      background: userSettings?.hasGeminiApiKey ? 'var(--ok)' : 'var(--danger)'
    }
  }, userSettings?.hasGeminiApiKey ? '설정됨' : '미설정'), userSettings?.hasGeminiApiKey && React.createElement("span", {
    style: {
      fontSize: 11,
      color: 'var(--muted)',
      fontFamily: 'JetBrains Mono, monospace'
    }
  }, userSettings.geminiApiKeyPreview)), React.createElement("small", null, userSettings?.geminiApiKeySource === 'env' ? '환경변수의 GEMINI_API_KEY 사용 중. 저장하면 그 값이 우선.' : 'GEMINI_API_KEY로 자동 적용. .app_runtime/user_settings.json에 저장.'))), React.createElement("div", {
    className: "key-input-row"
  }, React.createElement("input", {
    type: showKey ? 'text' : 'password',
    className: "key-input",
    placeholder: userSettings?.hasGeminiApiKey ? `현재 ${userSettings.geminiApiKeyPreview} (덮어쓰기)` : 'AIza...',
    value: keyDraft,
    onChange: e => setKeyDraft(e.target.value),
    spellCheck: false,
    autoComplete: "off"
  }), React.createElement("button", {
    className: "btn icon",
    type: "button",
    onClick: () => setShowKey(s => !s),
    title: showKey ? '숨기기' : '보기'
  }, showKey ? '🙈' : '👁')), React.createElement("div", {
    style: {
      display: 'flex',
      gap: 6
    }
  }, React.createElement("button", {
    className: "btn primary",
    style: {
      flex: 1,
      justifyContent: 'center'
    },
    onClick: () => {
      onSaveGeminiKey?.(keyDraft.trim());
      setKeyDraft('');
    },
    disabled: !keyDraft.trim()
  }, "\uD0A4 \uC800\uC7A5"), React.createElement("button", {
    className: "btn",
    style: {
      flex: 1,
      justifyContent: 'center'
    },
    onClick: () => {
      if (window.confirm('저장된 Gemini API 키를 삭제할까요?')) onSaveGeminiKey?.('');
    },
    disabled: !userSettings?.hasStoredGeminiApiKey
  }, "\uC800\uC7A5\uB41C \uD0A4 \uC0AD\uC81C")), React.createElement("div", {
    className: "panel-section-hd",
    style: {
      marginTop: 4
    }
  }, "OpenAI API \uD0A4 ", React.createElement("span", {
    className: "line"
  })), React.createElement("div", {
    className: "row-control",
    style: {
      gridTemplateColumns: '1fr'
    }
  }, React.createElement("div", {
    className: "lbl"
  }, React.createElement("span", {
    style: {
      display: 'flex',
      alignItems: 'center',
      gap: 8
    }
  }, React.createElement("span", {
    className: `pos-tag`,
    style: {
      background: userSettings?.hasOpenAiApiKey ? 'var(--ok)' : 'var(--danger)'
    }
  }, userSettings?.hasOpenAiApiKey ? '설정됨' : '미설정'), userSettings?.hasOpenAiApiKey && React.createElement("span", {
    style: {
      fontSize: 11,
      color: 'var(--muted)',
      fontFamily: 'JetBrains Mono, monospace'
    }
  }, userSettings.openAiApiKeyPreview)), React.createElement("small", null, "OpenAI \uAE30\uBC18 \uC5C5\uC2A4\uCF00\uC77C \uC7AC\uAD6C\uC131 fallback\uC5D0\uB9CC \uC0AC\uC6A9\uD569\uB2C8\uB2E4. \uAE30\uBCF8 3\uB2E8\uACC4 \uC5C5\uC2A4\uCF00\uC77C\uC740 Gemini\uB97C \uC0AC\uC6A9\uD569\uB2C8\uB2E4."))), React.createElement("div", {
    className: "key-input-row"
  }, React.createElement("input", {
    type: showOpenAiKey ? 'text' : 'password',
    className: "key-input",
    placeholder: userSettings?.hasOpenAiApiKey ? `현재 ${userSettings.openAiApiKeyPreview} (덮어쓰기)` : 'sk-...',
    value: openAiKeyDraft,
    onChange: e => setOpenAiKeyDraft(e.target.value),
    spellCheck: false,
    autoComplete: "off"
  }), React.createElement("button", {
    className: "btn icon",
    type: "button",
    onClick: () => setShowOpenAiKey(s => !s),
    title: showOpenAiKey ? '숨기기' : '보기'
  }, showOpenAiKey ? '숨' : '보기')), React.createElement("div", {
    style: {
      display: 'flex',
      gap: 6
    }
  }, React.createElement("button", {
    className: "btn primary",
    style: {
      flex: 1,
      justifyContent: 'center'
    },
    onClick: () => {
      onSaveOpenAiKey?.(openAiKeyDraft.trim());
      setOpenAiKeyDraft('');
    },
    disabled: !openAiKeyDraft.trim()
  }, "\uD0A4 \uC800\uC7A5"), React.createElement("button", {
    className: "btn",
    style: {
      flex: 1,
      justifyContent: 'center'
    },
    onClick: () => {
      if (window.confirm('저장된 OpenAI API 키를 삭제할까요?')) onSaveOpenAiKey?.('');
    },
    disabled: !userSettings?.hasStoredOpenAiApiKey
  }, "\uC800\uC7A5\uB41C \uD0A4 \uC0AD\uC81C")), React.createElement("div", {
    className: "panel-section-hd",
    style: {
      marginTop: 4
    }
  }, "\uC571 \uC5C5\uB370\uC774\uD2B8 ", React.createElement("span", {
    className: "line"
  })), React.createElement("div", {
    className: "row-control"
  }, React.createElement("div", {
    className: "lbl"
  }, "\uD604\uC7AC \uBC84\uC804", React.createElement("small", null, updateVersionLine)), React.createElement("span", {
    className: "pos-tag",
    style: {
      background: updateStatusTone,
      minWidth: 58,
      textAlign: 'center'
    }
  }, updateStatusLabel)), updateInfo?.error && React.createElement("div", {
    className: "runtime-note warn"
  }, updateInfo.error), React.createElement("div", {
    style: {
      display: 'flex',
      gap: 6
    }
  }, React.createElement("button", {
    className: "btn",
    style: {
      flex: 1,
      justifyContent: 'center'
    },
    type: "button",
    onClick: () => onCheckUpdate?.(),
    disabled: updateBusy
  }, updateBusy ? '확인 중...' : '업데이트 확인'), React.createElement("button", {
    className: "btn primary",
    style: {
      flex: 1,
      justifyContent: 'center'
    },
    type: "button",
    onClick: () => onOpenUpdate?.(),
    disabled: !updateDownloadUrl,
    title: updateDownloadUrl ? '다운로드 페이지 열기' : '업데이트 다운로드 URL이 없습니다'
  }, Icon.download, " \uB2E4\uC6B4\uB85C\uB4DC \uC5F4\uAE30")))));
}
function LoadingOverlay({
  label,
  hint,
  startedAt
}) {
  const [elapsed, setElapsed] = useState(0);
  useEffect(() => {
    if (!startedAt) {
      setElapsed(0);
      return;
    }
    const tick = () => setElapsed(Math.floor((Date.now() - startedAt) / 1000));
    tick();
    const id = setInterval(tick, 500);
    return () => clearInterval(id);
  }, [startedAt]);
  const fmt = s => s >= 60 ? `${Math.floor(s / 60)}분 ${s % 60}초` : `${s}초`;
  return React.createElement("div", {
    className: "loading-overlay"
  }, React.createElement("div", {
    className: "loading-card"
  }, React.createElement("div", {
    className: "loading-spinner"
  }), React.createElement("div", {
    className: "loading-label"
  }, label), startedAt && React.createElement("div", {
    className: "loading-elapsed"
  }, fmt(elapsed), " \uACBD\uACFC"), hint && React.createElement("div", {
    className: "loading-hint"
  }, hint)));
}
function BackgroundJobsPanel({
  jobs,
  onCancel,
  onDismiss
}) {
  const visibleJobs = (jobs || []).filter(job => job.status !== 'dismissed');
  const [now, setNow] = useState(Date.now());
  useEffect(() => {
    if (!visibleJobs.some(job => job.status === 'running')) return;
    const id = setInterval(() => setNow(Date.now()), 500);
    return () => clearInterval(id);
  }, [visibleJobs]);
  if (!visibleJobs.length) return null;
  const elapsedLabel = startedAt => {
    const seconds = Math.max(0, Math.floor((now - startedAt) / 1000));
    return seconds >= 60 ? `${Math.floor(seconds / 60)}분 ${seconds % 60}초` : `${seconds}초`;
  };
  const statusLabel = status => {
    if (status === 'running') return '진행 중';
    if (status === 'failed') return '실패';
    if (status === 'canceled') return '취소됨';
    return '완료';
  };
  return React.createElement("div", {
    className: "bg-jobs",
    "aria-live": "polite"
  }, visibleJobs.map(job => {
    const isRecognition = String(job.scope || '').includes('recognition');
    return React.createElement("div", {
      key: job.id,
      className: `bg-job ${job.status}`
    }, React.createElement("div", {
      className: "bg-job-mark"
    }, job.status === 'running' ? React.createElement("span", {
      className: "mini-spinner"
    }) : React.createElement("span", null, job.status === 'failed' ? '!' : Icon.check)), React.createElement("div", {
      className: "bg-job-main"
    }, React.createElement("div", {
      className: "bg-job-title"
    }, React.createElement("strong", null, job.label), React.createElement("span", null, statusLabel(job.status))), job.hint && React.createElement("div", {
      className: "bg-job-hint"
    }, job.hint), job.status === 'running' && React.createElement("div", {
      className: "bg-job-time"
    }, elapsedLabel(job.startedAt), " \uACBD\uACFC")), job.status === 'running' ? React.createElement("button", {
      className: "bg-job-action",
      type: "button",
      onClick: () => onCancel?.(job.id)
    }, isRecognition ? '인식 중단' : '취소') : React.createElement("button", {
      className: "bg-job-action",
      type: "button",
      onClick: () => onDismiss?.(job.id)
    }, "\uB2EB\uAE30"));
  }));
}
function RecognitionReviewModal({
  review,
  confirming,
  onConfirm,
  onCancel
}) {
  const previewSession = review?.session;
  const targetPageIds = review?.pageIds || null;
  const pages = useMemo(() => {
    const allPages = Array.isArray(previewSession?.pages) ? previewSession.pages : [];
    if (!targetPageIds?.length) return allPages;
    const allowed = new Set(targetPageIds);
    return allPages.filter(page => allowed.has(page.id));
  }, [previewSession, targetPageIds]);
  const problemsById = useMemo(() => {
    const map = new Map();
    (previewSession?.problems || []).forEach(problem => {
      if (problem?.id) map.set(problem.id, problem);
    });
    return map;
  }, [previewSession]);
  const summary = useMemo(() => summarizeRecognitionSession(previewSession, targetPageIds), [previewSession, targetPageIds]);
  if (!review) return null;
  const title = review.title || 'AI 인식 결과 확인';
  const movesToReview = review?.kind === 'queue-recognition';
  const subtitle = review.subtitle || (movesToReview ? `${summary.problemLabel}로 분할했습니다. 맞으면 검수 화면에서 경계를 확인합니다.` : `${summary.problemLabel}로 분할했습니다. 맞으면 바로 칠판에 붙입니다.`);
  const confirmLabel = movesToReview ? '맞아요, 검수로 이동' : '맞아요, 칠판에 붙이기';
  const confirmingLabel = movesToReview ? '검수로 이동 중...' : '붙이는 중...';
  return React.createElement("div", {
    className: "recognition-modal-shell",
    role: "dialog",
    "aria-modal": "true",
    "aria-labelledby": "recognition-review-title"
  }, React.createElement("div", {
    className: "recognition-modal-backdrop",
    onClick: confirming ? undefined : onCancel
  }), React.createElement("div", {
    className: "recognition-modal"
  }, React.createElement("div", {
    className: "recognition-modal-hd"
  }, React.createElement("div", null, React.createElement("span", {
    className: "recognition-eyebrow"
  }, "AI \uC778\uC2DD \uACB0\uACFC"), React.createElement("h2", {
    id: "recognition-review-title"
  }, title), React.createElement("p", null, subtitle)), React.createElement("button", {
    className: "modal-x",
    type: "button",
    onClick: onCancel,
    disabled: confirming,
    title: "\uB2EB\uAE30"
  }, "\xD7")), React.createElement("div", {
    className: "recognition-summary"
  }, React.createElement("span", null, summary.pages, " \uD398\uC774\uC9C0"), React.createElement("span", null, summary.problemLabel), React.createElement("span", {
    className: summary.riskCount ? 'warn' : ''
  }, summary.riskCount ? `${summary.riskCount}개 확인 필요` : '위험 표시 없음')), React.createElement("div", {
    className: "recognition-preview"
  }, pages.length ? pages.map(page => {
    const pageProblems = (page.problemIds || page.problem_ids || []).map(id => problemsById.get(id)).filter(Boolean);
    return React.createElement("div", {
      key: page.id,
      className: "recognition-page"
    }, React.createElement("div", {
      className: "recognition-page-hd"
    }, React.createElement("strong", null, page.id), React.createElement("span", null, formatProblemCount(countSessionProblems(pageProblems))), React.createElement("span", null, page.width, "\xD7", page.height)), React.createElement("div", {
      className: "recognition-page-canvas"
    }, page.sourceImageUri ? React.createElement("img", {
      src: page.sourceImageUri,
      alt: page.id,
      draggable: false
    }) : React.createElement("div", {
      className: "recognition-page-empty"
    }, "\uD398\uC774\uC9C0 \uC774\uBBF8\uC9C0\uB97C \uBD88\uB7EC\uC62C \uC218 \uC5C6\uC5B4\uC694."), pageProblems.map((problem, index) => {
      const bbox = problem.bbox || {};
      const w = page.width || 1;
      const h = page.height || 1;
      if (!bbox.width || !bbox.height) return null;
      const status = deriveProblemStatus(problem);
      return React.createElement("div", {
        key: problem.id,
        className: `recognition-box ${reviewStatusClass(status)}`,
        style: {
          left: `${bbox.left / w * 100}%`,
          top: `${bbox.top / h * 100}%`,
          width: `${bbox.width / w * 100}%`,
          height: `${bbox.height / h * 100}%`
        },
        title: problem.title || problem.id
      }, React.createElement("span", null, String(index + 1).padStart(2, '0')));
    })));
  }) : React.createElement("div", {
    className: "recognition-empty"
  }, "\uD655\uC778\uD560 \uD398\uC774\uC9C0\uAC00 \uC5C6\uC2B5\uB2C8\uB2E4.")), React.createElement("div", {
    className: "recognition-modal-foot"
  }, React.createElement("button", {
    className: "btn",
    type: "button",
    onClick: onCancel,
    disabled: confirming
  }, "\uCDE8\uC18C"), React.createElement("button", {
    className: "btn primary",
    type: "button",
    onClick: onConfirm,
    disabled: confirming || !summary.problems
  }, confirming ? confirmingLabel : confirmLabel))));
}
function TileImage({
  item,
  forceMode
}) {
  const wantChalk = forceMode ? forceMode === 'chalk' : item.step !== 's1';
  const chalk = item.chalkUrl;
  const raw = item.imageUrl;
  const url = wantChalk ? chalk || raw : raw || chalk;
  if (url) {
    return React.createElement("img", {
      src: url,
      alt: item.name || '',
      className: `tile-img ${wantChalk ? 'is-chalk' : 'is-raw'}`,
      draggable: false
    });
  }
  return React.createElement(ItemArt, {
    kind: item.kind,
    mode: wantChalk ? 'chalk' : 'raw'
  });
}
function fileToBase64(file) {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();
    reader.onload = () => {
      const result = String(reader.result || '');
      const comma = result.indexOf(',');
      resolve(comma >= 0 ? result.slice(comma + 1) : result);
    };
    reader.onerror = () => reject(reader.error || new Error('file read failed'));
    reader.readAsDataURL(file);
  });
}
function fileQueueKey(file) {
  return [file?.name || 'file', file?.size || 0, file?.lastModified || 0].join('::');
}
function sourceFileExtension(file) {
  const match = String(file?.name || '').toLowerCase().match(/\.([^.]+)$/);
  return match ? match[1] : '';
}
function isPdfFile(file) {
  return sourceFileExtension(file) === 'pdf';
}
function isHwpFile(file) {
  return ['hwp', 'hwpx'].includes(sourceFileExtension(file));
}
function isDocumentLikeFile(file) {
  return isPdfFile(file) || isHwpFile(file);
}
function sourceFileKindLabel(file) {
  if (isPdfFile(file)) return 'PDF';
  if (isHwpFile(file)) return 'HWP';
  return 'IMG';
}
function formatBytes(bytes) {
  const size = Number(bytes);
  if (!Number.isFinite(size) || size <= 0) return '0KB';
  if (size >= 1024 * 1024) return `${(size / (1024 * 1024)).toFixed(1)}MB`;
  return `${Math.max(1, Math.round(size / 1024))}KB`;
}
function cloneSession(session) {
  return session ? JSON.parse(JSON.stringify(session)) : null;
}
function makeUniqueId(baseId, existingIds) {
  const raw = String(baseId || 'item').trim() || 'item';
  if (!existingIds.has(raw)) {
    existingIds.add(raw);
    return raw;
  }
  let counter = 2;
  while (existingIds.has(`${raw}-add-${counter}`)) counter += 1;
  const next = `${raw}-add-${counter}`;
  existingIds.add(next);
  return next;
}
function applyItemStateToProblem(problem, item) {
  const next = {
    ...problem
  };
  const actualHeightPages = Math.max(0.12, item.heightFrac || next.actualHeightPages || next.actual_height_pages || 0.8);
  const startYPages = Number.isFinite(item.startYPages) ? Math.max(0, item.startYPages) : null;
  const snappedNextStartYPages = Number.isFinite(item.snappedNextStartYPages) ? Math.max(startYPages || 0, item.snappedNextStartYPages) : null;
  next.title = item.name || next.title || '';
  next.riskFlags = Array.isArray(item.riskFlags) ? [...item.riskFlags] : [];
  next.risk_flags = next.riskFlags;
  next.reviewStatus = normalizeReviewStatus(item.reviewStatus) || deriveProblemStatus(next);
  next.review_status = next.reviewStatus;
  next.step = normalizeProcessingStep(item.step);
  next.processingStep = next.step;
  next.processing_step = next.step;
  next.placementXRatio = normalizePlacementXRatio(item.placementXRatio);
  next.placementYRatio = verticalPlacementRoomPages(item) > 0.001 ? normalizePlacementYRatio(item.placementYRatio) : DEFAULT_PLACEMENT_Y_RATIO;
  next.placementScaleRatio = normalizePlacementScaleRatio(item.placementScaleRatio, maxPlacementScaleRatio(item));
  next.actualHeightPages = actualHeightPages;
  next.actual_height_pages = actualHeightPages;
  if (startYPages !== null) {
    next.startYPages = Number(startYPages.toFixed(6));
    next.start_y_pages = next.startYPages;
  }
  if (snappedNextStartYPages !== null) {
    next.snappedNextStartYPages = Number(snappedNextStartYPages.toFixed(6));
    next.snapped_next_start_y_pages = next.snappedNextStartYPages;
    next.slotSpanCount = Math.max(1, Math.round((snappedNextStartYPages - (startYPages || 0)) / DEFAULT_SLOT_HEIGHT_PAGES));
    next.slot_span_count = next.slotSpanCount;
  }
  if (startYPages !== null) {
    next.actualBottomYPages = Number((startYPages + actualHeightPages).toFixed(6));
    next.actual_bottom_y_pages = next.actualBottomYPages;
  }
  next.overflowAmountPages = Math.max(0, actualHeightPages - DEFAULT_SLOT_HEIGHT_PAGES);
  next.overflow_amount_pages = next.overflowAmountPages;
  return next;
}
function confirmedItemState(item) {
  const statusMeta = reviewStatusMeta('normal');
  return {
    ...item,
    riskFlags: [],
    reviewStatus: 'normal',
    statusLabel: statusMeta.label,
    statusShortLabel: statusMeta.shortLabel || statusMeta.label,
    statusReason: '',
    retryable: false
  };
}
function confirmedProblemState(problem) {
  return {
    ...problem,
    riskFlags: [],
    risk_flags: [],
    reviewStatus: 'normal',
    review_status: 'normal',
    parseFailed: false,
    parse_failed: false
  };
}
function markSessionProblemsConfirmed(rawSession, targetIds) {
  const snapshot = cloneSession(rawSession);
  if (!snapshot || !Array.isArray(snapshot.problems)) return snapshot;
  const confirmedIds = new Set([...targetIds].filter(Boolean));
  snapshot.problems = snapshot.problems.map(problem => confirmedIds.has(problem?.id) ? confirmedProblemState(problem) : problem);
  if (Array.isArray(snapshot.pages)) {
    const byId = new Map(snapshot.problems.map(problem => [problem.id, problem]));
    snapshot.pages = snapshot.pages.map(page => {
      const problemIds = page.problemIds || page.problem_ids || [];
      const pageProblems = problemIds.map(id => byId.get(id)).filter(Boolean);
      const pageIsConfirmed = pageProblems.length > 0 && pageProblems.every(problem => deriveProblemStatus(problem) === 'normal');
      if (!pageIsConfirmed) return page;
      return {
        ...page,
        riskFlags: [],
        risk_flags: [],
        reviewStatus: 'normal',
        review_status: 'normal'
      };
    });
  }
  return snapshot;
}
function materializeSessionForItems(rawSession, items, fileName) {
  const snapshot = cloneSession(rawSession);
  if (!snapshot || !Array.isArray(snapshot.problems)) return null;
  const byId = new Map(snapshot.problems.map(problem => [problem.id, problem]));
  const reflowedItems = reflowItemsForBoardOrder(items);
  const orderedProblems = reflowedItems.filter(item => byId.has(item.id)).map(item => applyItemStateToProblem(byId.get(item.id), item));
  const activeIds = new Set(orderedProblems.map(problem => problem.id));
  snapshot.problems = orderedProblems;
  applyProblemCounts(snapshot, orderedProblems);
  snapshot.session_name = fileName || snapshot.session_name || '새 세션';
  snapshot.edb_path = null;
  snapshot.edb_file_uri = null;
  snapshot.edbPath = null;
  snapshot.edbFileUri = null;
  if (Array.isArray(snapshot.pages)) {
    const orderIndex = new Map(orderedProblems.map((problem, index) => [problem.id, index]));
    snapshot.pages = snapshot.pages.map(page => ({
      ...page,
      problemIds: (page.problemIds || page.problem_ids || []).filter(id => activeIds.has(id)).sort((a, b) => (orderIndex.get(a) ?? 999999) - (orderIndex.get(b) ?? 999999))
    }));
  }
  return snapshot;
}
function mergeSessions(baseSession, incomingSession, fileName) {
  const base = cloneSession(baseSession);
  const incoming = cloneSession(incomingSession);
  if (!base) return incoming;
  if (!incoming) return base;
  const existingProblemIds = new Set((base.problems || []).map(problem => problem.id).filter(Boolean));
  const existingPageIds = new Set((base.pages || []).map(page => page.id).filter(Boolean));
  const pageIdMap = new Map();
  const incomingPages = (incoming.pages || []).map(page => {
    const oldId = page.id;
    const nextId = makeUniqueId(oldId, existingPageIds);
    pageIdMap.set(oldId, nextId);
    return {
      ...page,
      id: nextId,
      problemIds: [...(page.problemIds || page.problem_ids || [])]
    };
  });
  const problemIdMap = new Map();
  const incomingProblems = (incoming.problems || []).map(problem => {
    const oldId = problem.id;
    const nextId = makeUniqueId(oldId, existingProblemIds);
    problemIdMap.set(oldId, nextId);
    return {
      ...problem,
      id: nextId,
      sourcePageId: pageIdMap.get(problem.sourcePageId) || problem.sourcePageId
    };
  });
  incomingPages.forEach(page => {
    page.problemIds = page.problemIds.map(id => problemIdMap.get(id) || id);
  });
  const mergedProblems = [...(base.problems || []), ...incomingProblems];
  const mergedPages = [...(base.pages || []), ...incomingPages];
  const concatUnique = (...lists) => Array.from(new Set(lists.flat().filter(Boolean)));
  const merged = {
    ...base,
    session_name: fileName || base.session_name || incoming.session_name || '새 세션',
    data_source: 'question_export',
    source_mode: 'batch',
    input_file_count: concatUnique(base.input_files || base.inputFiles || [], incoming.input_files || incoming.inputFiles || []).length,
    input_files: concatUnique(base.input_files || base.inputFiles || [], incoming.input_files || incoming.inputFiles || []),
    source_page_count: mergedPages.length,
    rendered_page_paths: concatUnique(base.rendered_page_paths || [], incoming.rendered_page_paths || []),
    rendered_page_file_uris: concatUnique(base.rendered_page_file_uris || [], incoming.rendered_page_file_uris || []),
    warning_messages: [...(base.warning_messages || base.warningMessages || []), ...(incoming.warning_messages || incoming.warningMessages || [])],
    problems: mergedProblems,
    pages: mergedPages,
    edb_path: null,
    edb_file_uri: null,
    edbPath: null,
    edbFileUri: null
  };
  applyProblemCounts(merged, mergedProblems);
  return merged;
}
const KIND_BY_SUBJECT = {
  math: 'equation',
  science: 'graph',
  korean: 'paragraph',
  english: 'paragraph',
  social: 'paragraph'
};
function mapProblemToItem(problem, idx) {
  const title = (problem.title || '').trim();
  const fallbackName = `문항 ${idx + 1}`;
  const cleanTitle = title.replace(/page-\d+\s+problem\s+\d+\s*$/i, '').trim();
  const name = cleanTitle || `문항 ${idx + 1}`;
  const riskFlags = Array.isArray(problem.riskFlags) ? problem.riskFlags : [];
  const reviewStatus = deriveProblemStatus(problem);
  const statusMeta = reviewStatusMeta(reviewStatus);
  const initialScale = normalizePlacementScaleRatio(problem.placementScaleRatio ?? problem.placement_scale_ratio);
  const step = normalizeProcessingStep(problem.step || problem.processingStep || problem.processing_step);
  const manualCrop = normalizeManualCrop(problem.manualCrop || problem.manual_crop || problem.cropAdjustments || problem);
  return {
    id: problem.id || `p${idx + 1}`,
    name: name === '' ? fallbackName : name,
    source: problem.sourcePageId || problem.subject || '업로드',
    type: 'image',
    kind: KIND_BY_SUBJECT[problem.subject] || 'paragraph',
    step,
    heightFrac: typeof problem.actualHeightPages === 'number' && problem.actualHeightPages > 0 ? problem.actualHeightPages : 0.8,
    imageUrl: problem.imagePath || null,
    chalkUrl: problem.boardRenderPath || null,
    subject: problem.subject || 'unknown',
    riskFlags,
    reviewStatus,
    statusLabel: statusMeta.label,
    statusShortLabel: statusMeta.shortLabel || statusMeta.label,
    statusReason: riskFlags.length ? riskFlags.join(', ') : '',
    retryable: reviewStatus !== 'normal',
    parseConfidence: typeof problem.parseConfidence === 'number' ? problem.parseConfidence : null,
    confidence: problem.confidence || null,
    aiStatus: problem.aiStatus || 'unknown',
    startYPages: typeof problem.startYPages === 'number' ? problem.startYPages : null,
    snappedNextStartYPages: typeof problem.snappedNextStartYPages === 'number' ? problem.snappedNextStartYPages : null,
    overflowAmountPages: typeof problem.overflowAmountPages === 'number' ? problem.overflowAmountPages : 0,
    overflowViolation: Boolean(problem.overflowViolation),
    slotSpanCount: Number.isInteger(problem.slotSpanCount) ? problem.slotSpanCount : null,
    placementXRatio: normalizePlacementXRatio(problem.placementXRatio ?? problem.placement_x_ratio),
    placementYRatio: normalizePlacementYRatio(problem.placementYRatio ?? problem.placement_y_ratio),
    placementScaleRatio: initialScale < 0.95 ? DEFAULT_PLACEMENT_SCALE_RATIO : initialScale,
    manualCrop
  };
}
async function fetchLatestSession() {
  const resp = await fetch('/api/session/latest');
  if (resp.status === 404) return null;
  const json = await expectOkJson(resp, '세션 로드 실패');
  return json.session;
}
async function fetchSessionHistory() {
  const resp = await fetch('/api/session/history');
  const json = await expectOkJson(resp, '작업 이력 로드 실패');
  return Array.isArray(json.history) ? json.history : [];
}
async function postRestoreSessionHistory(id) {
  const resp = await fetch('/api/session/history/restore', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      id
    })
  });
  const json = await expectOkJson(resp, '작업 열기 실패');
  return json;
}
const AI_FALLBACK_OFF = {
  enabled: false,
  mode: 'off'
};
const AI_FALLBACK_ON = {
  enabled: true,
  mode: 'auto',
  provider: 'gemini',
  model: 'gemini-3.1-pro-preview',
  threshold: 0.72,
  maxRegions: 48,
  maxTokens: 4096,
  timeoutMs: 30000,
  saveDebug: false
};
const AI_MODEL_LABELS = {
  'gemini-3.1-pro-preview': 'Gemini 3.1 Pro',
  'gemini-3-pro-preview': 'Gemini 3 Pro',
  'gemini-2.5-pro': 'Gemini 2.5 Pro'
};
const DEFAULT_INPUT_INTENT = 'multi-problem';
const INPUT_INTENT_OPTIONS = [{
  value: 'auto',
  label: '자동 판별',
  description: '사진 내용에 맞춰 문항 경계를 판단',
  exportMode: 'question'
}, {
  value: 'single-problem',
  label: '한 문제',
  description: '한 이미지나 페이지를 한 문항으로 처리',
  exportMode: 'question'
}, {
  value: 'multi-problem',
  label: '여러 문제',
  description: '한 페이지 안의 여러 문항을 분리',
  exportMode: 'question'
}, {
  value: 'page-as-is',
  label: '페이지 그대로',
  description: '문항 분리 없이 페이지 단위로 변환',
  exportMode: 'question'
}];
const INPUT_INTENT_BY_VALUE = Object.freeze(INPUT_INTENT_OPTIONS.reduce((acc, option) => {
  acc[option.value] = option;
  return acc;
}, {}));
function normalizeInputIntent(value) {
  const normalized = String(value || DEFAULT_INPUT_INTENT).trim().toLowerCase().replace(/_/g, '-');
  return INPUT_INTENT_BY_VALUE[normalized] ? normalized : DEFAULT_INPUT_INTENT;
}
const REVIEW_STATUS_META = {
  normal: {
    label: '정상',
    shortLabel: '정상',
    tone: 'normal'
  },
  check_needed: {
    label: '확인 필요',
    shortLabel: '확인',
    tone: 'check'
  },
  failed: {
    label: '인식 실패',
    shortLabel: '실패',
    tone: 'failed'
  }
};
const RISK_FLAG_META = {
  ai_image_missing_source: '이미지 원본 없음',
  ai_image_reconstructed_check_text: 'AI 보정 확인',
  ai_image_reconstruction_failed: 'AI 보정 실패',
  ai_retry_missing_source: '재인식 원본 없음',
  duplicate_problem_number: '중복 번호',
  fallback_grouping: '문항 경계 추정',
  hwp_oversegmentation: 'HWP 과분할',
  hwp_problem_count_mismatch: 'HWP 문항 수 차이',
  large_block_dominance: '큰 블록 우세',
  marker_document_continuation: '지문·자료 분리',
  needs_review: '검토 필요',
  no_problem_markers: '문항 번호 부족',
  ocr_disabled: 'OCR 미사용',
  passage_missing_child_questions: '지문 하위 문항 누락',
  passage_group_source_reuse: '지문 그룹 원본 중복',
  passage_cross_page_merge_check: '긴 지문 병합 확인',
  problem_per_block: '블록 단위 분리',
  sparse_segmentation: '성긴 분할',
  source_problem_bbox_overlap: '원본 영역 겹침'
};
const CLASSIN_PREFLIGHT_ISSUE_LABELS = {
  board_placement_overlap: '판서 배치 겹침',
  duplicate_problem_number: '중복 번호',
  low_ink_problem_image: '이미지 내용 부족',
  missing_problem_image: '문항 이미지 없음',
  passage_missing_child_questions: '지문 하위 문항 누락',
  passage_review_queue_remaining: '긴 지문 검수 남음',
  passage_group_source_reuse: '지문 그룹 원본 중복',
  review_flags_remaining: '검수 플래그 남음',
  small_problem_image: '문항 이미지 작음',
  source_problem_bbox_overlap: '원본 영역 겹침',
  unreadable_problem_image: '문항 이미지 흐림'
};
const PASSAGE_REVIEW_REASON_LABELS = {
  cross_page_passage_group: '페이지 넘김 긴 지문',
  hwp_text_fallback_problem: 'HWP 텍스트 fallback',
  marker_document_continuation: '문서 이어짐 표시',
  passage_cross_page_merge_check: '긴 지문 병합 확인',
  passage_fragment: '이어짐 자료',
  passage_group_source_reuse: '지문 그룹 원본 중복',
  passage_missing_child_questions: '지문 하위 문항 누락',
  source_problem_bbox_overlap: '원본 영역 겹침'
};
function classinPreflightIssueLabel(type) {
  const normalized = String(type || '').trim();
  return CLASSIN_PREFLIGHT_ISSUE_LABELS[normalized] || normalized || '기타 주의';
}
function passageReviewReasonLabel(reason) {
  const normalized = String(reason || '').trim();
  return PASSAGE_REVIEW_REASON_LABELS[normalized] || normalized;
}
function classinPreflightIssueLabels(preflight) {
  const issues = Array.isArray(preflight?.issues) ? preflight.issues : [];
  const counts = new Map();
  issues.forEach(issue => {
    const type = String(issue?.type || issue?.issueType || issue?.issue_type || '').trim();
    const key = type || 'unknown';
    counts.set(key, (counts.get(key) || 0) + 1);
  });
  return Array.from(counts.entries()).map(([type, count]) => `${classinPreflightIssueLabel(type)} ${count}`);
}
function normalizePublishPreflightBlock(raw) {
  const helper = globalThis.EDB_PUBLISH_SUMMARY?.normalizePublishPreflightBlock;
  if (typeof helper === 'function') return helper(raw);
  if (!raw || typeof raw !== 'object') return null;
  const errorKind = String(raw.errorKind || raw.error_kind || '').trim();
  const classinPreflight = raw.classinPreflight || raw.classin_preflight || {};
  const preflightStatus = String(raw.classinPreflightStatus || raw.classin_preflight_status || classinPreflight.status || '').trim();
  if (errorKind !== 'publish_preflight_blocked' && preflightStatus !== 'blocked') return null;
  const issueLabels = classinPreflightIssueLabels(classinPreflight);
  const issueSummaryLabel = String(raw.classinPreflightIssueSummaryLabel || raw.classin_preflight_issue_summary_label || issueLabels.join(' · ')).trim();
  const issueCount = Number(raw.classinPreflightIssueCount ?? raw.classin_preflight_issue_count ?? classinPreflight.issueCount ?? classinPreflight.issue_count ?? 0);
  const issues = Array.isArray(classinPreflight.issues) ? classinPreflight.issues : [];
  const blockingIssueTypes = Array.isArray(raw.blockingIssueTypes) ? raw.blockingIssueTypes : Array.isArray(raw.blocking_issue_types) ? raw.blocking_issue_types : [];
  const issueTypes = Array.from(new Set([...issues.map(issue => String(issue?.type || issue?.issueType || issue?.issue_type || '').trim()).filter(Boolean), ...blockingIssueTypes.map(type => String(type || '').trim()).filter(Boolean)]));
  const blockingProblemIds = Array.from(new Set((Array.isArray(raw.blockingProblemIds) ? raw.blockingProblemIds : Array.isArray(raw.blocking_problem_ids) ? raw.blocking_problem_ids : []).map(id => String(id || '').trim()).filter(Boolean)));
  const message = String(raw.error || 'ClassIn 사전점검에서 제작 전 확인 문제가 발견되어 EDB 제작을 중단했습니다.').trim();
  return {
    blocked: true,
    errorKind: errorKind || 'publish_preflight_blocked',
    message,
    classinPreflight,
    issueCount: Number.isFinite(issueCount) ? Math.max(0, issueCount) : 0,
    issueTypes,
    blockingProblemIds,
    blocking_problem_ids: blockingProblemIds,
    issueLabels,
    issueSummaryLabel,
    toastLabel: [message, issueSummaryLabel].filter(Boolean).join(' ')
  };
}
function publishBlockedTarget(blockedPublish) {
  const issueTypes = Array.isArray(blockedPublish?.issueTypes) ? blockedPublish.issueTypes.map(type => String(type || '').trim()).filter(Boolean) : [];
  const issues = Array.isArray(blockedPublish?.classinPreflight?.issues) ? blockedPublish.classinPreflight.issues.filter(issue => issue && typeof issue === 'object') : [];
  const focusIssueTypes = ['source_problem_bbox_overlap', 'duplicate_problem_number', 'passage_group_source_reuse', 'passage_missing_child_questions'];
  const passageReviewIssueTypes = ['passage_review_queue_remaining', 'passage_missing_child_questions'];
  const topLevelProblemIds = Array.isArray(blockedPublish?.blockingProblemIds) ? blockedPublish.blockingProblemIds : Array.isArray(blockedPublish?.blocking_problem_ids) ? blockedPublish.blocking_problem_ids : [];
  const focusedProblemIds = Array.from(new Set((topLevelProblemIds.length ? topLevelProblemIds : issues.filter(issue => focusIssueTypes.includes(String(issue?.type || issue?.issueType || issue?.issue_type || '').trim())).flatMap(issue => [...(Array.isArray(issue.problemIds || issue.problem_ids) ? issue.problemIds || issue.problem_ids : []), issue.problemId || issue.problem_id, issue.nextProblemId || issue.next_problem_id])).map(id => String(id || '').trim()).filter(Boolean)));
  const onlyBoardPlacement = issueTypes.length > 0 && issueTypes.every(type => type === 'board_placement_overlap');
  if (onlyBoardPlacement) return {
    view: 'board',
    reviewFocus: null
  };
  if (issueTypes.some(type => passageReviewIssueTypes.includes(type))) {
    return {
      view: 'review',
      reviewFocus: {
        filter: 'passage-review',
        ...(focusedProblemIds.length ? {
          problemIds: focusedProblemIds
        } : {}),
        source: 'publish-preflight'
      }
    };
  }
  if (focusedProblemIds.length) {
    return {
      view: 'review',
      reviewFocus: {
        filter: 'all',
        problemIds: focusedProblemIds,
        source: 'publish-preflight'
      }
    };
  }
  return {
    view: 'review',
    reviewFocus: null
  };
}
const NON_ACTIONABLE_RISK_FLAGS = new Set(['marker_document_continuation', 'ocr_disabled']);
const HWP_COUNT_MATCH_DISMISSIBLE_RISK_FLAGS = new Set(['fallback_grouping', 'large_block_dominance', 'no_problem_markers', 'problem_per_block', 'sparse_segmentation']);
function normalizeReviewStatus(value) {
  const normalized = String(value || '').trim().toLowerCase().replace(/-/g, '_');
  return REVIEW_STATUS_META[normalized] ? normalized : null;
}
function deriveProblemStatus(problem) {
  const explicit = normalizeReviewStatus(problem?.reviewStatus || problem?.review_status);
  if (explicit) return explicit;
  const bbox = problem?.bbox || {};
  const hasBox = Number(bbox.width || 0) > 0 && Number(bbox.height || 0) > 0;
  if (!hasBox || problem?.parseFailed || problem?.parse_failed) return 'failed';
  const flags = problem?.riskFlags || problem?.risk_flags || [];
  return Array.isArray(flags) && flags.length > 0 ? 'check_needed' : 'normal';
}
function reviewStatusMeta(status) {
  return REVIEW_STATUS_META[status] || REVIEW_STATUS_META.normal;
}
function reviewStatusClass(status) {
  return `status-${String(status || 'normal').replace(/_/g, '-')}`;
}
function riskFlagLabel(flag) {
  const key = String(flag || '').trim();
  if (!key) return '검토 사유';
  if (RISK_FLAG_META[key]) return RISK_FLAG_META[key];
  return key.split('_').filter(Boolean).map(part => part.length > 8 ? part.slice(0, 8) : part).join(' ');
}
function hangulRuntimeStatusMeta(hangul) {
  const status = String(hangul?.status || '').toLowerCase();
  if (!hangul) return {
    label: '점검 중',
    tone: 'var(--muted)'
  };
  if (status === 'ready') return {
    label: hangul.label || '준비됨',
    tone: 'var(--ok)'
  };
  if (status === 'partial') return {
    label: hangul.label || '부분 준비',
    tone: '#aa6516'
  };
  return {
    label: hangul.label || '확인 필요',
    tone: 'var(--danger)'
  };
}
function hangulRuntimeSummary(hangul) {
  if (!hangul) return 'HWP/HWPX 변환 환경 확인 중';
  if (hangul.summary) return String(hangul.summary);
  const pdfCount = (hangul.pdfConverters || []).length;
  const textCount = (hangul.textExtractors || []).length;
  const bridgeCount = (hangul.hwpToHwpxConverters || []).length;
  const rendererCount = (hangul.hwpRenderers || []).length;
  const parts = [`PDF ${pdfCount}`, `텍스트 ${textCount}`, `브리지 ${bridgeCount}`];
  if (rendererCount) parts.push(`렌더 ${rendererCount}`);
  if (hangul.htmlPdfFallbackReady) parts.push('HTML fallback');
  if (Array.isArray(hangul.warnings) && hangul.warnings.length) parts.push(`주의 ${hangul.warnings.length}`);
  return parts.join(' · ');
}
function hangulRuntimeToolRows(hangul) {
  if (!hangul) return [];
  return [['PDF 변환', hangul.pdfConverters || []], ['HWP 렌더', hangul.hwpRenderers || []], ['HWP→HWPX', hangul.hwpToHwpxConverters || []], ['텍스트', hangul.textExtractors || []], ['HTML', hangul.htmlConverters || []], ['Chrome PDF', hangul.chromePdfConverters || []]].map(([label, tools]) => ({
    label,
    count: Array.isArray(tools) ? tools.length : 0,
    names: Array.isArray(tools) ? tools.map(tool => tool?.name).filter(Boolean).join(', ') : ''
  }));
}
function listUnique(values) {
  return Array.from(new Set(values));
}
function riskFlagsFor(entity) {
  const flags = entity?.riskFlags || entity?.risk_flags || [];
  if (!Array.isArray(flags)) return [];
  return flags.map(flag => String(flag || '').trim()).filter(Boolean);
}
function hasRiskFlag(entity, flag) {
  const key = String(flag || '').trim();
  return key ? riskFlagsFor(entity).includes(key) : false;
}
function isSupplementalProblem(problem) {
  const helper = globalThis.EDB_REVIEW_FILTERS?.isSupplementalProblem;
  if (typeof helper === 'function') return helper(problem);
  if (hasRiskFlag(problem, 'marker_document_continuation')) return true;
  if (problem?.metadata?.marker_document_continuation) return true;
  const id = String(problem?.id || problem?.problem_id || '');
  return id.endsWith('-continuation');
}
function passageGroupIdFor(problem) {
  const helper = globalThis.EDB_REVIEW_FILTERS?.passageGroupIdFor;
  if (typeof helper === 'function') return helper(problem);
  return String(problem?.passageGroupId || problem?.passage_group_id || problem?.metadata?.passageGroupId || problem?.metadata?.passage_group_id || '').trim();
}
function isPassageProblem(problem) {
  const helper = globalThis.EDB_REVIEW_FILTERS?.isPassageProblem;
  if (typeof helper === 'function') return helper(problem);
  return Boolean(passageGroupIdFor(problem));
}
function problemMatchesReviewFilter(problem, filter, options = {}) {
  const helper = globalThis.EDB_REVIEW_FILTERS?.problemMatchesReviewFilter;
  if (typeof helper === 'function') return helper(problem, filter, options);
  const normalizedFilter = String(filter || 'all').trim() || 'all';
  if (normalizedFilter === 'all') return true;
  if (normalizedFilter === 'supplemental') return isSupplementalProblem(problem);
  if (normalizedFilter === 'passage') return isPassageProblem(problem);
  if (normalizedFilter === 'passage-review') {
    const rawIds = options?.passageReviewProblemIds || options?.passage_review_problem_ids || [];
    const idSet = rawIds instanceof Set ? rawIds : new Set(Array.isArray(rawIds) ? rawIds.map(id => String(id || '').trim()).filter(Boolean) : []);
    const problemId = String(problem?.id || problem?.problem_id || '').trim();
    return Boolean(problemId && idSet.has(problemId));
  }
  return deriveProblemStatus(problem) === normalizedFilter;
}
function countSessionProblems(problems) {
  const list = Array.isArray(problems) ? problems : [];
  const supplemental = list.filter(isSupplementalProblem).length;
  return {
    total: list.length,
    core: Math.max(0, list.length - supplemental),
    supplemental
  };
}
function sessionProblemCounts(session, problemOverride = null) {
  if (Array.isArray(problemOverride)) return countSessionProblems(problemOverride);
  const fallback = countSessionProblems(session?.problems || []);
  const total = Number(session?.detected_problem_count ?? session?.detectedProblemCount);
  const supplemental = Number(session?.supplemental_item_count ?? session?.supplementalItemCount);
  const core = Number(session?.core_problem_count ?? session?.coreProblemCount);
  if (Number.isFinite(total) && Number.isFinite(core) && Number.isFinite(supplemental)) {
    return {
      total: Math.max(0, total),
      core: Math.max(0, core),
      supplemental: Math.max(0, supplemental)
    };
  }
  return fallback;
}
function sessionWarningMessages(session) {
  const warnings = Array.isArray(session?.warning_messages) ? session.warning_messages : Array.isArray(session?.warningMessages) ? session.warningMessages : [];
  return warnings.map(message => String(message || '').trim()).filter(Boolean);
}
function collectReviewStatusCounts(session) {
  const counts = {
    all: 0,
    normal: 0,
    check_needed: 0,
    failed: 0
  };
  (session?.problems || []).forEach(problem => {
    const status = deriveProblemStatus(problem);
    counts.all += 1;
    counts[status] = (counts[status] || 0) + 1;
  });
  return counts;
}
function collectRiskFlagCounts(session) {
  const counts = {};
  const addFlags = flags => {
    if (!Array.isArray(flags)) return;
    flags.forEach(flag => {
      const key = String(flag || '').trim();
      if (key) counts[key] = (counts[key] || 0) + 1;
    });
  };
  (session?.problems || []).forEach(problem => addFlags(riskFlagsFor(problem)));
  (session?.pages || []).forEach(page => addFlags(riskFlagsFor(page)));
  return counts;
}
function normalizeRiskFlagItems(rawItems, fallbackCounts) {
  const sourceItems = Array.isArray(rawItems) ? rawItems : Object.entries(fallbackCounts || {}).map(([flag, count]) => ({
    flag,
    count
  }));
  return sourceItems.map(item => ({
    flag: String(item?.flag || '').trim(),
    count: Number(item?.count || 0)
  })).filter(item => item.flag && Number.isFinite(item.count) && item.count > 0).sort((a, b) => b.count - a.count || a.flag.localeCompare(b.flag)).slice(0, 3);
}
function countRiskFilterMatches(session, flag) {
  const key = String(flag || '').trim();
  if (!key) return 0;
  const problems = Array.isArray(session?.problems) ? session.problems : [];
  const problemMatchCount = problems.filter(problem => hasRiskFlag(problem, key)).length;
  if (problemMatchCount > 0) return problemMatchCount;
  const pageById = new Map((session?.pages || []).map(page => [String(page?.id || ''), page]));
  return problems.filter(problem => hasRiskFlag(pageById.get(String(problem?.sourcePageId || '')), key)).length;
}
function normalizeFilterableRiskFlagItems(session, fallbackCounts) {
  const problems = Array.isArray(session?.problems) ? session.problems : [];
  const total = problems.length;
  const items = Object.keys(fallbackCounts || {}).map(flag => ({
    flag,
    count: countRiskFilterMatches(session, flag),
    diagnosticCount: Number(fallbackCounts[flag] || 0)
  })).filter(item => item.flag && item.count > 0 && (!total || item.count < total)).sort((a, b) => b.count - a.count || b.diagnosticCount - a.diagnosticCount || a.flag.localeCompare(b.flag)).slice(0, 3);
  return items.length ? items : normalizeRiskFlagItems(null, fallbackCounts);
}
function hasHwpCountMatch(summary) {
  return Boolean(summary?.hwpTextProblemCountMatches || summary?.hwpLayoutProblemCountMatches || summary?.hwp_text_problem_count_matches || summary?.hwp_layout_problem_count_matches || summary?.hwpTextProblemCountStatus === 'match' || summary?.hwpLayoutProblemCountStatus === 'match' || summary?.hwp_text_problem_count_status === 'match' || summary?.hwp_layout_problem_count_status === 'match');
}
function filterActionableRiskFlagCounts(counts, options = {}) {
  const dismissed = new Set(NON_ACTIONABLE_RISK_FLAGS);
  if (options.hwpCountsMatch) {
    HWP_COUNT_MATCH_DISMISSIBLE_RISK_FLAGS.forEach(flag => dismissed.add(flag));
  }
  return Object.fromEntries(Object.entries(counts || {}).filter(([flag, count]) => !dismissed.has(flag) && Number(count) > 0));
}
function collectActionableReviewProblemIds(session, actionableRiskFlagCounts) {
  const actionableFlags = new Set(Object.entries(actionableRiskFlagCounts || {}).filter(([, count]) => Number(count) > 0).map(([flag]) => String(flag || '').trim()).filter(Boolean));
  const matched = new Set();
  const problems = Array.isArray(session?.problems) ? session.problems : [];
  problems.forEach((problem, index) => {
    const id = String(problem?.id || problem?.problem_id || `problem-index-${index}`);
    if (deriveProblemStatus(problem) === 'failed') matched.add(id);
    const flags = riskFlagsFor(problem);
    if (flags.some(flag => actionableFlags.has(flag))) matched.add(id);
  });
  (session?.pages || []).forEach(page => {
    const flags = riskFlagsFor(page);
    if (!flags.some(flag => actionableFlags.has(flag))) return;
    const ids = Array.isArray(page?.problemIds || page?.problem_ids) ? (page.problemIds || page.problem_ids).map(id => String(id || '')).filter(Boolean) : [];
    if (ids.length) {
      ids.forEach(id => matched.add(id));
    } else {
      matched.add(`page:${String(page?.id || matched.size)}`);
    }
  });
  return matched;
}
function collectUnresolvedReviewProblemIds(session, actionableRiskFlagCounts) {
  const matched = collectActionableReviewProblemIds(session, actionableRiskFlagCounts);
  const problems = Array.isArray(session?.problems) ? session.problems : [];
  problems.forEach((problem, index) => {
    const id = String(problem?.id || problem?.problem_id || `problem-index-${index}`);
    if (deriveProblemStatus(problem) !== 'normal') matched.add(id);
  });
  (session?.pages || []).forEach(page => {
    const status = normalizeReviewStatus(page?.reviewStatus || page?.review_status);
    if (!status || status === 'normal') return;
    const ids = Array.isArray(page?.problemIds || page?.problem_ids) ? (page.problemIds || page.problem_ids).map(id => String(id || '')).filter(Boolean) : [];
    if (ids.length) {
      ids.forEach(id => matched.add(id));
    } else {
      matched.add(`page:${String(page?.id || matched.size)}`);
    }
  });
  return matched;
}
function countActionableReviewMatches(session, actionableRiskFlagCounts, failedCount = 0) {
  const matched = collectActionableReviewProblemIds(session, actionableRiskFlagCounts);
  return Math.max(matched.size, Math.max(0, Number(failedCount) || 0));
}
function collectPassageGroupSummary(session) {
  const groups = new Map();
  const problems = Array.isArray(session?.problems) ? session.problems : [];
  problems.forEach(problem => {
    const groupId = passageGroupIdFor(problem);
    if (!groupId) return;
    const group = groups.get(groupId) || {
      id: groupId,
      problemCount: 0,
      detectedProblemCount: 0,
      fragmentProblemCount: 0,
      problemNumbers: new Set(),
      sourcePageIds: new Set(),
      range: problem.passageRange || problem.passage_range || null,
      continuesAcrossPages: false,
      continuationBlockIds: new Set()
    };
    group.detectedProblemCount += 1;
    const role = String(problem.passageRole || problem.passage_role || '').trim();
    const problemId = String(problem.id || problem.problem_id || '').trim();
    const riskFlags = Array.isArray(problem.riskFlags || problem.risk_flags) ? problem.riskFlags || problem.risk_flags : [];
    const isFragment = role === 'passage_fragment' || problemId.endsWith('-continuation') || riskFlags.map(flag => String(flag || '').trim()).includes('marker_document_continuation');
    if (isFragment) {
      group.fragmentProblemCount += 1;
    } else {
      group.problemCount += 1;
    }
    const explicitNumbers = problem.passageChildProblemNumbers || problem.passage_child_problem_numbers || [];
    if (Array.isArray(explicitNumbers)) {
      explicitNumbers.forEach(number => {
        const normalized = Number(number);
        if (Number.isFinite(normalized) && normalized > 0) group.problemNumbers.add(normalized);
      });
    }
    const problemNumber = Number(problem.problemNumber ?? problem.problem_number);
    if (!isFragment && Number.isFinite(problemNumber) && problemNumber > 0) {
      group.problemNumbers.add(problemNumber);
    }
    const rawPageIds = problem.passageSourcePageIds || problem.passage_source_page_ids || [];
    if (Array.isArray(rawPageIds)) {
      rawPageIds.forEach(pageId => {
        const normalized = String(pageId || '').trim();
        if (normalized) group.sourcePageIds.add(normalized);
      });
    }
    const sourcePageId = String(problem.sourcePageId || problem.source_page_id || '').trim();
    if (sourcePageId) group.sourcePageIds.add(sourcePageId);
    group.continuesAcrossPages = group.continuesAcrossPages || Boolean(problem.passageContinuesAcrossPages || problem.passage_continues_across_pages);
    const rawContinuationBlockIds = problem.passagePreQuestionContinuationBlockIds || problem.passage_pre_question_continuation_block_ids || [];
    if (Array.isArray(rawContinuationBlockIds)) {
      rawContinuationBlockIds.forEach(blockId => {
        const normalized = String(blockId || '').trim();
        if (normalized) group.continuationBlockIds.add(normalized);
      });
    }
    groups.set(groupId, group);
  });
  const items = Array.from(groups.values()).map(group => {
    const problemNumbers = Array.from(group.problemNumbers).sort((a, b) => a - b);
    const problemCount = problemNumbers.length || group.problemCount;
    return {
      id: group.id,
      problemCount,
      detectedProblemCount: group.detectedProblemCount,
      fragmentProblemCount: group.fragmentProblemCount,
      problemNumbers,
      sourcePageIds: Array.from(group.sourcePageIds),
      sourcePageCount: group.sourcePageIds.size,
      range: group.range,
      continuesAcrossPages: group.continuesAcrossPages || group.sourcePageIds.size > 1,
      continuationBlockCount: group.continuationBlockIds.size
    };
  });
  return {
    passageGroups: items,
    passageGroupCount: items.length,
    passageProblemCount: items.reduce((total, group) => total + group.problemCount, 0),
    crossPagePassageGroupCount: items.filter(group => group.continuesAcrossPages).length,
    passageContinuationBlockCount: items.reduce((total, group) => total + group.continuationBlockCount, 0)
  };
}
function passageReviewItemProblemIds(item) {
  return [...(Array.isArray(item?.problemIds || item?.problem_ids) ? item.problemIds || item.problem_ids : []), ...(Array.isArray(item?.fragmentProblemIds || item?.fragment_problem_ids) ? item.fragmentProblemIds || item.fragment_problem_ids : [])].map(id => String(id || '').trim()).filter(Boolean);
}
function passageReviewItemReasonCodes(item) {
  const rawCodes = Array.isArray(item?.reviewReasonCodes) ? item.reviewReasonCodes : Array.isArray(item?.review_reason_codes) ? item.review_reason_codes : [];
  return rawCodes.map(code => String(code || '').trim()).filter(Boolean);
}
function passageReviewReasonSummary(items) {
  const seen = new Set();
  const labels = [];
  items.forEach(item => {
    passageReviewItemReasonCodes(item).forEach(code => {
      if (seen.has(code)) return;
      seen.add(code);
      const label = passageReviewReasonLabel(code);
      if (label) labels.push(label);
    });
  });
  return labels.join(', ');
}
function collectPassageReviewSummary(session, options = {}) {
  const rawItems = Array.isArray(session?.passageReviewItems) ? session.passageReviewItems : Array.isArray(session?.passage_review_items) ? session.passage_review_items : [];
  const passageReviewItems = rawItems.filter(item => item && typeof item === 'object');
  const actionableProblemIds = options.actionableProblemIds instanceof Set ? options.actionableProblemIds : null;
  const unresolvedPassageReviewItems = actionableProblemIds ? passageReviewItems.filter(item => {
    const ids = passageReviewItemProblemIds(item);
    return ids.length === 0 || ids.some(id => actionableProblemIds.has(id));
  }) : passageReviewItems;
  const explicitCount = Number(session?.passageReviewItemCount ?? session?.passage_review_item_count);
  const passageReviewItemCount = actionableProblemIds ? unresolvedPassageReviewItems.length : Number.isFinite(explicitCount) ? Math.max(0, explicitCount) : unresolvedPassageReviewItems.length;
  const explicitCrossPageCount = Number(session?.crossPagePassageReviewItemCount ?? session?.cross_page_passage_review_item_count);
  const crossPagePassageReviewItemCount = actionableProblemIds ? unresolvedPassageReviewItems.filter(item => item.continuesAcrossPages || item.continues_across_pages).length : Number.isFinite(explicitCrossPageCount) ? Math.max(0, explicitCrossPageCount) : unresolvedPassageReviewItems.filter(item => item.continuesAcrossPages || item.continues_across_pages).length;
  const passageReviewLabel = passageReviewItemCount > 0 ? [`긴 지문 검수 ${passageReviewItemCount}`, crossPagePassageReviewItemCount > 0 ? `페이지 넘김 ${crossPagePassageReviewItemCount}` : ''].filter(Boolean).join(' · ') : '';
  const passageReviewPreview = unresolvedPassageReviewItems.map(item => String(item.numberLabel || item.number_label || item.groupId || item.group_id || '').trim()).filter(Boolean).slice(0, 5).join(', ');
  const passageReviewReasonLabel = passageReviewReasonSummary(unresolvedPassageReviewItems);
  const passageReviewProblemIds = Array.from(new Set(unresolvedPassageReviewItems.flatMap(item => passageReviewItemProblemIds(item))));
  return {
    passageReviewItems: unresolvedPassageReviewItems,
    passageReviewProblemIds,
    passageReviewItemCount,
    crossPagePassageReviewItemCount,
    passageReviewLabel,
    passageReviewPreview,
    passageReviewReasonLabel
  };
}
function nonNegativeNumber(value) {
  const parsed = Number(value);
  return Number.isFinite(parsed) ? Math.max(0, parsed) : 0;
}
function normalizeAiStageSummaries(session) {
  const rawSummary = session?.ai_summary || session?.aiSummary || {};
  const rawStages = Array.isArray(rawSummary?.stages || rawSummary?.aiStages) ? rawSummary.stages || rawSummary.aiStages : [];
  return rawStages.filter(stage => stage && typeof stage === 'object').map(stage => {
    const statusCounts = stage.status_counts && typeof stage.status_counts === 'object' ? stage.status_counts : stage.statusCounts && typeof stage.statusCounts === 'object' ? stage.statusCounts : {};
    return {
      stage: String(stage.stage || '').trim(),
      order: nonNegativeNumber(stage.order || 999),
      label: String(stage.label || stage.stage || 'AI 단계').trim(),
      provider: String(stage.provider || '').trim(),
      pageCount: nonNegativeNumber(stage.page_count ?? stage.pageCount),
      usedPageCount: nonNegativeNumber(stage.used_page_count ?? stage.usedPageCount),
      attemptedPageCount: nonNegativeNumber(stage.attempted_page_count ?? stage.attemptedPageCount),
      appliedPageCount: nonNegativeNumber(stage.applied_page_count ?? stage.appliedPageCount),
      eligibleBlockCount: nonNegativeNumber(stage.eligible_block_count ?? stage.eligibleBlockCount),
      processedBlockCount: nonNegativeNumber(stage.processed_block_count ?? stage.processedBlockCount),
      apiCallBlockCount: nonNegativeNumber(stage.api_call_block_count ?? stage.apiCallBlockCount),
      cacheHitCount: nonNegativeNumber(stage.cache_hit_count ?? stage.cacheHitCount),
      cacheMissCount: nonNegativeNumber(stage.cache_miss_count ?? stage.cacheMissCount),
      skippedBlockCount: nonNegativeNumber(stage.skipped_block_count ?? stage.skippedBlockCount),
      attemptedBlockCount: nonNegativeNumber(stage.attempted_block_count ?? stage.attemptedBlockCount),
      appliedBlockCount: nonNegativeNumber(stage.applied_block_count ?? stage.appliedBlockCount),
      statusCounts
    };
  }).filter(stage => stage.stage).sort((a, b) => a.order - b.order || a.stage.localeCompare(b.stage));
}
function aiStageChipText(stage) {
  if (stage.stage === 'ocr') {
    return `${stage.label} · 블록 ${stage.processedBlockCount || stage.eligibleBlockCount}`;
  }
  if (stage.stage === 'ocr_escalation') {
    return `${stage.label} · 보강 ${stage.appliedBlockCount}/${stage.attemptedBlockCount}`;
  }
  if (stage.stage === 'page_repair') {
    return `${stage.label} · 적용 ${stage.appliedPageCount}/${stage.usedPageCount || stage.pageCount}`;
  }
  return `${stage.label} · ${stage.usedPageCount || stage.pageCount}`;
}
function aiStageTooltip(stage) {
  const statuses = Object.entries(stage.statusCounts || {}).filter(([, count]) => Number(count) > 0).map(([status, count]) => `${status} ${count}`).join(', ');
  return [stage.label, stage.provider ? `provider: ${stage.provider}` : '', `pages: ${stage.usedPageCount}/${stage.pageCount}`, stage.eligibleBlockCount ? `eligible blocks: ${stage.eligibleBlockCount}` : '', stage.processedBlockCount ? `processed blocks: ${stage.processedBlockCount}` : '', stage.apiCallBlockCount ? `API calls: ${stage.apiCallBlockCount}` : '', stage.cacheHitCount ? `cache hits: ${stage.cacheHitCount}` : '', stage.attemptedBlockCount ? `escalation attempts: ${stage.attemptedBlockCount}` : '', stage.appliedBlockCount ? `applied blocks: ${stage.appliedBlockCount}` : '', stage.attemptedPageCount ? `attempted pages: ${stage.attemptedPageCount}` : '', stage.appliedPageCount ? `applied pages: ${stage.appliedPageCount}` : '', statuses ? `status: ${statuses}` : ''].filter(Boolean).join(' · ');
}
function sessionReviewSummary(session) {
  const raw = session?.review_summary || session?.reviewSummary || {};
  const counts = sessionProblemCounts(session);
  const passageSummary = collectPassageGroupSummary(session);
  const fallbackStatusCounts = collectReviewStatusCounts(session);
  const reviewStatusCounts = {
    all: Number(fallbackStatusCounts.all) || 0,
    normal: Number(fallbackStatusCounts.normal) || 0,
    check_needed: Number(fallbackStatusCounts.check_needed) || 0,
    failed: Number(fallbackStatusCounts.failed) || 0
  };
  const rawSupplementalStatusCounts = raw.supplementalReviewStatusCounts && typeof raw.supplementalReviewStatusCounts === 'object' ? raw.supplementalReviewStatusCounts : {};
  const rawCoreStatusCounts = raw.coreReviewStatusCounts && typeof raw.coreReviewStatusCounts === 'object' ? raw.coreReviewStatusCounts : {};
  const fallbackRiskFlagCounts = collectRiskFlagCounts(session);
  const riskFlagCounts = fallbackRiskFlagCounts;
  const actionableRiskFlagCounts = filterActionableRiskFlagCounts(riskFlagCounts, {
    hwpCountsMatch: hasHwpCountMatch(raw)
  });
  const actionableReviewProblemIds = collectActionableReviewProblemIds(session, actionableRiskFlagCounts);
  const unresolvedReviewProblemIds = collectUnresolvedReviewProblemIds(session, actionableRiskFlagCounts);
  const actionableNeedsReviewCount = countActionableReviewMatches(session, actionableRiskFlagCounts, reviewStatusCounts.failed);
  const passageReviewSummary = collectPassageReviewSummary(session, {
    actionableProblemIds: unresolvedReviewProblemIds
  });
  const topRiskFlags = normalizeFilterableRiskFlagItems(session, actionableRiskFlagCounts);
  const warningMessages = Array.isArray(raw.warningMessages) ? raw.warningMessages.map(message => String(message || '').trim()).filter(Boolean) : sessionWarningMessages(session);
  const extractorMap = raw.hwpTextExtractors && typeof raw.hwpTextExtractors === 'object' ? raw.hwpTextExtractors : {};
  const layoutExtractorMap = raw.hwpLayoutExtractors && typeof raw.hwpLayoutExtractors === 'object' ? raw.hwpLayoutExtractors : {};
  const extractorNames = Object.entries(extractorMap).filter(([, count]) => Number(count) > 0).sort((a, b) => String(a[0]).localeCompare(String(b[0]))).map(([name, count]) => `${name} ${count}`);
  const layoutExtractorNames = Object.entries(layoutExtractorMap).filter(([, count]) => Number(count) > 0).sort((a, b) => String(a[0]).localeCompare(String(b[0]))).map(([name, count]) => `${name} ${count}`);
  const hwpTextProblemSignalCount = Number(raw.hwpTextProblemSignalCount || 0);
  const hwpTextProblemDelta = Number(raw.hwpTextProblemDelta || 0);
  const hwpTextProblemCountStatus = String(raw.hwpTextProblemCountStatus || 'unknown');
  const hwpLayoutProblemSignalCount = Number(raw.hwpLayoutProblemSignalCount || 0);
  const hwpLayoutProblemDelta = Number(raw.hwpLayoutProblemDelta || 0);
  const hwpLayoutProblemCountStatus = String(raw.hwpLayoutProblemCountStatus || 'unknown');
  const hwpCacheHitPageCount = Number(raw.hwpCacheHitPageCount ?? raw.hwp_cache_hit_page_count ?? 0);
  const hwpRendererCacheHitCount = Number(raw.hwpRendererCacheHitCount ?? raw.hwp_renderer_cache_hit_count ?? 0);
  const hwpNormalizedCacheHitCount = Number(raw.hwpNormalizedCacheHitCount ?? raw.hwp_normalized_cache_hit_count ?? 0);
  const hwpProblemCountMismatchCount = Number(raw.hwpProblemCountMismatchCount ?? raw.hwp_problem_count_mismatch_count ?? riskFlagCounts.hwp_problem_count_mismatch ?? 0);
  const hwpOversegmentationCount = Number(raw.hwpOversegmentationCount ?? raw.hwp_oversegmentation_count ?? riskFlagCounts.hwp_oversegmentation ?? 0);
  const aiStages = normalizeAiStageSummaries(session);
  const duplicateProblemNumberGroups = Array.isArray(session?.duplicateProblemNumberGroups) ? session.duplicateProblemNumberGroups : Array.isArray(session?.duplicate_problem_number_groups) ? session.duplicate_problem_number_groups : [];
  const blockingDuplicateProblemNumberGroups = Array.isArray(session?.blockingDuplicateProblemNumberGroups) ? session.blockingDuplicateProblemNumberGroups : Array.isArray(session?.blocking_duplicate_problem_number_groups) ? session.blocking_duplicate_problem_number_groups : duplicateProblemNumberGroups.filter(group => group?.blocking !== false);
  const duplicateProblemNumberLabel = duplicateProblemNumberGroups.map(group => {
    const label = String(group?.numberLabel || group?.number_label || '').trim();
    const occurrences = Number(group?.occurrencesPerNumber ?? group?.occurrences_per_number ?? 0);
    return label && Number.isFinite(occurrences) && occurrences > 1 ? `${label} x${occurrences}` : '';
  }).filter(Boolean).join(', ');
  const sourceProblemOverlapGroups = Array.isArray(session?.sourceProblemOverlapGroups) ? session.sourceProblemOverlapGroups : Array.isArray(session?.source_problem_overlap_groups) ? session.source_problem_overlap_groups : [];
  const sourceProblemOverlapLabel = sourceProblemOverlapGroups.map(group => {
    const pageId = String(group?.sourcePageId || group?.source_page_id || '').trim();
    const ratio = Number(group?.overlapAreaRatio ?? group?.overlap_area_ratio ?? 0);
    const percent = Number.isFinite(ratio) && ratio > 0 ? `${Math.round(ratio * 100)}%` : '';
    return [pageId, percent].filter(Boolean).join(' ');
  }).filter(Boolean).join(', ') || `${sourceProblemOverlapGroups.length}`;
  const passageGroupSourceReuseGroups = Array.isArray(session?.passageGroupSourceReuseGroups) ? session.passageGroupSourceReuseGroups : Array.isArray(session?.passage_group_source_reuse_groups) ? session.passage_group_source_reuse_groups : [];
  const explicitPassageGroupSourceReuseCount = Number(session?.passageGroupSourceReuseGroupCount ?? session?.passage_group_source_reuse_group_count ?? passageGroupSourceReuseGroups.length);
  const passageGroupSourceReuseGroupCount = Number.isFinite(explicitPassageGroupSourceReuseCount) ? Math.max(0, explicitPassageGroupSourceReuseCount) : passageGroupSourceReuseGroups.length;
  const passageGroupSourceReuseLabel = passageGroupSourceReuseGroups.map(group => {
    const groupId = String(group?.passageGroupId || group?.passage_group_id || '').trim();
    const ratio = Number(group?.overlapAreaRatio ?? group?.overlap_area_ratio ?? 0);
    const percent = Number.isFinite(ratio) && ratio > 0 ? `${Math.round(ratio * 100)}%` : '';
    return [groupId, percent].filter(Boolean).join(' ');
  }).filter(Boolean).join(', ') || `${passageGroupSourceReuseGroupCount}`;
  return {
    counts,
    reviewStatusCounts,
    coreReviewStatusCounts: rawCoreStatusCounts,
    supplementalReviewStatusCounts: rawSupplementalStatusCounts,
    needsReviewCount: Math.max(0, reviewStatusCounts.check_needed + reviewStatusCounts.failed),
    actionableNeedsReviewCount,
    riskFlagCounts,
    actionableRiskFlagCounts,
    topRiskFlags,
    warningCount: Number.isFinite(Number(raw.warningCount)) ? Number(raw.warningCount) : warningMessages.length,
    warningPreview: warningMessages[0] || '',
    hwpTextExtractorLabel: extractorNames.join(', '),
    hwpTextProblemSignalCount: Number.isFinite(hwpTextProblemSignalCount) ? Math.max(0, hwpTextProblemSignalCount) : 0,
    hwpTextProblemCountStatus,
    hwpTextProblemCountMessage: String(raw.hwpTextProblemCountMessage || ''),
    hwpTextProblemDelta: Number.isFinite(hwpTextProblemDelta) ? hwpTextProblemDelta : 0,
    hwpLayoutExtractorLabel: layoutExtractorNames.join(', '),
    hwpLayoutProblemSignalCount: Number.isFinite(hwpLayoutProblemSignalCount) ? Math.max(0, hwpLayoutProblemSignalCount) : 0,
    hwpLayoutProblemCountStatus,
    hwpLayoutProblemCountMessage: String(raw.hwpLayoutProblemCountMessage || ''),
    hwpLayoutProblemDelta: Number.isFinite(hwpLayoutProblemDelta) ? hwpLayoutProblemDelta : 0,
    hwpCacheHitPageCount: Number.isFinite(hwpCacheHitPageCount) ? Math.max(0, hwpCacheHitPageCount) : 0,
    hwpRendererCacheHitCount: Number.isFinite(hwpRendererCacheHitCount) ? Math.max(0, hwpRendererCacheHitCount) : 0,
    hwpNormalizedCacheHitCount: Number.isFinite(hwpNormalizedCacheHitCount) ? Math.max(0, hwpNormalizedCacheHitCount) : 0,
    aiStages,
    hwpProblemCountMismatchCount: Number.isFinite(hwpProblemCountMismatchCount) ? Math.max(0, hwpProblemCountMismatchCount) : 0,
    hwpOversegmentationCount: Number.isFinite(hwpOversegmentationCount) ? Math.max(0, hwpOversegmentationCount) : 0,
    duplicateProblemNumberGroups,
    blockingDuplicateProblemNumberGroups,
    duplicateProblemNumberLabel,
    sourceProblemOverlapGroups,
    sourceProblemOverlapLabel,
    passageGroupSourceReuseGroups,
    passageGroupSourceReuseGroupCount,
    passageGroupSourceReuseLabel,
    passageGroups: passageSummary.passageGroups,
    passageGroupCount: passageSummary.passageGroupCount,
    passageProblemCount: passageSummary.passageProblemCount,
    crossPagePassageGroupCount: passageSummary.crossPagePassageGroupCount,
    passageContinuationBlockCount: passageSummary.passageContinuationBlockCount,
    passageReviewItems: passageReviewSummary.passageReviewItems,
    passageReviewProblemIds: passageReviewSummary.passageReviewProblemIds,
    passageReviewItemCount: passageReviewSummary.passageReviewItemCount,
    crossPagePassageReviewItemCount: passageReviewSummary.crossPagePassageReviewItemCount,
    passageReviewLabel: passageReviewSummary.passageReviewLabel,
    passageReviewPreview: passageReviewSummary.passageReviewPreview,
    passageReviewReasonLabel: passageReviewSummary.passageReviewReasonLabel
  };
}
function publishReviewWarningMessage(session, publishReviewSummary) {
  const actionableNeedsReviewCount = Math.max(0, Number(publishReviewSummary?.actionableNeedsReviewCount) || 0);
  const passageReviewItemCount = Math.max(0, Number(publishReviewSummary?.passageReviewItemCount) || 0);
  const passageReviewProblemIds = new Set(Array.isArray(publishReviewSummary?.passageReviewProblemIds) ? publishReviewSummary.passageReviewProblemIds.map(id => String(id || '').trim()).filter(Boolean) : []);
  const unresolvedReviewProblemIds = collectUnresolvedReviewProblemIds(session, publishReviewSummary?.actionableRiskFlagCounts);
  const hasUnresolvedPassageReview = passageReviewItemCount > 0 && (passageReviewProblemIds.size === 0 || Array.from(passageReviewProblemIds).some(id => unresolvedReviewProblemIds.has(id)));
  if (actionableNeedsReviewCount <= 0 && !hasUnresolvedPassageReview) return null;
  const passageReviewLine = hasUnresolvedPassageReview ? [publishReviewSummary?.passageReviewLabel || `긴 지문 검수 ${passageReviewItemCount}`, publishReviewSummary?.passageReviewReasonLabel || '', publishReviewSummary?.passageReviewPreview ? `대상 ${publishReviewSummary.passageReviewPreview}` : ''].filter(Boolean).join(' · ') : '';
  const reviewCountLine = actionableNeedsReviewCount > 0 ? `검수 화면에 확인 필요 ${actionableNeedsReviewCount}개가 남아 있습니다.` : '검수 화면에 긴 지문 확인 항목이 남아 있습니다.';
  return {
    message: [passageReviewLine, reviewCountLine, '그래도 EDB를 제작할까요?'].filter(Boolean).join('\n'),
    cancelToast: passageReviewLine ? '제작을 멈췄어요. 긴 지문 검수 큐를 먼저 확인하세요.' : '제작을 멈췄어요. 검수 화면에서 확인 필요 항목을 먼저 확인하세요.',
    reviewFilter: passageReviewLine ? 'passage-review' : 'check_needed'
  };
}
function normalizePublishSummary(raw, session = null) {
  const helper = globalThis.EDB_PUBLISH_SUMMARY?.normalizePublishSummary;
  if (typeof helper === 'function') return helper(raw, session);
  if (!raw || typeof raw !== 'object') return null;
  const recordCount = Number(raw.recordCount ?? raw.record_count ?? raw.recordCountActual ?? raw.record_count_actual ?? 0);
  const recordCountActual = Number(raw.recordCountActual ?? raw.record_count_actual ?? recordCount);
  const coreProblemCount = Number(raw.coreProblemCount ?? raw.core_problem_count ?? 0);
  const supplementalItemCount = Number(raw.supplementalItemCount ?? raw.supplemental_item_count ?? 0);
  const pageCountHint = Number(raw.pageCountHint ?? raw.page_count_hint ?? 0);
  const outerSize = Number(raw.outerSize ?? raw.outer_size ?? 0);
  const edbFileName = String(raw.edbFileName || raw.edb_file_name || '').trim();
  const edbPath = String(raw.edbPath || raw.edb_path || '').trim();
  const outputDir = String(raw.outputDir || raw.output_dir || session?.output_dir || session?.outputDir || '').trim();
  const edbFileUri = String(raw.edbFileUri || raw.edb_file_uri || session?.edb_file_uri || session?.edbFileUri || '').trim();
  const classinReview = raw.classinReview || raw.classin_review || session?.classinReview || session?.classin_review || {};
  const classinReviewStatus = String(raw.classinReviewStatus || raw.classin_review_status || classinReview.status || '').trim();
  const classinReviewStatusLabel = String(raw.classinReviewStatusLabel || raw.classin_review_status_label || classinReview.statusLabel || classinReview.status_label || (classinReviewStatus === 'passed' ? 'ClassIn 확인 완료' : '')).trim();
  const classinHandoffUri = String(raw.classinHandoffUri || raw.classin_handoff_uri || session?.classin_handoff_uri || session?.classinHandoffUri || '').trim();
  const classinHandoffMarkdownUri = String(raw.classinHandoffMarkdownUri || raw.classin_handoff_markdown_uri || session?.classin_handoff_markdown_uri || session?.classinHandoffMarkdownUri || '').trim();
  const classinHandoffStatus = String(raw.classinHandoffStatus || raw.classin_handoff_status || session?.classin_handoff_status || session?.classinHandoffStatus || '').trim();
  const rawReadyForClassIn = raw.readyForClassIn ?? raw.ready_for_classin ?? session?.readyForClassIn ?? session?.ready_for_classin;
  const readyForClassIn = rawReadyForClassIn === undefined ? classinHandoffStatus === 'ready_for_classin_review' : rawReadyForClassIn !== false;
  const classinHandoffStatusLabel = String(raw.classinHandoffStatusLabel || raw.classin_handoff_status_label || (classinHandoffStatus ? readyForClassIn ? 'ClassIn 전달 준비' : 'ClassIn 전달 주의' : '')).trim();
  const classinPreflight = raw.classinPreflight || raw.classin_preflight || session?.classinPreflight || session?.classin_preflight || {};
  const classinPreflightStatus = String(raw.classinPreflightStatus || raw.classin_preflight_status || classinPreflight.status || '').trim();
  const classinPreflightIssueCount = Number(raw.classinPreflightIssueCount ?? raw.classin_preflight_issue_count ?? classinPreflight.issueCount ?? classinPreflight.issue_count ?? 0);
  const rawClassinPreflightPassed = raw.classinPreflightPassed ?? raw.classin_preflight_passed ?? classinPreflight.passed;
  const classinPreflightPassed = rawClassinPreflightPassed === undefined ? classinPreflightStatus === 'passed' : rawClassinPreflightPassed !== false;
  const classinPreflightStatusLabel = String(raw.classinPreflightStatusLabel || raw.classin_preflight_status_label || (classinPreflightStatus ? classinPreflightPassed ? 'ClassIn 사전점검 OK' : `ClassIn 사전점검 주의 ${Number.isFinite(classinPreflightIssueCount) ? Math.max(0, classinPreflightIssueCount) : 0}` : '')).trim();
  const classinPreflightIssueLabelList = Array.isArray(raw.classinPreflightIssueLabels) ? raw.classinPreflightIssueLabels.map(label => String(label || '').trim()).filter(Boolean) : classinPreflightIssueLabels(classinPreflight);
  const classinPreflightIssueSummaryLabel = String(raw.classinPreflightIssueSummaryLabel || raw.classin_preflight_issue_summary_label || classinPreflightIssueLabelList.join(' · ')).trim();
  const passageGroupsRaw = raw.passageGroups || raw.passage_groups || session?.passageGroups || session?.passage_groups || [];
  const passageGroups = Array.isArray(passageGroupsRaw) ? passageGroupsRaw.filter(group => group && typeof group === 'object') : [];
  const passageGroupCount = Number(raw.passageGroupCount ?? raw.passage_group_count ?? session?.passageGroupCount ?? session?.passage_group_count ?? passageGroups.length);
  const passageProblemCountFallback = passageGroups.reduce((total, group) => {
    const count = Number(group.problemCount ?? group.problem_count ?? 0);
    return total + (Number.isFinite(count) ? Math.max(0, count) : 0);
  }, 0);
  const passageProblemCount = Number(raw.passageProblemCount ?? raw.passage_problem_count ?? session?.passageProblemCount ?? session?.passage_problem_count ?? passageProblemCountFallback);
  const crossPagePassageGroupCountFallback = passageGroups.filter(group => group.continuesAcrossPages || group.continues_across_pages).length;
  const crossPagePassageGroupCount = Number(raw.crossPagePassageGroupCount ?? raw.cross_page_passage_group_count ?? session?.crossPagePassageGroupCount ?? session?.cross_page_passage_group_count ?? crossPagePassageGroupCountFallback);
  const normalizedPassageGroupCount = Number.isFinite(passageGroupCount) ? Math.max(0, passageGroupCount) : 0;
  const normalizedPassageProblemCount = Number.isFinite(passageProblemCount) ? Math.max(0, passageProblemCount) : 0;
  const normalizedCrossPagePassageGroupCount = Number.isFinite(crossPagePassageGroupCount) ? Math.max(0, crossPagePassageGroupCount) : 0;
  const passageGroupLabel = String(raw.passageGroupLabel || raw.passage_group_label || (normalizedPassageGroupCount > 0 ? [`긴 지문 그룹 ${normalizedPassageGroupCount}`, `${normalizedPassageProblemCount}문항`, normalizedCrossPagePassageGroupCount > 0 ? `페이지 넘김 ${normalizedCrossPagePassageGroupCount}` : ''].filter(Boolean).join(' · ') : '')).trim();
  const passageReviewItemsRaw = raw.passageReviewItems || raw.passage_review_items || session?.passageReviewItems || session?.passage_review_items || [];
  const passageReviewItems = Array.isArray(passageReviewItemsRaw) ? passageReviewItemsRaw.filter(item => item && typeof item === 'object') : [];
  const passageReviewItemCount = Number(raw.passageReviewItemCount ?? raw.passage_review_item_count ?? session?.passageReviewItemCount ?? session?.passage_review_item_count ?? passageReviewItems.length);
  const crossPagePassageReviewItemCountFallback = passageReviewItems.filter(item => item.continuesAcrossPages || item.continues_across_pages).length;
  const crossPagePassageReviewItemCount = Number(raw.crossPagePassageReviewItemCount ?? raw.cross_page_passage_review_item_count ?? session?.crossPagePassageReviewItemCount ?? session?.cross_page_passage_review_item_count ?? crossPagePassageReviewItemCountFallback);
  const normalizedPassageReviewItemCount = Number.isFinite(passageReviewItemCount) ? Math.max(0, passageReviewItemCount) : 0;
  const normalizedCrossPagePassageReviewItemCount = Number.isFinite(crossPagePassageReviewItemCount) ? Math.max(0, crossPagePassageReviewItemCount) : 0;
  const passageReviewLabel = String(raw.passageReviewLabel || raw.passage_review_label || (normalizedPassageReviewItemCount > 0 ? [`긴 지문 검수 ${normalizedPassageReviewItemCount}`, normalizedCrossPagePassageReviewItemCount > 0 ? `페이지 넘김 ${normalizedCrossPagePassageReviewItemCount}` : ''].filter(Boolean).join(' · ') : '')).trim();
  const passageReviewReasonLabel = String(raw.passageReviewReasonLabel || raw.passage_review_reason_label || passageReviewReasonSummary(passageReviewItems)).trim();
  const sourceProblemOverlapGroupsRaw = raw.sourceProblemOverlapGroups || raw.source_problem_overlap_groups || session?.sourceProblemOverlapGroups || session?.source_problem_overlap_groups || [];
  const sourceProblemOverlapGroups = Array.isArray(sourceProblemOverlapGroupsRaw) ? sourceProblemOverlapGroupsRaw.filter(group => group && typeof group === 'object') : [];
  const sourceProblemOverlapGroupCount = Number(raw.sourceProblemOverlapGroupCount ?? raw.source_problem_overlap_group_count ?? session?.sourceProblemOverlapGroupCount ?? session?.source_problem_overlap_group_count ?? sourceProblemOverlapGroups.length);
  const normalizedSourceProblemOverlapGroupCount = Number.isFinite(sourceProblemOverlapGroupCount) ? Math.max(0, sourceProblemOverlapGroupCount) : 0;
  const sourceProblemOverlapDetails = sourceProblemOverlapGroups.map(group => {
    const pageId = String(group?.sourcePageId || group?.source_page_id || '').trim();
    const ratio = Number(group?.overlapAreaRatio ?? group?.overlap_area_ratio ?? 0);
    const percent = Number.isFinite(ratio) && ratio > 0 ? `${Math.round(ratio * 100)}%` : '';
    return [pageId, percent].filter(Boolean).join(' ');
  }).filter(Boolean).join(', ');
  const sourceProblemOverlapLabel = String(raw.sourceProblemOverlapLabel || raw.source_problem_overlap_label || (normalizedSourceProblemOverlapGroupCount > 0 ? [`원본 겹침 ${normalizedSourceProblemOverlapGroupCount}`, sourceProblemOverlapDetails].filter(Boolean).join(' · ') : '')).trim();
  const passageGroupSourceReuseGroupsRaw = raw.passageGroupSourceReuseGroups || raw.passage_group_source_reuse_groups || session?.passageGroupSourceReuseGroups || session?.passage_group_source_reuse_groups || [];
  const passageGroupSourceReuseGroups = Array.isArray(passageGroupSourceReuseGroupsRaw) ? passageGroupSourceReuseGroupsRaw.filter(group => group && typeof group === 'object') : [];
  const passageGroupSourceReuseGroupCount = Number(raw.passageGroupSourceReuseGroupCount ?? raw.passage_group_source_reuse_group_count ?? session?.passageGroupSourceReuseGroupCount ?? session?.passage_group_source_reuse_group_count ?? passageGroupSourceReuseGroups.length);
  const normalizedPassageGroupSourceReuseGroupCount = Number.isFinite(passageGroupSourceReuseGroupCount) ? Math.max(0, passageGroupSourceReuseGroupCount) : 0;
  const passageGroupSourceReuseDetails = passageGroupSourceReuseGroups.map(group => {
    const groupId = String(group?.passageGroupId || group?.passage_group_id || '').trim();
    const ratio = Number(group?.overlapAreaRatio ?? group?.overlap_area_ratio ?? 0);
    const percent = Number.isFinite(ratio) && ratio > 0 ? `${Math.round(ratio * 100)}%` : '';
    return [groupId, percent].filter(Boolean).join(' ');
  }).filter(Boolean).join(', ');
  const passageGroupSourceReuseLabel = String(raw.passageGroupSourceReuseLabel || raw.passage_group_source_reuse_label || (normalizedPassageGroupSourceReuseGroupCount > 0 ? [`지문 원본 중복 ${normalizedPassageGroupSourceReuseGroupCount}`, passageGroupSourceReuseDetails].filter(Boolean).join(' · ') : '')).trim();
  const edbFileExists = raw.edbFileExists ?? raw.edb_file_exists;
  const outputDirExists = raw.outputDirExists ?? raw.output_dir_exists;
  if (!edbFileName && !edbPath && !edbFileUri) return null;
  const normalizedCore = Number.isFinite(coreProblemCount) ? Math.max(0, coreProblemCount) : 0;
  const normalizedSupplemental = Number.isFinite(supplementalItemCount) ? Math.max(0, supplementalItemCount) : 0;
  const fallbackRecordCount = Number.isFinite(recordCount) ? Math.max(0, recordCount) : 0;
  const explicitRecordCountLabel = String(raw.recordCountLabel || raw.record_count_label || '').trim();
  const summary = {
    validated: raw.validated !== false,
    statusLabel: String(raw.statusLabel || raw.status_label || '제작 완료'),
    edbFileName: edbFileName || (edbPath ? edbPath.split('/').pop() : 'classin.edb'),
    edbPath,
    edbFileUri,
    outputDir,
    classinReview,
    classinReviewStatus,
    classinReviewStatusLabel,
    classinReviewPassed: (raw.classinReviewPassed ?? raw.classin_review_passed) === undefined ? classinReviewStatus === 'passed' : (raw.classinReviewPassed ?? raw.classin_review_passed) !== false,
    classinHandoffUri,
    classinHandoffMarkdownUri,
    classinHandoffStatus,
    classinHandoffStatusLabel,
    readyForClassIn,
    classinPreflight,
    classinPreflightStatus,
    classinPreflightStatusLabel,
    classinPreflightPassed,
    classinPreflightIssueCount: Number.isFinite(classinPreflightIssueCount) ? Math.max(0, classinPreflightIssueCount) : 0,
    classinPreflightIssueLabels: classinPreflightIssueLabelList,
    classinPreflightIssueSummaryLabel,
    passageGroups,
    passageGroupCount: normalizedPassageGroupCount,
    passageProblemCount: normalizedPassageProblemCount,
    crossPagePassageGroupCount: normalizedCrossPagePassageGroupCount,
    passageGroupLabel,
    passageReviewItems,
    passageReviewItemCount: normalizedPassageReviewItemCount,
    crossPagePassageReviewItemCount: normalizedCrossPagePassageReviewItemCount,
    passageReviewLabel,
    passageReviewReasonLabel,
    sourceProblemOverlapGroups,
    sourceProblemOverlapGroupCount: normalizedSourceProblemOverlapGroupCount,
    sourceProblemOverlapLabel,
    passageGroupSourceReuseGroups,
    passageGroupSourceReuseGroupCount: normalizedPassageGroupSourceReuseGroupCount,
    passageGroupSourceReuseLabel,
    edbFileExists: edbFileExists === undefined ? true : edbFileExists !== false,
    outputDirExists: outputDirExists === undefined ? Boolean(outputDir) : outputDirExists !== false,
    recordCount: fallbackRecordCount,
    recordCountActual: Number.isFinite(recordCountActual) ? Math.max(0, recordCountActual) : 0,
    coreProblemCount: normalizedCore,
    supplementalItemCount: normalizedSupplemental,
    recordCountLabel: explicitRecordCountLabel || (normalizedSupplemental > 0 ? `${normalizedCore}문항 + 자료 ${normalizedSupplemental}` : `${fallbackRecordCount}개 자료`),
    pageCountHint: Number.isFinite(pageCountHint) ? Math.max(0, pageCountHint) : 0,
    outerSize: Number.isFinite(outerSize) ? Math.max(0, outerSize) : 0,
    publishedAt: String(raw.publishedAt || raw.published_at || '').trim()
  };
  summary.canDownload = Boolean(summary.edbFileUri) && summary.edbFileExists !== false;
  summary.canOpenEdbFile = Boolean(summary.edbPath) && summary.edbFileExists !== false;
  summary.canOpenOutputDir = Boolean(summary.outputDir) && summary.outputDirExists !== false;
  summary.canOpenClassinHandoff = Boolean(summary.classinHandoffMarkdownUri || summary.classinHandoffUri);
  summary.canMarkClassinReviewComplete = summary.canOpenEdbFile && !summary.classinReviewPassed;
  return summary;
}
function sessionPublishSummary(session) {
  return normalizePublishSummary(session?.publish_summary || session?.publishSummary, session);
}
function sessionPublishHistory(session) {
  const rawHistory = Array.isArray(session?.publish_history) ? session.publish_history : Array.isArray(session?.publishHistory) ? session.publishHistory : [];
  const history = rawHistory.map(item => normalizePublishSummary(item, session)).filter(Boolean);
  const latest = sessionPublishSummary(session);
  const latestKey = latest ? latest.edbPath || latest.edbFileName : '';
  if (latest && !history.some(item => (item.edbPath || item.edbFileName) === latestKey)) {
    history.unshift(latest);
  }
  return history.slice(0, 5);
}
function formatPublishTime(value) {
  const helper = globalThis.EDB_PUBLISH_SUMMARY?.formatPublishTime;
  if (typeof helper === 'function') return helper(value);
  if (!value) return '';
  const date = new Date(value);
  if (Number.isNaN(date.getTime())) return '';
  return date.toLocaleString('ko-KR', {
    month: '2-digit',
    day: '2-digit',
    hour: '2-digit',
    minute: '2-digit'
  });
}
function formatPublishHistoryMeta(summary) {
  const helper = globalThis.EDB_PUBLISH_SUMMARY?.formatPublishHistoryMeta;
  if (typeof helper === 'function') return helper(summary);
  const label = summary?.recordCountLabel || `${summary?.recordCountActual || summary?.recordCount || 0}개 자료`;
  const time = formatPublishTime(summary?.publishedAt);
  return [time, label].filter(Boolean).join(' · ') || label;
}
function applyProblemCounts(session, problems = null) {
  const counts = sessionProblemCounts({
    problems: problems || session?.problems || []
  }, problems || session?.problems || []);
  session.detected_problem_count = counts.total;
  session.detectedProblemCount = counts.total;
  session.core_problem_count = counts.core;
  session.coreProblemCount = counts.core;
  session.supplemental_item_count = counts.supplemental;
  session.supplementalItemCount = counts.supplemental;
  return counts;
}
function formatProblemCount(counts) {
  const core = Number(counts?.core ?? counts?.problems ?? counts?.total ?? 0);
  const supplemental = Number(counts?.supplemental ?? counts?.supplementalItems ?? 0);
  if (supplemental > 0) return `${core}문항 + 자료 ${supplemental}`;
  return `${core}문항`;
}
function hasReviewPages(session) {
  return Array.isArray(session?.pages) && session.pages.length > 0;
}
function sessionRiskCount(session) {
  const problemRiskCount = (session?.problems || []).filter(p => Array.isArray(p?.riskFlags) && p.riskFlags.length > 0).length;
  const pageRiskCount = (session?.pages || []).filter(page => {
    const flags = page?.riskFlags || page?.risk_flags || [];
    return Array.isArray(flags) && flags.length > 0;
  }).length;
  const warningCount = Array.isArray(session?.warning_messages) ? session.warning_messages.length : Array.isArray(session?.warningMessages) ? session.warningMessages.length : 0;
  return problemRiskCount + pageRiskCount + warningCount;
}
function shouldOpenReview(session) {
  if (!hasReviewPages(session)) return false;
  const problemCount = Array.isArray(session?.problems) ? session.problems.length : 0;
  return problemCount > 1 || sessionRiskCount(session) > 0;
}
function summarizeRecognitionSession(session, pageIds) {
  const pages = Array.isArray(session?.pages) ? session.pages : [];
  const targetIds = Array.isArray(pageIds) && pageIds.length ? new Set(pageIds) : null;
  const visiblePages = targetIds ? pages.filter(page => targetIds.has(page.id)) : pages;
  const visiblePageIds = new Set(visiblePages.map(page => page.id));
  const problems = (session?.problems || []).filter(problem => {
    if (!targetIds) return true;
    return visiblePageIds.has(problem?.sourcePageId);
  });
  const riskCount = problems.filter(problem => deriveProblemStatus(problem) !== 'normal').length;
  const counts = countSessionProblems(problems);
  return {
    pages: visiblePages.length,
    problems: counts.total,
    coreProblems: counts.core,
    supplementalItems: counts.supplemental,
    problemLabel: formatProblemCount(counts),
    riskCount
  };
}
function aiModelFallbackToast(session) {
  const summary = session?.ai_summary || session?.aiSummary || {};
  const fallbacks = Array.isArray(summary.model_fallbacks || summary.modelFallbacks) ? summary.model_fallbacks || summary.modelFallbacks : [];
  const first = fallbacks.find(item => item && item.from && item.to);
  if (!first) return '';
  const from = AI_MODEL_LABELS[first.from] || first.from;
  const to = AI_MODEL_LABELS[first.to] || first.to;
  return `${from} 호출 오류 · ${to}로 재시도했어요`;
}
function mergeRetryCandidateIntoCurrent(currentSession, candidateSession, pageIds, options = {}) {
  const base = cloneSession(currentSession);
  const candidate = cloneSession(candidateSession);
  if (!base || !candidate || !Array.isArray(candidate.problems)) return candidate;
  const targetProblemIds = new Set((Array.isArray(options.problemIds) ? options.problemIds : []).map(id => String(id || '').trim()).filter(Boolean));
  if (options.partial && targetProblemIds.size) {
    const replacementsByProblemId = new Map();
    candidate.problems.forEach(problem => {
      const sourceId = replacementSourceIdFor(problem);
      if (!sourceId || !targetProblemIds.has(sourceId)) return;
      const replacements = replacementsByProblemId.get(sourceId) || [];
      replacements.push(problem);
      replacementsByProblemId.set(sourceId, replacements);
    });
    if (replacementsByProblemId.size) {
      const insertedReplacementIds = new Set();
      const nextProblems = [];
      (base.problems || []).forEach(problem => {
        const problemId = String(problem?.id || '');
        if (!targetProblemIds.has(problemId)) {
          nextProblems.push(problem);
          return;
        }
        const replacements = replacementsByProblemId.get(problemId) || [];
        if (!replacements.length) {
          nextProblems.push(problem);
          return;
        }
        replacements.forEach(replacement => {
          if (replacement?.id) insertedReplacementIds.add(String(replacement.id));
          nextProblems.push(replacement);
        });
      });
      replacementsByProblemId.forEach(replacements => {
        replacements.forEach(replacement => {
          if (replacement?.id && !insertedReplacementIds.has(String(replacement.id))) {
            insertedReplacementIds.add(String(replacement.id));
            nextProblems.push(replacement);
          }
        });
      });
      base.problems = nextProblems;
      base.pages = (base.pages || []).map(page => {
        const ids = page.problemIds || page.problem_ids || [];
        const nextIds = [];
        ids.forEach(rawId => {
          const id = String(rawId || '');
          const replacements = replacementsByProblemId.get(id);
          if (replacements?.length) {
            nextIds.push(...replacements.map(problem => problem.id).filter(Boolean));
          } else {
            nextIds.push(rawId);
          }
        });
        return {
          ...page,
          problemIds: nextIds
        };
      });
      applyProblemCounts(base, nextProblems);
      base.ai_retry_summary = candidate.ai_retry_summary || candidate.aiRetrySummary || [];
      base.edb_path = null;
      base.edb_file_uri = null;
      base.edbPath = null;
      base.edbFileUri = null;
      return base;
    }
  }
  const targetPageIds = new Set((Array.isArray(pageIds) && pageIds.length ? pageIds : (candidate.pages || []).map(page => page.id)).filter(Boolean));
  if (!targetPageIds.size) return candidate;
  const candidatePagesById = new Map();
  (candidate.pages || []).forEach(page => {
    if (page?.id && targetPageIds.has(page.id)) candidatePagesById.set(page.id, page);
  });
  const candidateProblemsById = new Map();
  candidate.problems.forEach(problem => {
    if (problem?.id) candidateProblemsById.set(problem.id, problem);
  });
  const replacementsByPageId = new Map();
  candidatePagesById.forEach((page, pageId) => {
    const ids = page.problemIds || page.problem_ids || [];
    const replacements = ids.map(id => candidateProblemsById.get(id)).filter(Boolean);
    replacementsByPageId.set(pageId, replacements);
  });
  const insertedPages = new Set();
  const nextProblems = [];
  (base.problems || []).forEach(problem => {
    const pageId = problem?.sourcePageId;
    if (targetPageIds.has(pageId)) {
      if (!insertedPages.has(pageId)) {
        nextProblems.push(...(replacementsByPageId.get(pageId) || []));
        insertedPages.add(pageId);
      }
      return;
    }
    nextProblems.push(problem);
  });
  targetPageIds.forEach(pageId => {
    if (!insertedPages.has(pageId)) {
      nextProblems.push(...(replacementsByPageId.get(pageId) || []));
    }
  });
  base.problems = nextProblems;
  base.pages = (base.pages || []).map(page => {
    const candidatePage = candidatePagesById.get(page.id);
    if (!candidatePage) return page;
    return {
      ...page,
      ...candidatePage,
      id: page.id,
      sourceImagePath: page.sourceImagePath || candidatePage.sourceImagePath,
      sourceImageUri: page.sourceImageUri || candidatePage.sourceImageUri,
      problemIds: (candidatePage.problemIds || candidatePage.problem_ids || []).filter(Boolean)
    };
  });
  applyProblemCounts(base, nextProblems);
  base.ai_retry_summary = candidate.ai_retry_summary || candidate.aiRetrySummary || [];
  base.edb_path = null;
  base.edb_file_uri = null;
  base.edbPath = null;
  base.edbFileUri = null;
  return base;
}
function requestedInitialView() {
  try {
    return new URLSearchParams(window.location.search).get('view') === 'review' ? 'review' : 'board';
  } catch (_err) {
    return 'board';
  }
}
async function readJsonResponse(resp, fallbackMessage) {
  const text = await resp.text();
  if (!text) return {};
  try {
    return JSON.parse(text);
  } catch (err) {
    const excerpt = text.replace(/\s+/g, ' ').trim().slice(0, 180);
    throw new Error(`${fallbackMessage || '응답 파싱 실패'} (${resp.status}) · JSON 응답 아님${excerpt ? `: ${excerpt}` : ''}`);
  }
}
function assertOkJson(resp, json, fallbackMessage) {
  if (!resp.ok || !json.ok) {
    throw new Error(json?.error || `${fallbackMessage || '요청 실패'} (${resp.status})`);
  }
  return json;
}
async function expectOkJson(resp, fallbackMessage) {
  const json = await readJsonResponse(resp, fallbackMessage);
  return assertOkJson(resp, json, fallbackMessage);
}
async function postExport(files, aiFallback, inputIntent = DEFAULT_INPUT_INTENT, options = {}) {
  const resolvedInputIntent = normalizeInputIntent(inputIntent);
  const inputIntentConfig = INPUT_INTENT_BY_VALUE[resolvedInputIntent] || INPUT_INTENT_BY_VALUE[DEFAULT_INPUT_INTENT];
  const filesPayload = await Promise.all(files.map(async f => ({
    fileName: f.name,
    fileDataBase64: await fileToBase64(f)
  })));
  const resp = await fetch('/api/export', {
    method: 'POST',
    signal: options.signal,
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      files: filesPayload,
      preview: !!options.preview,
      exportMode: inputIntentConfig.exportMode,
      inputIntent: resolvedInputIntent,
      input_intent: resolvedInputIntent,
      sourceMode: 'auto',
      source_mode: 'auto',
      subject: 'unknown',
      ocr: 'auto',
      exportEdb: Object.prototype.hasOwnProperty.call(options, 'exportEdb') ? !!options.exportEdb : !options.preview,
      detectPerspective: files.some(f => !isDocumentLikeFile(f)),
      maxDimension: 2400,
      aiFallback: aiFallback || AI_FALLBACK_OFF
    })
  });
  const json = await readJsonResponse(resp, '파싱 실행 실패');
  if (!resp.ok || !json.ok) throw new Error(formatApiError(json, `파싱 실행 실패 (${resp.status})`));
  return json.session;
}
function formatApiError(payload, fallbackMessage) {
  const baseMessage = String(payload?.error || fallbackMessage || '요청에 실패했습니다').trim();
  const steps = Array.isArray(payload?.recoverySteps) ? payload.recoverySteps.map(step => String(step || '').trim()).filter(Boolean) : [];
  if (!steps.length) return baseMessage;
  return `${baseMessage}\n\n다음 조치:\n${steps.map((step, index) => `${index + 1}. ${step}`).join('\n')}`;
}
async function fetchUserSettings() {
  const resp = await fetch('/api/user-settings');
  const json = await expectOkJson(resp, '설정 로드 실패');
  return json.settings;
}
async function fetchRuntimeDiagnostics() {
  const resp = await fetch('/api/runtime-diagnostics');
  const json = await expectOkJson(resp, '진단 로드 실패');
  return json;
}
async function fetchAppUpdateStatus() {
  const resp = await fetch('/api/app/update');
  const json = await expectOkJson(resp, '업데이트 확인 실패');
  return json;
}
async function clearSession() {
  const resp = await fetch('/api/session/latest', {
    method: 'DELETE'
  });
  if (resp.status === 404) return {
    history: []
  };
  const json = await expectOkJson(resp, '세션 초기화 실패');
  return json;
}
async function postShutdown() {
  const resp = await fetch('/api/system/shutdown', {
    method: 'POST'
  });
  const json = await expectOkJson(resp, '앱 종료 실패');
  return json;
}
async function postOpenUrl(url) {
  const resp = await fetch('/api/system/open-url', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      url
    })
  });
  const json = await expectOkJson(resp, '브라우저 열기 실패');
  return json;
}
async function postMutate(action, args) {
  const resp = await fetch('/api/session/mutate', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      action,
      ...args
    })
  });
  const json = await expectOkJson(resp, '검수 수정 실패');
  return json.session;
}
async function postRetryAi(args, options = {}) {
  const resp = await fetch('/api/session/retry-ai', {
    method: 'POST',
    signal: options.signal,
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      ...(args || {}),
      preview: !!options.preview
    })
  });
  const json = await expectOkJson(resp, 'AI 재인식 실패');
  return json;
}
async function postEnhanceImage(args, options = {}) {
  const resp = await fetch('/api/session/enhance-image', {
    method: 'POST',
    signal: options.signal,
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify(args || {})
  });
  const json = await expectOkJson(resp, 'AI 업스케일 실패');
  return json;
}
async function postRestore(snapshot) {
  const resp = await fetch('/api/session/restore', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      session: snapshot
    })
  });
  const json = await expectOkJson(resp, '이전 상태 복원 실패');
  return json.session;
}
async function postClassinReviewResult(payload) {
  const resp = await fetch('/api/session/classin-review', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify(payload || {})
  });
  const json = await expectOkJson(resp, 'ClassIn 검수 저장 실패');
  return json.session;
}
async function openOutputFolder(path) {
  if (!path) return;
  try {
    const resp = await fetch('/api/system/open-folder', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json'
      },
      body: JSON.stringify({
        path
      })
    });
    const json = await readJsonResponse(resp, '폴더 열기 실패').catch(() => ({}));
    if (!resp.ok || !json.ok) {
      console.warn('[board] open-folder failed:', json.error || resp.status);
    }
  } catch (e) {
    console.warn('[board] open-folder error:', e.message);
  }
}
async function saveUserSettings(settings) {
  const payload = typeof settings === 'string' ? {
    geminiApiKey: settings
  } : {
    ...(settings || {})
  };
  const resp = await fetch('/api/user-settings', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify(payload)
  });
  const json = await expectOkJson(resp, '설정 저장 실패');
  return json.settings;
}
function App() {
  const [t, setTweak] = useTweaks(TWEAK_DEFAULTS);
  useEffect(() => {
    document.body.classList.toggle('dark', !!t.dark);
    document.documentElement.style.setProperty('--accent', t.accent);
    document.documentElement.style.setProperty('--board', t.boardColor);
  }, [t.dark, t.accent, t.boardColor]);
  const [items, setItems] = useState([]);
  const [activeId, setActiveId] = useState(null);
  const [fileName, setFileName] = useState('새 세션');
  const [bulk, setBulk] = useState(false);
  const [toast, setToast] = useState(null);
  const [published, setPublished] = useState(false);
  const [session, setSession] = useState(null);
  const [loading, setLoading] = useState(null);
  const [backgroundJobs, setBackgroundJobs] = useState([]);
  const [recognitionReview, setRecognitionReview] = useState(null);
  const [confirmingRecognition, setConfirmingRecognition] = useState(false);
  const [usingMock, setUsingMock] = useState(false);
  const [userSettings, setUserSettings] = useState(null);
  const [runtimeDiagnostics, setRuntimeDiagnostics] = useState(null);
  const [updateInfo, setUpdateInfo] = useState(null);
  const [updateBusy, setUpdateBusy] = useState(false);
  const [aiEnabled, setAiEnabled] = useState(true);
  const [inputIntent, setInputIntent] = useState(DEFAULT_INPUT_INTENT);
  const [refreshing, setRefreshing] = useState(false);
  const [recentSessions, setRecentSessions] = useState([]);
  const [restoringSessionId, setRestoringSessionId] = useState(null);
  const [pendingFiles, setPendingFiles] = useState([]);
  const initialViewRef = useRef(requestedInitialView());
  const initialViewConsumedRef = useRef(false);
  const [view, setView] = useState(initialViewRef.current);
  const [reviewFocus, setReviewFocus] = useState(null);
  const [mutating, setMutating] = useState(false);
  const [historyStack, setHistoryStack] = useState([]);
  const fileInputRef = useRef(null);
  const jobControllersRef = useRef(new Map());
  const reviewAvailable = Array.isArray(session?.pages) && session.pages.length > 0;
  useEffect(() => {
    if (view === 'review' && !reviewAvailable) setView('board');
  }, [view, reviewAvailable]);
  const canUndo = historyStack.length > 0 && !mutating;
  const activeIndex = items.findIndex(i => i.id === activeId);
  const active = activeIndex >= 0 ? items[activeIndex] : null;
  const processed = items.filter(i => i.step !== 'raw').length;
  const progress = items.length ? processed / items.length : 0;
  const hasRunningQueueRecognition = backgroundJobs.some(job => job.status === 'running' && job.scope === 'queue-recognition');
  const hasRunningSessionRecognition = backgroundJobs.some(job => job.status === 'running' && job.scope === 'session-recognition');
  const hasRunningImageEnhance = backgroundJobs.some(job => job.status === 'running' && job.scope === 'image-enhance');
  const showToast = msg => {
    setToast(msg);
    setTimeout(() => setToast(null), 2200);
  };
  const updateStatusToast = info => {
    if (!info) return '업데이트 정보를 확인하지 못했습니다';
    if (info.updateAvailable) {
      const latestVersion = info.latest?.version || '새 버전';
      return `업데이트 가능 · ${latestVersion}`;
    }
    if (info.channelStatus === 'up_to_date') return '현재 최신 버전입니다';
    if (info.channelStatus === 'manual_download') return '다운로드 페이지를 열 수 있습니다';
    if (info.channelStatus === 'not_configured') return '업데이트 채널이 아직 설정되지 않았습니다';
    if (info.channelStatus === 'unsupported_platform') return '이 OS용 업데이트가 아직 없습니다';
    if (info.channelStatus === 'error') return `업데이트 확인 오류: ${info.error || '채널 확인 실패'}`;
    return '업데이트 정보를 확인했습니다';
  };
  const checkForUpdates = useCallback(async (options = {}) => {
    setUpdateBusy(true);
    try {
      const info = await fetchAppUpdateStatus();
      setUpdateInfo(info);
      if (!options.silent) showToast(updateStatusToast(info));
      return info;
    } catch (e) {
      if (!options.silent) showToast('업데이트 확인 실패: ' + e.message);
      console.warn('[board] update check skipped:', e.message);
      return null;
    } finally {
      setUpdateBusy(false);
    }
  }, []);
  const openUpdatePage = useCallback(async () => {
    const info = updateInfo || (await checkForUpdates({
      silent: true
    }));
    const url = info?.downloadUrl || info?.latest?.downloadUrl || info?.releaseNotesUrl || info?.latest?.releaseNotesUrl || '';
    if (!url) {
      showToast('업데이트 다운로드 URL이 없습니다');
      return;
    }
    try {
      await postOpenUrl(url);
      showToast('다운로드 페이지를 열었어요');
    } catch (e) {
      showToast('다운로드 페이지 열기 실패: ' + e.message);
    }
  }, [updateInfo, checkForUpdates]);
  const refreshSessionHistory = useCallback(async () => {
    try {
      const history = await fetchSessionHistory();
      setRecentSessions(history);
      return history;
    } catch (e) {
      console.warn('[board] session history skipped:', e.message);
      return [];
    }
  }, []);
  const dismissBackgroundJob = useCallback(id => {
    setBackgroundJobs(prev => prev.filter(job => job.id !== id));
    jobControllersRef.current.delete(id);
  }, []);
  const settleBackgroundJob = useCallback((id, patch, autoDismissMs = 1800) => {
    jobControllersRef.current.delete(id);
    setBackgroundJobs(prev => prev.map(job => job.id === id ? {
      ...job,
      ...patch
    } : job));
    if (autoDismissMs) {
      window.setTimeout(() => {
        setBackgroundJobs(prev => prev.filter(job => job.id !== id));
      }, autoDismissMs);
    }
  }, []);
  const startBackgroundJob = useCallback(job => {
    const id = `job-${Date.now()}-${Math.random().toString(36).slice(2, 7)}`;
    const controller = new AbortController();
    jobControllersRef.current.set(id, controller);
    setBackgroundJobs(prev => [{
      id,
      status: 'running',
      startedAt: Date.now(),
      label: job.label || '백그라운드 작업',
      hint: job.hint || '',
      scope: job.scope || 'general'
    }, ...prev.filter(item => item.status === 'running').slice(0, 2), ...prev.filter(item => item.status !== 'running').slice(0, 1)]);
    return {
      id,
      controller
    };
  }, []);
  const cancelBackgroundJob = useCallback(id => {
    const job = backgroundJobs.find(item => item.id === id);
    const isRecognition = String(job?.scope || '').includes('recognition');
    const controller = jobControllersRef.current.get(id);
    if (controller) controller.abort();
    settleBackgroundJob(id, {
      status: 'canceled',
      label: isRecognition ? 'AI 인식 취소됨' : '작업 취소됨',
      hint: '결과를 적용하지 않았습니다.'
    }, 2200);
    showToast(isRecognition ? 'AI 인식을 취소했어요' : '작업을 취소했어요');
  }, [backgroundJobs, settleBackgroundJob]);
  useEffect(() => {
    return () => {
      jobControllersRef.current.forEach(controller => controller.abort());
      jobControllersRef.current.clear();
    };
  }, []);
  const showMockItems = (message = '더미 자료를 표시했어요') => {
    const mockItems = freshInitialItems();
    setSession(null);
    setItems(mockItems);
    setActiveId(mockItems[0]?.id || null);
    setUsingMock(true);
    setPublished(false);
    setFileName('더미 세션');
    setHistoryStack([]);
    setView('board');
    showToast(message);
  };
  const hideMockItems = (message = '빈 세션으로 전환했어요') => {
    setSession(null);
    setItems([]);
    setActiveId(null);
    setUsingMock(false);
    setPublished(false);
    setFileName('새 세션');
    setHistoryStack([]);
    setView('board');
    showToast(message);
  };
  const applySession = useCallback(rawSession => {
    if (!rawSession || !Array.isArray(rawSession.problems) || rawSession.problems.length === 0) {
      return false;
    }
    const mapped = rawSession.problems.map((p, idx) => mapProblemToItem(p, idx));
    setItems(mapped);
    setActiveId(mapped[0].id);
    setSession(rawSession);
    setUsingMock(false);
    setPublished(!!sessionPublishSummary(rawSession));
    if (rawSession.session_name) setFileName(rawSession.session_name);
    const wantsReview = !initialViewConsumedRef.current && initialViewRef.current === 'review' || shouldOpenReview(rawSession);
    if (hasReviewPages(rawSession) && wantsReview) {
      setView('review');
      initialViewConsumedRef.current = true;
    }
    return true;
  }, []);
  const adoptMutatedSession = useCallback((nextSession, prevSession) => {
    if (!nextSession || !Array.isArray(nextSession.problems)) return;
    const nextProblemsById = new Map();
    nextSession.problems.forEach(p => {
      if (p && p.id) nextProblemsById.set(p.id, p);
    });
    const replacementsBySourceId = new Map();
    nextSession.problems.forEach(problem => {
      const sourceId = replacementSourceIdFor(problem);
      if (!sourceId || !problem?.id) return;
      const replacements = replacementsBySourceId.get(sourceId) || [];
      replacements.push(problem.id);
      replacementsBySourceId.set(sourceId, replacements);
    });
    const prevItemIds = items.map(it => it.id);
    const seen = new Set();
    const orderedIds = [];
    for (const id of prevItemIds) {
      if (nextProblemsById.has(id) && !seen.has(id)) {
        orderedIds.push(id);
        seen.add(id);
        continue;
      }
      const replacements = replacementsBySourceId.get(id) || [];
      replacements.forEach(replacementId => {
        if (nextProblemsById.has(replacementId) && !seen.has(replacementId)) {
          orderedIds.push(replacementId);
          seen.add(replacementId);
        }
      });
      if (replacements.length) {
        continue;
      }
    }
    for (const prob of nextSession.problems) {
      if (prob && prob.id && !seen.has(prob.id)) {
        orderedIds.push(prob.id);
        seen.add(prob.id);
      }
    }
    const orderedProblems = orderedIds.map(id => nextProblemsById.get(id)).filter(Boolean);
    const mapped = orderedProblems.map((p, idx) => mapProblemToItem(p, idx));
    setItems(mapped);
    setSession(nextSession);
    setPublished(false);
    if (!nextProblemsById.has(activeId)) {
      const replacementActiveId = (replacementsBySourceId.get(activeId) || []).find(id => nextProblemsById.has(id));
      setActiveId(replacementActiveId || mapped[0]?.id || null);
    }
  }, [items, activeId]);
  const mutateSession = useCallback(async (action, args) => {
    if (!session) {
      showToast('변경할 세션이 없습니다');
      return;
    }
    setMutating(true);
    setLoading({
      label: action === 'split' ? '문제를 가르는 중…' : action === 'merge' ? '문제를 합치는 중…' : action === 'crop' ? '이미지를 자르는 중…' : action === 'exclude' ? '문제를 제외하는 중…' : '변경 중…',
      startedAt: Date.now()
    });
    const snapshotBefore = materializeSessionForItems(session, items, fileName) || session;
    try {
      await postRestore(snapshotBefore);
      const next = await postMutate(action, args);
      setHistoryStack(prev => [...prev, snapshotBefore]);
      adoptMutatedSession(next, snapshotBefore);
      refreshSessionHistory();
      showToast(action === 'split' ? '문제를 두 개로 갈랐어요' : action === 'merge' ? '문제를 합쳤어요' : action === 'crop' ? '자르기를 적용했어요' : '문제를 제외했어요');
    } catch (e) {
      showToast(`수정 실패: ${e.message}`);
    } finally {
      setMutating(false);
      setLoading(null);
    }
  }, [session, items, fileName, adoptMutatedSession, refreshSessionHistory]);
  const retryAiSession = useCallback(async args => {
    if (!session) {
      showToast('변경할 세션이 없습니다');
      return;
    }
    if (!userSettings?.hasGeminiApiKey) {
      showToast('Gemini API 키를 먼저 저장해 주세요');
      return;
    }
    const pageIds = listUnique((args?.pageIds || args?.page_ids || []).filter(Boolean));
    const problemIds = listUnique((args?.problemIds || args?.problem_ids || []).filter(Boolean));
    const isPartialRetry = Boolean(args?.partial || args?.partialRetry || args?.partial_retry) && problemIds.length > 0;
    const targetPageIds = isPartialRetry ? listUnique(problemIds.map(id => (session.problems || []).find(problem => problem?.id === id)?.sourcePageId).filter(Boolean)) : pageIds;
    const snapshotBefore = materializeSessionForItems(session, items, fileName) || cloneSession(session);
    const job = startBackgroundJob({
      scope: 'session-recognition',
      label: isPartialRetry ? problemIds.length === 1 ? '주변 영역 AI 재인식 중' : `${problemIds.length}개 주변 영역 AI 재인식 중` : pageIds.length === 1 ? 'AI 문제 인식 중' : `${pageIds.length || '전체'}개 페이지 AI 인식 중`,
      hint: isPartialRetry ? '선택한 박스 주변 여백까지 다시 자릅니다. 완료되면 확인 팝업이 열립니다.' : '보드 작업은 계속할 수 있습니다. 완료되면 확인 팝업이 열립니다.'
    });
    try {
      const result = await postRetryAi(args, {
        signal: job.controller.signal,
        preview: true
      });
      if (job.controller.signal.aborted) return;
      const next = result.session;
      const fallbackMessage = aiModelFallbackToast(next);
      if (fallbackMessage) showToast(fallbackMessage);
      const applied = (result.retry || []).filter(row => row.status === 'applied').length;
      settleBackgroundJob(job.id, {
        status: 'done',
        label: 'AI 인식 완료',
        hint: '결과 확인 팝업에서 문제 경계를 확인하세요.'
      });
      setRecognitionReview({
        id: `review-${job.id}`,
        kind: 'retry-ai',
        partial: isPartialRetry,
        title: isPartialRetry ? applied ? `${applied}개 주변 영역을 다시 인식했어요` : '부분 AI 인식 결과를 확인해 주세요' : applied ? `${applied}개 페이지를 다시 인식했어요` : 'AI 인식 결과를 확인해 주세요',
        subtitle: isPartialRetry ? '선택한 박스 주변에서 다시 찾은 문제 경계입니다. 맞으면 기존 자리만 바꿔 반영합니다.' : '문제 경계가 맞으면 바로 칠판에 분할해서 붙입니다.',
        session: next,
        pageIds: targetPageIds,
        problemIds,
        snapshotBefore,
        retrySummary: result.retry || []
      });
    } catch (e) {
      if (e?.name === 'AbortError') {
        settleBackgroundJob(job.id, {
          status: 'canceled',
          label: 'AI 인식 취소됨',
          hint: '결과를 적용하지 않았습니다.'
        });
        return;
      }
      settleBackgroundJob(job.id, {
        status: 'failed',
        label: 'AI 인식 실패',
        hint: e.message
      }, 5000);
      showToast(`AI 재인식 실패: ${e.message}`);
    }
  }, [session, userSettings, items, fileName, startBackgroundJob, settleBackgroundJob]);
  const recognizeCurrentSession = useCallback(async () => {
    if (!session || !Array.isArray(session.pages) || session.pages.length === 0) {
      showToast('문제 인식할 원본 페이지가 없습니다');
      return;
    }
    if (!userSettings?.hasGeminiApiKey) {
      showToast('Gemini API 키를 먼저 저장해 주세요');
      return;
    }
    const pageIds = listUnique(session.pages.map(page => page.id).filter(Boolean));
    await retryAiSession({
      pageIds
    });
  }, [session, userSettings, retryAiSession]);
  const undoMutation = useCallback(async () => {
    if (historyStack.length === 0) return;
    const snapshot = historyStack[historyStack.length - 1];
    setMutating(true);
    setLoading({
      label: '되돌리는 중…',
      startedAt: Date.now()
    });
    try {
      const restored = await postRestore(snapshot);
      setHistoryStack(prev => prev.slice(0, -1));
      adoptMutatedSession(restored, session);
      refreshSessionHistory();
      showToast('이전 상태로 되돌렸어요');
    } catch (e) {
      showToast(`되돌리기 실패: ${e.message}`);
    } finally {
      setMutating(false);
      setLoading(null);
    }
  }, [historyStack, session, adoptMutatedSession, refreshSessionHistory]);
  useEffect(() => {
    const onKey = evt => {
      if (!(evt.key === 'z' || evt.key === 'Z')) return;
      if (!(evt.ctrlKey || evt.metaKey)) return;
      if (evt.shiftKey) return;
      const target = evt.target;
      const tag = (target?.tagName || '').toUpperCase();
      const isEditable = tag === 'INPUT' || tag === 'TEXTAREA' || target && target.isContentEditable;
      if (isEditable) return;
      if (historyStack.length === 0 || mutating) return;
      evt.preventDefault();
      undoMutation();
    };
    window.addEventListener('keydown', onKey);
    return () => window.removeEventListener('keydown', onKey);
  }, [historyStack.length, mutating, undoMutation]);
  useEffect(() => {
    let cancelled = false;
    (async () => {
      try {
        const s = await fetchLatestSession();
        if (cancelled) return;
        if (s) applySession(s);
      } catch (e) {
        if (!cancelled) console.warn('[board] session load skipped:', e.message);
      }
    })();
    return () => {
      cancelled = true;
    };
  }, [applySession]);
  useEffect(() => {
    refreshSessionHistory();
  }, [refreshSessionHistory]);
  useEffect(() => {
    let cancelled = false;
    (async () => {
      try {
        const s = await fetchUserSettings();
        if (cancelled) return;
        setUserSettings(s);
        setAiEnabled(!!s?.hasGeminiApiKey);
      } catch (e) {
        if (!cancelled) console.warn('[board] user-settings load skipped:', e.message);
      }
    })();
    return () => {
      cancelled = true;
    };
  }, []);
  useEffect(() => {
    let cancelled = false;
    (async () => {
      try {
        const diagnostics = await fetchRuntimeDiagnostics();
        if (!cancelled) setRuntimeDiagnostics(diagnostics);
      } catch (e) {
        if (!cancelled) console.warn('[board] runtime diagnostics skipped:', e.message);
      }
    })();
    return () => {
      cancelled = true;
    };
  }, []);
  useEffect(() => {
    checkForUpdates({
      silent: true
    });
  }, [checkForUpdates]);
  const onSaveGeminiKey = useCallback(async key => {
    try {
      const s = await saveUserSettings({
        geminiApiKey: key || ''
      });
      setUserSettings(s);
      showToast(key ? 'Gemini 키 저장됨' : 'Gemini 키 삭제됨');
    } catch (e) {
      showToast('저장 실패: ' + e.message);
    }
  }, []);
  const onSaveOpenAiKey = useCallback(async key => {
    try {
      const s = await saveUserSettings({
        openAiApiKey: key || ''
      });
      setUserSettings(s);
      showToast(key ? 'OpenAI 키 저장됨' : 'OpenAI 키 삭제됨');
    } catch (e) {
      showToast('저장 실패: ' + e.message);
    }
  }, []);
  const enhanceImageSession = useCallback(async problemIds => {
    if (!session) {
      showToast('변경할 세션이 없습니다');
      return;
    }
    if (!userSettings?.hasGeminiApiKey) {
      showToast('Gemini API 키를 먼저 저장해 주세요');
      return;
    }
    const ids = listUnique((problemIds || []).filter(Boolean));
    if (!ids.length) {
      showToast('업스케일할 문항을 선택해 주세요');
      return;
    }
    const snapshotBefore = materializeSessionForItems(session, items, fileName) || cloneSession(session);
    const job = startBackgroundJob({
      scope: 'image-enhance',
      label: ids.length === 1 ? 'AI 업스케일 재구성 중' : `${ids.length}개 문항 AI 업스케일 중`,
      hint: 'Nano Banana 2로 투명 배경과 문자·숫자 선명도를 개선합니다.'
    });
    try {
      const result = await postEnhanceImage({
        problemIds: ids,
        provider: 'gemini'
      }, {
        signal: job.controller.signal
      });
      if (job.controller.signal.aborted) return;
      const next = result.session;
      const applied = (result.enhance || []).filter(row => row.status === 'applied').length;
      setHistoryStack(prev => [...prev, snapshotBefore]);
      adoptMutatedSession(next, snapshotBefore);
      settleBackgroundJob(job.id, {
        status: 'done',
        label: 'AI 업스케일 완료',
        hint: applied ? `${applied}개 문항을 재구성했습니다.` : '적용된 문항이 없습니다. 결과를 확인해 주세요.'
      });
      showToast(applied ? `AI 업스케일 적용 · ${applied}개 문항` : 'AI 업스케일 결과를 확인해 주세요');
    } catch (e) {
      if (e?.name === 'AbortError') {
        settleBackgroundJob(job.id, {
          status: 'canceled',
          label: 'AI 업스케일 취소됨',
          hint: '결과를 적용하지 않았습니다.'
        });
        return;
      }
      settleBackgroundJob(job.id, {
        status: 'failed',
        label: 'AI 업스케일 실패',
        hint: e.message
      }, 5000);
      showToast(`AI 업스케일 실패: ${e.message}`);
    }
  }, [session, userSettings, items, fileName, startBackgroundJob, settleBackgroundJob, adoptMutatedSession]);
  const resetSession = useCallback(async () => {
    if (loading) {
      showToast('작업 중에는 초기화할 수 없습니다');
      return;
    }
    if (!session && items.length === 0 && pendingFiles.length === 0 && recentSessions.length === 0) {
      showToast('이미 빈 세션입니다');
      return;
    }
    if (!window.confirm('세션을 초기화할까요? 업로드 대기열, 보드 자료, 최근 작업 목록이 모두 사라집니다.')) return;
    try {
      if (session || recentSessions.length) {
        const result = await clearSession();
        setRecentSessions(Array.isArray(result?.history) ? result.history : []);
      }
      setPendingFiles([]);
      hideMockItems('초기화 완료 · 빈 세션');
    } catch (e) {
      showToast('초기화 실패: ' + e.message);
    }
  }, [loading, session, items.length, pendingFiles.length, recentSessions.length]);
  const shutdownApp = useCallback(async () => {
    if (!window.confirm('로컬 앱을 종료할까요? 브라우저 창은 직접 닫으면 됩니다.')) return;
    try {
      await postShutdown();
      showToast('앱을 종료합니다. 브라우저 창을 닫아도 됩니다.');
    } catch (e) {
      showToast('앱 종료 실패: ' + e.message);
    }
  }, []);
  const refreshSession = useCallback(async () => {
    setRefreshing(true);
    try {
      const s = await fetchLatestSession();
      if (s && Array.isArray(s.problems) && s.problems.length) {
        applySession(s);
        refreshSessionHistory();
        showToast(`새로고침 완료 · ${formatProblemCount(sessionProblemCounts(s))}`);
      } else {
        if (!usingMock) {
          setSession(null);
          setItems([]);
          setActiveId(null);
          setPublished(false);
          setView('board');
        }
        showToast(usingMock ? '저장된 세션 없음 · 더미 유지' : '저장된 세션 없음 · 빈 세션');
      }
    } catch (e) {
      showToast('새로고침 실패: ' + e.message);
    } finally {
      setRefreshing(false);
    }
  }, [applySession, usingMock, refreshSessionHistory]);
  const restoreRecentSession = useCallback(async id => {
    if (!id || restoringSessionId) return;
    setRestoringSessionId(id);
    setLoading({
      label: '최근 작업을 여는 중…',
      startedAt: Date.now()
    });
    try {
      const result = await postRestoreSessionHistory(id);
      if (Array.isArray(result.history)) setRecentSessions(result.history);
      applySession(result.session);
      setHistoryStack([]);
      showToast('최근 작업을 열었어요');
    } catch (e) {
      showToast('작업 열기 실패: ' + e.message);
    } finally {
      setRestoringSessionId(null);
      setLoading(null);
    }
  }, [applySession, restoringSessionId]);
  const triggerUpload = () => fileInputRef.current?.click();
  const handleFiles = fileList => {
    const files = Array.from(fileList || []);
    if (!files.length) return;
    setPendingFiles(prev => {
      const seen = new Set(prev.map(fileQueueKey));
      const next = [...prev];
      files.forEach(file => {
        const key = fileQueueKey(file);
        if (!seen.has(key)) {
          seen.add(key);
          next.push(file);
        }
      });
      return next;
    });
    showToast(`${files.length}개 파일을 대기열에 추가했어요`);
    if (fileInputRef.current) fileInputRef.current.value = '';
  };
  const removePendingFile = useCallback(key => {
    setPendingFiles(prev => prev.filter(file => fileQueueKey(file) !== key));
  }, []);
  const clearPendingFiles = useCallback(() => {
    setPendingFiles([]);
    showToast('업로드 대기열을 비웠어요');
  }, []);
  const processQueuedFiles = useCallback(async (mode, targetKey = null) => {
    const files = targetKey ? pendingFiles.filter(file => fileQueueKey(file) === targetKey) : [...pendingFiles];
    if (!files.length) {
      showToast(targetKey ? '해당 파일이 대기열에 없습니다' : '대기열에 파일이 없습니다');
      return;
    }
    const isRecognition = mode === 'recognize';
    const resolvedInputIntent = isRecognition ? 'multi-problem' : 'single-problem';
    const aiFallback = isRecognition && aiEnabled && userSettings?.hasGeminiApiKey ? AI_FALLBACK_ON : AI_FALLBACK_OFF;
    if (isRecognition) {
      const fileKeys = files.map(fileQueueKey);
      const job = startBackgroundJob({
        scope: 'queue-recognition',
        label: files.length === 1 ? '1개 파일 AI 문제 인식 중' : `${files.length}개 파일 AI 문제 인식 중`,
        hint: aiFallback.enabled ? 'Gemini AI 보정으로 문항 경계를 확인합니다.' : '기본 문항 인식으로 실행 중입니다.'
      });
      try {
        const incomingSession = await postExport(files, aiFallback, resolvedInputIntent, {
          signal: job.controller.signal,
          preview: true
        });
        if (job.controller.signal.aborted) return;
        const fallbackMessage = aiModelFallbackToast(incomingSession);
        if (fallbackMessage) showToast(fallbackMessage);
        const summary = summarizeRecognitionSession(incomingSession);
        settleBackgroundJob(job.id, {
          status: 'done',
          label: '문제 인식 완료',
          hint: `${summary.problemLabel}을 찾았습니다.`
        });
        setRecognitionReview({
          id: `review-${job.id}`,
          kind: 'queue-recognition',
          title: files.length === 1 ? `${files[0].name || '파일'} · ${summary.problemLabel}로 인식했어요` : `${summary.problemLabel}로 인식했어요`,
          subtitle: '문제 경계가 맞으면 검수 화면에서 원본 위 박스를 확인합니다.',
          session: incomingSession,
          incomingSession,
          fileKeys,
          fileCount: files.length,
          outputFolder: incomingSession?.output_dir || incomingSession?.outputDir
        });
      } catch (e) {
        if (e?.name === 'AbortError') {
          settleBackgroundJob(job.id, {
            status: 'canceled',
            label: '문제 인식 취소됨',
            hint: '대기열은 그대로 유지했습니다.'
          });
          return;
        }
        settleBackgroundJob(job.id, {
          status: 'failed',
          label: '문제 인식 실패',
          hint: e.message
        }, 5000);
        showToast(`문제 인식 실패: ${e.message}`);
      } finally {
        if (fileInputRef.current) fileInputRef.current.value = '';
      }
      return;
    }
    setLoading({
      label: isRecognition ? `${files.length}개 파일에서 문제 인식 중...` : files.length === 1 ? '1개 파일을 그대로 등록 중...' : `${files.length}개 파일을 순서대로 등록 중...`,
      hint: isRecognition ? aiFallback.enabled ? 'Gemini AI 보정으로 문항 경계를 다시 확인합니다.' : 'Gemini 키가 없어 기본 문항 인식만 실행합니다.' : '각 이미지와 PDF 페이지를 하나의 자료로 등록합니다.',
      startedAt: Date.now()
    });
    try {
      const s = await postExport(files, aiFallback, resolvedInputIntent);
      let sessionToApply = s;
      if (session && !usingMock) {
        const currentSnapshot = materializeSessionForItems(session, items, fileName);
        const merged = mergeSessions(currentSnapshot, s, fileName);
        sessionToApply = await postRestore(merged);
      }
      applySession(sessionToApply);
      refreshSessionHistory();
      const appliedKeys = new Set(files.map(fileQueueKey));
      setPendingFiles(prev => prev.filter(file => !appliedKeys.has(fileQueueKey(file))));
      const intentLabel = isRecognition ? '문제 인식' : '순서 등록';
      showToast(`${intentLabel} 완료 · ${formatProblemCount(sessionProblemCounts(sessionToApply))}`);
      const folder = sessionToApply?.output_dir || sessionToApply?.outputDir || s?.output_dir || s?.outputDir;
      if (folder) openOutputFolder(folder);
    } catch (e) {
      showToast(`${isRecognition ? '문제 인식' : '등록'} 실패: ${e.message}`);
    } finally {
      setLoading(null);
      if (fileInputRef.current) fileInputRef.current.value = '';
    }
  }, [pendingFiles, aiEnabled, userSettings, session, usingMock, items, fileName, applySession, startBackgroundJob, settleBackgroundJob, refreshSessionHistory]);
  const cancelRecognitionReview = useCallback(() => {
    if (confirmingRecognition) return;
    setRecognitionReview(null);
    showToast('인식 결과를 적용하지 않았어요');
  }, [confirmingRecognition]);
  const confirmRecognitionReview = useCallback(async () => {
    const review = recognitionReview;
    if (!review) return;
    setConfirmingRecognition(true);
    try {
      if (review.kind === 'queue-recognition') {
        const incomingSession = review.incomingSession || review.session;
        const currentSnapshot = session && !usingMock ? materializeSessionForItems(session, items, fileName) : null;
        const candidate = currentSnapshot ? mergeSessions(currentSnapshot, incomingSession, fileName) : cloneSession(incomingSession);
        const restored = await postRestore(candidate);
        applySession(restored);
        refreshSessionHistory();
        setView('review');
        const appliedKeys = new Set(review.fileKeys || []);
        setPendingFiles(prev => prev.filter(file => !appliedKeys.has(fileQueueKey(file))));
        const summary = summarizeRecognitionSession(incomingSession);
        showToast(`검수로 이동 · ${summary.problemLabel}을 확인하세요`);
        if (review.outputFolder) openOutputFolder(review.outputFolder);
      } else if (review.kind === 'retry-ai') {
        const currentSnapshot = session ? materializeSessionForItems(session, items, fileName) || cloneSession(session) : cloneSession(review.snapshotBefore);
        const candidate = mergeRetryCandidateIntoCurrent(currentSnapshot, review.session, review.pageIds, {
          partial: !!review.partial,
          problemIds: review.problemIds || []
        });
        const restored = await postRestore(candidate);
        setHistoryStack(prev => [...prev, review.snapshotBefore || currentSnapshot].filter(Boolean));
        if (session) {
          adoptMutatedSession(restored, session);
        } else {
          applySession(restored);
        }
        refreshSessionHistory();
        setView('board');
        const summary = summarizeRecognitionSession(restored, review.pageIds);
        showToast(`AI 인식 적용 · ${summary.problemLabel}`);
      }
      setRecognitionReview(null);
    } catch (e) {
      showToast(`적용 실패: ${e.message}`);
    } finally {
      setConfirmingRecognition(false);
    }
  }, [recognitionReview, confirmingRecognition, session, usingMock, items, fileName, applySession, adoptMutatedSession, refreshSessionHistory]);
  const setStep = (id, step) => {
    const nextStep = normalizeProcessingStep(step);
    setItems(it => it.map(x => x.id === id ? {
      ...x,
      step: nextStep
    } : x));
    setPublished(false);
  };
  const applyToAll = (step, options = {}) => {
    if (!items.length) {
      showToast('적용할 자료가 없습니다');
      return;
    }
    const nextStep = normalizeProcessingStep(step);
    setItems(it => it.map(x => ({
      ...x,
      step: nextStep
    })));
    if (options.silent) return;
    showToast(`전체 ${items.length}개 항목에 ${stepLabel(nextStep)}을(를) 적용했어요`);
  };
  const setPlacement = (id, patch) => {
    setItems(it => it.map(x => {
      if (x.id !== id) return x;
      const next = {
        ...x
      };
      if (Object.prototype.hasOwnProperty.call(patch || {}, 'xRatio')) {
        next.placementXRatio = normalizePlacementXRatio(patch.xRatio);
      }
      if (Object.prototype.hasOwnProperty.call(patch || {}, 'scaleRatio')) {
        next.placementScaleRatio = normalizePlacementScaleRatio(patch.scaleRatio, maxPlacementScaleRatio(next));
      }
      if (Object.prototype.hasOwnProperty.call(patch || {}, 'yRatio')) {
        next.placementYRatio = verticalPlacementRoomPages(next, next.placementScaleRatio) > 0.001 ? normalizePlacementYRatio(patch.yRatio) : DEFAULT_PLACEMENT_Y_RATIO;
      } else if (Object.prototype.hasOwnProperty.call(patch || {}, 'scaleRatio') && verticalPlacementRoomPages(next, next.placementScaleRatio) <= 0.001) {
        next.placementYRatio = DEFAULT_PLACEMENT_Y_RATIO;
      }
      return next;
    }));
    setPublished(false);
  };
  const reorder = (fromId, toId, dropPosition = 'before', options = {}) => {
    const reordered = reorderItemsForDrop(items, fromId, toId, dropPosition);
    if (reordered === items) return;
    const resetItems = reordered.map(item => String(item.id) === String(fromId) ? resetItemPlacement(item) : item);
    const nextItems = reflowItemsForBoardOrder(options?.resetPlacement ? resetItems : reordered);
    setItems(nextItems);
    if (session) {
      const nextSession = materializeSessionForItems(session, nextItems, fileName) || session;
      setSession(nextSession);
      postRestore(nextSession).catch(e => console.warn('[board] reorder persist failed:', e.message));
    }
    setPublished(false);
  };
  const removeItem = id => {
    const nextItems = reflowItemsForBoardOrder(items.filter(x => x.id !== id));
    setItems(nextItems);
    if (activeId === id) {
      setActiveId(nextItems[0]?.id || null);
    }
    if (!session && usingMock && nextItems.length === 0) {
      setUsingMock(false);
      setFileName('새 세션');
    }
    setPublished(false);
  };
  const addMockSample = () => {
    if (session) {
      showToast('실제 세션에는 더미를 추가하지 않습니다');
      return;
    }
    if (loading || pendingFiles.length > 0) {
      showToast('대기열 처리 전에는 더미를 추가하지 않습니다');
      return;
    }
    const pool = ['geometry-circle', 'equation', 'table', 'graph', 'geometry-triangles', 'paragraph'];
    const kind = pool[Math.floor(Math.random() * pool.length)];
    const id = 'i' + Date.now() % 100000;
    const name = '새 자료 ' + (items.length + 1);
    setItems(it => [...it, {
      id,
      name,
      source: '방금 업로드',
      type: 'image',
      kind,
      step: 'raw',
      heightFrac: heightForKind(kind),
      placementXRatio: DEFAULT_PLACEMENT_X_RATIO,
      placementYRatio: DEFAULT_PLACEMENT_Y_RATIO,
      placementScaleRatio: DEFAULT_PLACEMENT_SCALE_RATIO
    }]);
    setUsingMock(true);
    setActiveId(id);
    setPublished(false);
    if (!items.length) {
      setFileName('더미 세션');
    }
    showToast('더미 자료 1개 추가됨');
  };
  const addSample = () => {
    triggerUpload();
  };
  const onConfirm = (id, options = {}) => {
    const explicitProblemIds = Array.isArray(options.problemIds) ? options.problemIds : null;
    const targetIds = explicitProblemIds?.length ? explicitProblemIds : options.bulk ? items.map(item => item.id) : [id];
    const confirmedIds = new Set(targetIds.filter(Boolean));
    if (!confirmedIds.size) return;
    const nextItemsForSnapshot = items.map(item => confirmedIds.has(item.id) ? confirmedItemState(item) : item);
    const confirmedSession = markSessionProblemsConfirmed(session, confirmedIds);
    const nextSession = confirmedSession ? materializeSessionForItems(confirmedSession, nextItemsForSnapshot, fileName) || confirmedSession : null;
    setItems(prev => prev.map(item => confirmedIds.has(item.id) ? confirmedItemState(item) : item));
    setSession(prev => nextSession || markSessionProblemsConfirmed(prev, confirmedIds));
    if (nextSession) {
      postRestore(nextSession).catch(e => console.warn('[board] confirm persist failed:', e.message));
    }
    setPublished(false);
    if (options.bulk) {
      showToast(`전체 ${confirmedIds.size}개 확인 완료`);
      return;
    }
    showToast(`"${items.find(i => i.id === id)?.name}" 확인 완료`);
  };
  const markClassinReviewComplete = useCallback(async () => {
    if (!session) {
      showToast('저장할 제작 세션이 없습니다');
      return;
    }
    try {
      const updated = await postClassinReviewResult({
        status: 'passed',
        notes: ''
      });
      setSession(updated);
      refreshSessionHistory();
      setPublished(true);
      showToast('ClassIn 검수 완료로 저장했어요');
    } catch (e) {
      showToast('ClassIn 검수 저장 실패: ' + e.message);
    }
  }, [session, refreshSessionHistory]);
  const onPublish = async () => {
    if (!session || !Array.isArray(session.problems)) {
      showToast('내보낼 자료가 없습니다. 먼저 파싱해 주세요.');
      return;
    }
    const sessionIds = new Set(session.problems.map(p => p.id));
    const currentIds = items.map(i => i.id);
    const order = currentIds.filter(id => sessionIds.has(id));
    const excluded = [...sessionIds].filter(id => !currentIds.includes(id));
    const sessionForPublish = materializeSessionForItems(session, items, fileName) || session;
    const publishReviewSummary = sessionReviewSummary(sessionForPublish);
    const duplicateProblemNumberGroups = Array.isArray(publishReviewSummary.blockingDuplicateProblemNumberGroups) ? publishReviewSummary.blockingDuplicateProblemNumberGroups : [];
    if (duplicateProblemNumberGroups.length > 0) {
      setReviewFocus({
        filter: 'all',
        problemIds: duplicateProblemNumberGroups.flatMap(group => Array.isArray(group?.problemIds || group?.problem_ids) ? group.problemIds || group.problem_ids : []),
        source: 'duplicate-number-preflight'
      });
      setView('review');
      const duplicateLabel = publishReviewSummary.duplicateProblemNumberLabel || `${duplicateProblemNumberGroups.length}그룹`;
      showToast(`중복 문항 번호가 있어 제작을 멈췄어요. ${duplicateLabel}`);
      return;
    }
    const passageSourceReuseIssues = findPassageGroupSourceReuse(sessionForPublish.problems || []).filter(issue => issue.type === 'passage_group_source_reuse');
    if (passageSourceReuseIssues.length > 0) {
      const firstIssue = passageSourceReuseIssues[0];
      const focusProblemIds = Array.isArray(firstIssue.problemIds || firstIssue.problem_ids) ? firstIssue.problemIds || firstIssue.problem_ids : [firstIssue.problemId, firstIssue.nextProblemId];
      setReviewFocus({
        filter: 'all',
        problemIds: focusProblemIds,
        source: 'passage-source-reuse-preflight'
      });
      setView('review');
      showToast(`긴 지문 그룹 안에서 원본 영역이 반복될 수 있어 제작을 멈췄어요. ${firstIssue.problemTitle || firstIssue.problemId} → ${firstIssue.nextProblemTitle || firstIssue.nextProblemId}`);
      return;
    }
    const sourceOverlapIssues = findSourceProblemOverlaps(sessionForPublish.problems || []).filter(issue => issue.type === 'source_problem_bbox_overlap');
    if (sourceOverlapIssues.length > 0) {
      const firstIssue = sourceOverlapIssues[0];
      const focusProblemIds = Array.isArray(firstIssue.problemIds || firstIssue.problem_ids) ? firstIssue.problemIds || firstIssue.problem_ids : [firstIssue.problemId, firstIssue.nextProblemId];
      setReviewFocus({
        filter: 'all',
        problemIds: focusProblemIds,
        source: 'source-overlap-preflight'
      });
      setView('review');
      showToast(`문항 원본 영역이 겹칠 수 있어 제작을 멈췄어요. ${firstIssue.problemTitle || firstIssue.problemId} → ${firstIssue.nextProblemTitle || firstIssue.nextProblemId}`);
      return;
    }
    const placementOverlapIssues = findBoardPlacementOverlaps(items, {
      sessionProblemIds: sessionIds
    }).filter(issue => issue.type === 'board_placement_overlap');
    if (placementOverlapIssues.length > 0) {
      const firstIssue = placementOverlapIssues[0];
      setView('board');
      showToast(`문항 배치가 겹칠 수 있어 제작을 멈췄어요. ${firstIssue.problemTitle || firstIssue.problemId} → ${firstIssue.nextProblemTitle || firstIssue.nextProblemId}`);
      return;
    }
    const publishReviewWarning = publishReviewWarningMessage(sessionForPublish, publishReviewSummary);
    if (publishReviewWarning) {
      const confirmedPublish = window.confirm(publishReviewWarning.message);
      if (!confirmedPublish) {
        setReviewFocus({
          filter: publishReviewWarning.reviewFilter,
          source: 'publish-warning'
        });
        setView('review');
        showToast(publishReviewWarning.cancelToast);
        return;
      }
    }
    const placements = Object.fromEntries(items.filter(item => sessionIds.has(item.id)).map(item => [item.id, {
      xRatio: normalizePlacementXRatio(item.placementXRatio),
      yRatio: verticalPlacementRoomPages(item) > 0.001 ? normalizePlacementYRatio(item.placementYRatio) : DEFAULT_PLACEMENT_Y_RATIO,
      scaleRatio: normalizePlacementScaleRatio(item.placementScaleRatio, maxPlacementScaleRatio(item))
    }]));
    setLoading({
      label: '편집된 .edb 파일 생성 중...',
      hint: order.length === sessionIds.size ? `${order.length}개 자료를 칠판 순서대로 재배치합니다.` : `${order.length}개 자료 (${excluded.length}개 제외)`,
      startedAt: Date.now()
    });
    try {
      const resp = await fetch('/api/session/publish', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json'
        },
        body: JSON.stringify({
          order,
          excluded,
          placements,
          session: sessionForPublish
        })
      });
      const json = await readJsonResponse(resp, 'publish 실패');
      if (!resp.ok || !json.ok) {
        const blockedPublish = normalizePublishPreflightBlock(json);
        if (blockedPublish) {
          const blockedTarget = publishBlockedTarget(blockedPublish);
          setReviewFocus(blockedTarget.reviewFocus);
          setView(blockedTarget.view);
          showToast(`서버 사전점검에서 제작을 멈췄어요. ${blockedPublish.issueSummaryLabel || blockedPublish.message}`);
          return;
        }
        throw new Error(json.error || `publish 실패 (${resp.status})`);
      }
      setSession(json.session);
      refreshSessionHistory();
      const publishSummary = json.publishSummary || json.publish_summary || json.session?.publishSummary || json.session?.publish_summary;
      const url = publishSummary?.edbFileUri || publishSummary?.edb_file_uri || json.session?.edb_file_uri;
      if (url) {
        const a = document.createElement('a');
        a.href = url;
        a.download = publishSummary?.edbFileName || publishSummary?.edb_file_name || (json.session.session_name || 'classin') + '.edb';
        document.body.appendChild(a);
        a.click();
        a.remove();
      }
      setPublished(true);
      const publishLabel = publishSummary?.recordCountLabel || publishSummary?.record_count_label || `${publishSummary?.recordCount || publishSummary?.record_count || order.length}개 자료`;
      showToast(`${publishLabel}로 EDB 제작 완료 · 다운로드 시작`);
    } catch (e) {
      showToast('제작 실패: ' + e.message);
    } finally {
      setLoading(null);
    }
  };
  return React.createElement("div", {
    className: "app"
  }, React.createElement(TopBar, {
    fileName: fileName,
    setFileName: setFileName,
    progress: progress,
    processed: processed,
    total: items.length,
    onPublish: onPublish,
    published: published,
    onReset: resetSession,
    onRefresh: refreshSession,
    refreshing: refreshing,
    canReset: (!!session || items.length > 0 || pendingFiles.length > 0 || recentSessions.length > 0) && !loading,
    view: view,
    setView: setView,
    reviewAvailable: reviewAvailable,
    onUndo: undoMutation,
    canUndo: canUndo,
    onShutdown: shutdownApp
  }), React.createElement("div", {
    className: "main"
  }, React.createElement(ItemsRail, {
    items: items,
    activeId: activeId,
    setActive: setActiveId,
    reorder: reorder,
    removeItem: removeItem,
    addSample: addSample,
    bulkApply: applyToAll,
    handleFiles: handleFiles,
    pendingFiles: pendingFiles,
    removePendingFile: removePendingFile,
    clearPendingFiles: clearPendingFiles,
    processQueuedFiles: processQueuedFiles,
    queueBusy: !!loading || hasRunningQueueRecognition,
    aiAvailable: !!userSettings?.hasGeminiApiKey,
    addMockSample: addMockSample,
    canAddDummy: !session && !loading && pendingFiles.length === 0,
    recentSessions: recentSessions,
    restoringSessionId: restoringSessionId,
    onRestoreRecentSession: restoreRecentSession
  }), view === 'review' ? React.createElement(ReviewStage, {
    session: session,
    items: items,
    activeId: activeId,
    setActive: setActiveId,
    mutateSession: mutateSession,
    retryAiSession: retryAiSession,
    mutating: mutating,
    aiAvailable: !!userSettings?.hasGeminiApiKey,
    aiBusy: hasRunningSessionRecognition,
    onConfirm: onConfirm,
    reviewFocus: reviewFocus
  }) : React.createElement(BoardStage, {
    items: items,
    activeId: activeId,
    setActive: setActiveId,
    boardColor: t.boardColor,
    boardColumns: t.boardColumns,
    fileName: fileName,
    addSample: addSample,
    setPlacement: setPlacement,
    reorder: reorder
  }), React.createElement(SidePanel, {
    item: active,
    items: items,
    activeIndex: activeIndex,
    setStep: setStep,
    applyToAll: applyToAll,
    bulk: bulk,
    setBulk: setBulk,
    setPlacement: setPlacement,
    mutateSession: mutateSession,
    mutating: mutating,
    boardColumns: t.boardColumns,
    setBoardColumns: v => setTweak('boardColumns', v),
    boardColor: t.boardColor,
    setBoardColor: v => setTweak('boardColor', v),
    accent: t.accent,
    setAccent: v => setTweak('accent', v),
    onConfirm: onConfirm,
    userSettings: userSettings,
    runtimeDiagnostics: runtimeDiagnostics,
    onSaveGeminiKey: onSaveGeminiKey,
    onSaveOpenAiKey: onSaveOpenAiKey,
    onEnhanceImage: enhanceImageSession,
    imageEnhanceBusy: hasRunningImageEnhance,
    aiEnabled: aiEnabled,
    setAiEnabled: setAiEnabled,
    inputIntent: inputIntent,
    setInputIntent: setInputIntent,
    onRecognizeSession: recognizeCurrentSession,
    canRecognizeSession: !!session && !!userSettings?.hasGeminiApiKey && !mutating && !hasRunningSessionRecognition,
    session: session,
    published: published,
    onClassinReviewComplete: markClassinReviewComplete,
    updateInfo: updateInfo,
    updateBusy: updateBusy,
    onCheckUpdate: checkForUpdates,
    onOpenUpdate: openUpdatePage
  })), React.createElement(BackgroundJobsPanel, {
    jobs: backgroundJobs,
    onCancel: cancelBackgroundJob,
    onDismiss: dismissBackgroundJob
  }), React.createElement(RecognitionReviewModal, {
    review: recognitionReview,
    confirming: confirmingRecognition,
    onConfirm: confirmRecognitionReview,
    onCancel: cancelRecognitionReview
  }), toast && React.createElement("div", {
    className: "toast"
  }, Icon.check, React.createElement("span", null, toast)), React.createElement("input", {
    ref: fileInputRef,
    type: "file",
    accept: ".pdf,.hwp,.hwpx,.png,.jpg,.jpeg,.webp,.bmp,.tif,.tiff,image/*",
    multiple: true,
    style: {
      display: 'none'
    },
    onChange: e => handleFiles(e.target.files)
  }), loading && React.createElement(LoadingOverlay, {
    label: loading.label,
    hint: loading.hint,
    startedAt: loading.startedAt
  }), React.createElement(TweaksPanel, {
    title: "Tweaks"
  }, React.createElement(TweakSection, {
    label: "\uD14C\uB9C8"
  }), React.createElement(TweakToggle, {
    label: "\uB2E4\uD06C \uBAA8\uB4DC",
    value: t.dark,
    onChange: v => setTweak('dark', v)
  }), React.createElement(TweakColor, {
    label: "\uAC15\uC870\uC0C9",
    value: t.accent,
    options: ACCENTS,
    onChange: v => setTweak('accent', v)
  }), React.createElement(TweakSection, {
    label: "\uCE60\uD310"
  }), React.createElement(TweakColor, {
    label: "\uCE60\uD310 \uC0C9",
    value: t.boardColor,
    options: BOARD_COLORS,
    onChange: v => setTweak('boardColor', v)
  }), React.createElement(TweakRadio, {
    label: "\uC5F4 \uC218",
    value: String(t.boardColumns),
    options: ['1', '2', '3'],
    onChange: v => setTweak('boardColumns', parseInt(v))
  })));
}
ReactDOM.createRoot(document.getElementById('root')).render(React.createElement(AppErrorBoundary, null, React.createElement(App, null)));
