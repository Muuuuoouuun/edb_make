# EDB 메이크 간단 사용 설명서

EDB 메이크는 PDF, 이미지, 한글 파일을 ClassIn에서 사용할 수 있는 `.edb` 칠판 자료로 만들어 주는 프로그램입니다.

처음 사용하는 분은 아래 순서만 따라 하면 됩니다.

1. 프로그램을 설치하고 실행합니다.
2. 프로그램 안에서 Gemini API 키를 입력합니다.
3. PDF, 이미지, HWP/HWPX 파일을 넣습니다.
4. 문제를 자동으로 인식하거나 직접 나눕니다.
5. 검수 후 `EDB 제작`을 누릅니다.
6. 만들어진 `.edb` 파일을 ClassIn에서 엽니다.

## 1. 프로그램 설치와 실행

### Windows

1. 전달받은 EDB 메이크 설치 파일을 실행합니다.
2. 설치가 끝나면 시작 메뉴 또는 바탕화면에서 `ClassIn EDB MVP`를 실행합니다.
3. 프로그램이 켜지면 브라우저 창이 자동으로 열립니다.

### macOS

1. 전달받은 macOS 앱 파일을 실행합니다.
2. 필요하면 앱을 `응용 프로그램` 폴더로 옮깁니다.
3. `ClassIn EDB MVP`를 실행하면 브라우저 창이 자동으로 열립니다.

브라우저가 자동으로 열리지 않으면 주소창에 아래 주소를 직접 입력하세요.

```text
http://127.0.0.1:8765/
```

## 2. API 키 입력하기

EDB 메이크는 기본 기능만 쓸 때는 API 키 없이도 사용할 수 있습니다.

하지만 아래 기능을 쓰려면 Gemini API 키가 필요합니다.

- 문제 자동 인식 품질 향상
- AI 재인식
- 흐린 문제 이미지 보정
- 고화질 업스케일 재구성

### 프로그램 안에서 키 입력

1. 화면 오른쪽에서 `칠판` 탭을 엽니다.
2. `Gemini API 키` 항목을 찾습니다.
3. 입력칸에 Gemini API 키를 붙여넣습니다.
4. `키 저장`을 누릅니다.
5. 상태가 `설정됨`으로 바뀌면 완료입니다.

OpenAI API 키는 일반 사용에서는 꼭 필요하지 않습니다. OpenAI 기반 이미지 재구성 fallback을 사용할 때만 입력하면 됩니다.

## 3. API 비용 안내

EDB 메이크 프로그램 자체가 별도로 과금하는 것은 아닙니다.

다만 Gemini 또는 OpenAI API 키를 입력하고 AI 기능을 사용하면, 해당 API 제공자 계정에 사용량에 따른 비용이 청구될 수 있습니다.

비용이 발생할 수 있는 경우:

- Gemini 키를 넣고 문제 자동 인식을 실행할 때
- `AI 재인식`을 사용할 때
- `AI 업스케일 재구성`을 사용할 때
- OpenAI fallback 기능을 사용할 때

비용을 줄이려면 처음에는 1~2페이지만 테스트하고, 필요한 문제에만 AI 보정을 적용하세요.

정확한 가격은 공식 가격표에서 확인합니다.

- Gemini API 가격표: <https://ai.google.dev/gemini-api/docs/pricing>
- OpenAI API 가격표: <https://developers.openai.com/api/docs/pricing>

## 4. 기본 화면 이해하기

화면은 크게 세 부분입니다.

| 위치 | 하는 일 |
| --- | --- |
| 왼쪽 `자료` | 파일을 넣고, 작업 목록을 봅니다. |
| 가운데 화면 | 칠판 배치 또는 문제 검수 화면입니다. |
| 오른쪽 패널 | API 키, 칠판 색상, 처리 방식, 세부 설정을 바꿉니다. |

처음에는 왼쪽에서 파일을 넣고, 가운데에서 확인하고, 오른쪽에서 설정한다고 생각하면 됩니다.

## 5. 파일 넣기

왼쪽 `자료` 영역에 파일을 드래그하거나, 업로드 버튼을 눌러 파일을 선택합니다.

사용 가능한 파일:

- PDF
- 이미지 파일
- HWP/HWPX

HWP/HWPX가 잘 열리지 않으면 한글 프로그램에서 PDF로 저장한 뒤 다시 넣는 것을 권장합니다.

## 6. 처리 방식 고르기

파일을 넣으면 `업로드 대기열`에 파일이 보입니다. 여기서 처리 방식을 선택합니다.

| 버튼 | 언제 사용하나요? |
| --- | --- |
| `문항 AI 인식` | 한 페이지에 여러 문제가 있고, 자동으로 문제를 나누고 싶을 때 |
| `페이지 PNG` | 문제를 나누지 않고 페이지 그대로 칠판에 넣고 싶을 때 |
| `수동 쪼개기` | 자동 인식이 불안하거나 직접 문제 영역을 나누고 싶을 때 |

일반적인 시험지나 문제지는 `문항 AI 인식`을 먼저 사용하면 됩니다.

## 7. 검수하기

문항 인식이 끝나면 `검수` 화면에서 결과를 확인합니다.

확인할 것:

- 문제가 빠지지 않았는지
- 한 문제가 둘로 잘리지 않았는지
- 두 문제가 하나로 붙지 않았는지
- 문제 번호, 보기, 지문이 함께 들어갔는지
- 페이지 번호나 꼬리말이 문제 안에 섞이지 않았는지

이상이 있으면 아래 기능을 사용합니다.

| 기능 | 하는 일 |
| --- | --- |
| `AI 재인식` | 선택한 페이지나 영역을 다시 인식합니다. |
| `수동 쪼개기` | 직접 문제 영역을 다시 나눕니다. |
| `합치기` | 잘못 나뉜 문제를 하나로 합칩니다. |
| `제외` | 필요 없는 항목을 빼고 제작합니다. |
| `확인` | 이 문제는 괜찮다고 표시합니다. |

빨간 표시나 `확인 필요`가 남아 있으면 먼저 확인하고 넘어가는 것이 좋습니다.

## 8. 칠판 배치 확인하기

검수가 끝나면 `보드` 화면에서 칠판에 어떻게 배치되는지 확인합니다.

할 수 있는 일:

- 문제 순서 바꾸기
- 한 줄에 몇 개를 놓을지 선택하기
- 칠판 색상 바꾸기
- 문제 크기 조절하기
- 필요 없는 문제 제외하기

왼쪽 목록에서 문제를 드래그하면 순서를 바꿀 수 있습니다.

## 9. 이미지 처리 단계

문제 하나를 선택하면 오른쪽 `자료` 탭에서 처리 단계를 고를 수 있습니다.

| 단계 | 설명 |
| --- | --- |
| `1단계 · 그대로 붙이기` | 원본 그대로 빠르게 넣습니다. |
| `2단계 · AI 변환` | 배경을 정리해 칠판에 어울리게 만듭니다. |
| `3단계 · 고화질` | 흐린 글자나 작은 수식을 더 선명하게 만듭니다. |

처음에는 대부분 `2단계`로 충분합니다. 글자가 작거나 흐린 문제만 `3단계` 또는 `AI 업스케일 재구성`을 사용하세요.

## 10. EDB 만들기

검수와 배치가 끝났다면 상단의 `EDB 제작` 버튼을 누릅니다.

제작이 끝나면 `.edb` 파일 다운로드가 시작됩니다.

자료가 많으면 ClassIn 제한에 맞춰 EDB 파일이 여러 개로 나뉠 수 있습니다. 이 경우 표시되는 순서대로 ClassIn에서 열어 확인하면 됩니다.

## 11. ClassIn에서 확인하기

다운로드된 `.edb` 파일을 ClassIn에서 열어 최종 확인합니다.

확인할 것:

- 칠판에 문제 순서가 맞는지
- 글자가 너무 작지 않은지
- 문제끼리 겹치지 않는지
- 지문과 보기, 그림, 표가 빠지지 않았는지

문제가 있으면 EDB 메이크로 돌아와 검수나 배치를 수정한 뒤 다시 제작합니다.

## 12. 문제 신고하기

사용 중 오류가 나거나 예상과 다르게 동작하면 오른쪽 `설정` 탭의
`문제 신고`에서 `버그 리포트`를 누릅니다.

1. 문제가 생기기 직전에 한 작업과 화면에 보인 현상을 적습니다.
2. 원인 확인에 도움이 필요하면 `진단 정보 포함`을 켭니다.
3. `리포트 보내기`를 누릅니다.
4. 화면에 표시되는 `EDB-...` 접수 번호를 메모합니다.

진단 정보에는 앱 버전, 운영체제, 오류 로그 일부가 포함될 수 있습니다.
원본 시험지, 작업 세션 내용, API 키, 전체 로컬 경로는 전송하지 않습니다.
진단 정보를 보내고 싶지 않으면 체크를 끈 뒤 설명만 보낼 수 있습니다.

## 12. 자주 묻는 질문

### API 키가 꼭 필요한가요?

기본 제작은 API 키 없이도 가능합니다. 다만 자동 인식 품질과 AI 보정 기능을 쓰려면 Gemini API 키를 넣는 것이 좋습니다.

### 어떤 버튼을 먼저 눌러야 하나요?

일반 문제지는 `문항 AI 인식`을 먼저 누르세요. 페이지를 그대로 넣고 싶다면 `페이지 PNG`를 누르면 됩니다.

### HWP 파일이 잘 안 됩니다

HWP/HWPX는 PC 환경에 따라 변환 품질이 달라질 수 있습니다. 잘 안 되면 HWP를 PDF로 저장해서 업로드하세요.

### 결과가 이상하게 잘렸습니다

`검수` 화면에서 `수동 쪼개기`, `합치기`, `AI 재인식`으로 수정할 수 있습니다.

### 비용이 많이 나올까 걱정됩니다

처음에는 몇 페이지만 테스트하세요. AI 재인식과 업스케일은 필요한 문제에만 쓰면 비용을 줄일 수 있습니다.

## 13. 빠른 사용 순서

1. EDB 메이크를 실행합니다.
2. 오른쪽 `칠판` 탭에서 Gemini API 키를 저장합니다.
3. 왼쪽 `자료`에 PDF나 이미지를 넣습니다.
4. `문항 AI 인식`을 누릅니다.
5. `검수` 화면에서 문제 영역을 확인합니다.
6. 필요하면 `수동 쪼개기`, `합치기`, `AI 재인식`으로 수정합니다.
7. `보드` 화면에서 순서와 크기를 확인합니다.
8. 상단의 `EDB 제작`을 누릅니다.
9. 다운로드된 `.edb`를 ClassIn에서 엽니다.
